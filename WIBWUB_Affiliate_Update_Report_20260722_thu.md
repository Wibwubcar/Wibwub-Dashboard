# รายงาน Affiliate Update — พฤหัสบดี 22 ก.ค. 2569

**สถานะ: ล้มเหลว (TikTok server error) — ไม่มีการอัปเดต dashboard**

## สรุปสั้นๆ

งานอัตโนมัติ (scheduled task) พยายามดึงข้อมูลจาก TikTok Affiliate Center เพื่ออัปเดต WIBWUB_Affiliate_Dashboard.html และ WIBWUB_Mobile.html แต่ติดปัญหาที่ปลายทาง TikTok เอง (ไม่ใช่ปัญหาจากฝั่งเรา) ทำให้ไม่สามารถดาวน์โหลดไฟล์ export ได้เลยแม้แต่ไฟล์เดียว จึงไม่มีการแก้ไขไฟล์ dashboard ใดๆ ในรอบนี้

## รายละเอียด

**สำเร็จ:**
- เปิด TikTok Affiliate Center → หน้า Transaction Analysis / Performance ได้ปกติ
- ตั้งช่วงวันที่เป็น 01/07/2026–21/07/2026 สำเร็จ (วันที่ 22 ยังไม่มีข้อมูลให้เลือก ตามพฤติกรรมปกติของ TikTok ที่ข้อมูลจะ lag 1 วัน)
- กดปุ่ม "ส่งออก" (export) สำเร็จทั้ง 4 แท็บ: ครีเอเตอร์, สินค้า, วิดีโอ, ไลฟ์สตรีม — สถานะขึ้นเป็น "กำลังส่งออก" แล้วเปลี่ยนเป็น "ดาวน์โหลด" (พร้อมดาวน์โหลด) ตามปกติ แต่ใช้เวลานานกว่าที่คาดไว้มาก (ปกติ 60-90 วินาที แต่รอบนี้ใช้เวลาราว 15-20 นาที และต้องรีโหลดหน้าเพื่อให้สถานะอัปเดต)

**ล้มเหลว — จุดที่ติดปัญหา:**
- เมื่อกดปุ่ม "ดาวน์โหลด" ที่แถวรายงานทั้ง 4 ไฟล์ ระบบไม่ส่งไฟล์มาให้เลย (ตรวจสอบแล้วว่าไม่มีไฟล์ใหม่ปรากฏใน Downloads)
- ตรวจสอบ network request เบื้องหลังพบว่า request ที่เรียกไฟล์จริง (`GET .../api/v1/oec/affiliate/compass/export_task/export?...task_id=...`) คืนค่า **HTTP 503 Service Unavailable** ทุกครั้ง
- ทดสอบซ้ำหลายรอบ (รอ 10s, 15s, 30s ระหว่างครั้ง) และทดสอบกับ task_id ที่ต่างกัน 2 ค่า — ได้ผลลัพธ์ 503 เหมือนกันทุกครั้ง
- สรุปว่าเป็นปัญหาฝั่งเซิร์ฟเวอร์ TikTok เอง (บริการ export ล่มชั่วคราว) ไม่ใช่ปัญหาการตั้งค่าหรือการดึงข้อมูลจากฝั่งเรา

**ไฟล์ที่ดาวน์โหลดมาโดยไม่ตั้งใจ:**
- ระหว่างพยายามหาปุ่มดาวน์โหลดที่ถูกต้อง มีการกดพลาดไปโดนปุ่ม "ตัวชี้วัดหลัก" (core stats overview) ทำให้ได้ไฟล์ `Core_Stats_20260701-20260721_....xlsx` มา 2 ชุด — ไฟล์นี้เป็นข้อมูลสรุปรวม 1 แถว (GMV รวม, จำนวนคำสั่งซื้อ ฯลฯ) **ไม่ใช่** ไฟล์รายละเอียดระดับ creator/product/video ที่ script ต้องใช้ จึงตัดสินใจไม่นำไฟล์นี้มาใช้ประมวลผล เพื่อไม่ให้ข้อมูลใน dashboard ผิดพลาด (ไฟล์ยังอยู่ใน Downloads หากต้องการตรวจสอบ)

## สิ่งที่ไม่ได้ทำในรอบนี้ (เนื่องจากไม่มีข้อมูลต้นทาง)

- ไม่ได้ย้ายไฟล์ไปยังโฟลเดอร์ Data Affiliate
- ไม่ได้แก้ไข WIBWUB_Affiliate_Dashboard.html (AF_GMV/AF_NET/AF_COM/AF_CR, VIDEOS, PRODUCTS)
- ไม่ได้แก้ไข WIBWUB_Mobile.html (AFI_GMV/AFI_NET/AFI_COMM)
- ไม่ได้ bump เวอร์ชัน cache ใน sw.js และไม่ได้สร้าง push_now.command (เพราะไม่มีข้อมูลอะไรให้ push)

## แนะนำ

1. ลองรันงานนี้ใหม่อีกครั้งในภายหลัง (เช่น อีก 1-2 ชั่วโมง หรือวันถัดไป) เพื่อดูว่าบริการ export ของ TikTok กลับมาใช้งานได้ปกติหรือยัง
2. หากยังพบ 503 ซ้ำอีก อาจต้องตรวจสอบสถานะบริการของ TikTok Seller/Affiliate Center โดยตรง หรือลองดาวน์โหลดผ่านเบราว์เซอร์ปกติด้วยตนเองเพื่อยืนยันว่าปัญหาไม่ได้เกิดเฉพาะจากการทำงานอัตโนมัติ
3. ไฟล์ Core_Stats ที่ดาวน์โหลดมาผิดโดยไม่ตั้งใจสามารถลบทิ้งได้ (อยู่ใน Downloads)
