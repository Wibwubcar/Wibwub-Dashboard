# WIBWUB Affiliate Update — วันอาทิตย์ 19 ก.ค. 2569 (automated run, wibwub-thursday-affiliate)

**สรุป: งานทับซ้อนกับรอบ `wibwub-monday-update` ที่รันไปก่อนหน้าเพียง ~20 นาที (commit `ebffe14`) ซึ่งอัปเดต Dashboard ถึง 1-18 ก.ค. ไปแล้ว — รอบนี้จึงเปลี่ยนไปทำการ "ตรวจสอบไขว้" (cross-validate) ด้วยไฟล์ export ครีเอเตอร์จริงที่เพิ่งดาวน์โหลดได้ (1-17 ก.ค.) แทนการอัปเดตซ้ำ ไม่มีการแก้ไขไฟล์ dashboard เพิ่มเติมในรอบนี้**

---

## ⚠️ พบว่างานถูกทำไปแล้วโดยรอบอื่นก่อนหน้า

ก่อนเริ่ม export ตรวจพบว่า:
- มีรายงาน `WIBWUB_Affiliate_Update_Report_20260719.md` (จากรอบ `wibwub-monday-update`) เขียนไว้เมื่อ 02:08 น. — ห่างจากรอบนี้เพียง ~20 นาที
- Git log ล่าสุด: `ebffe14 Weekly update: affiliate GMV/Net/Comm/Creators MTD (1-18)...` — commit แล้วบนดิสก์ (ยังไม่ push)
- Dashboard ปัจจุบัน (`WIBWUB_Affiliate_Dashboard.html`/`WIBWUB_Mobile.html`) มีข้อมูลถึง **1-18 ก.ค.** แล้ว: GMV=897,947 / Net=885,685 / Comm=103,159 / Creators=495 (ประมาณการ)
- `sw.js` อยู่ที่ `wibwub-v406` แล้ว

เพื่อไม่ให้เกิดการเขียนทับซ้อนกันหรือถอยหลังข้อมูล รอบนี้จึง **ไม่แก้ไข array ใน dashboard ซ้ำ** แต่ใช้เวลาที่มีตรวจสอบความถูกต้องของข้อมูลที่บันทึกไว้แทน

## ✅ ตรวจสอบไขว้ด้วยไฟล์ export ครีเอเตอร์จริง (1-17 ก.ค.)

พบไฟล์ `Transaction_Analysis_Creator_List_20260701-20260717.xlsx` (ดาวน์โหลดสำเร็จเมื่อ 01:42 น. ของเซสชันคู่ขนาน — ไฟล์จริงระดับแถว ไม่ใช่ KPI widget) คำนวณด้วย pandas ได้:

| ตัวชี้วัด (1-17 ก.ค., ของจริงจากไฟล์) | ค่า |
|---|---|
| GMV | ฿842,943.34 |
| การคืนเงิน | ฿11,952.14 |
| Net (GMV−คืนเงิน) | ฿830,991.20 |
| ค่าคอมมิชชั่น | ฿96,977.72 |
| Active Creators (GMV>0) | 489 |

**ผลตรวจสอบ**:
1. GMV ตรงกับค่าที่รายงาน 18 ก.ค. เคยบันทึกไว้จาก KPI widget (842,943) แทบเป๊ะ ✅ ยืนยันว่าวิธี fallback ด้วย KPI widget แม่นยำ
2. Active Creators = 489 ตรงกับตัวเลขที่รายงาน 18 ก.ค. ใช้ ✅
3. ค่าคอมมิชชั่น 96,977.72 ใกล้เคียงกับ 96,914 ที่เคยรายงาน (ต่างกัน <0.1%) ✅
4. **พบว่า "Net" ที่รายงาน 18 ก.ค. เคยระบุไว้ (793,554) คลาดเคลื่อนจากค่าจริง (830,991) อยู่มาก** — ค่า Net ที่ถูกต้องของรอบ 18 ก.ค. ควรใกล้เคียง 830,991 ไม่ใช่ 793,554 อย่างไรก็ตาม **รอบ `wibwub-monday-update` (19 ก.ค.) ได้แก้ไขวิธีคำนวณ Net ใหม่แล้ว** (ใช้ GMV−คืนเงินจากวิดเจ็ต KPI โดยตรง) ทำให้ Net ของ 1-18 ที่บันทึกไว้ปัจจุบัน (885,685 = 897,947−12,262) **สอดคล้องกับวิธีคำนวณที่ถูกต้องแล้ว ไม่ต้องแก้ไขซ้ำ**

## ⚠️ ข้อสังเกต: ตัวเลข Active Creators ประมาณการของ 1-18 (495) อาจต่ำไป

รอบ `wibwub-monday-update` บันทึก Active Creators 1-18 = 495 (ประมาณการ, ต่อยอดจาก 489 ด้วยอัตรา +3/วัน เพราะสันนิษฐานว่าการเติบโตช้าลงช่วงปลายเดือน)

แต่จากข้อมูลจริงที่ตรวจสอบได้ในรอบนี้: การเติบโตจริงจาก 1-16→1-17 คือ 459→489 = **+30 คน/วัน** ซึ่งสูงกว่าอัตราที่ใช้ประมาณ 1-18 มาก หากอัตราการเติบโตยังใกล้เคียงเดิม ตัวเลขจริงของ 1-18 อาจอยู่ที่ประมาณ 510-520 คน ไม่ใช่ 495

**ไม่ได้แก้ไขค่านี้ในรอบนี้** เพราะไม่มีไฟล์ export จริงของ 1-18 ยืนยัน (ยังติด 503) — เป็นเพียงข้อสังเกตให้ตรวจสอบซ้ำเมื่อ export ใช้งานได้

## ⛔ TikTok Creator/Product/Video export ยังคง 503 ต่อเนื่อง (ตรวจซ้ำอีกครั้ง)

เช็คสถานะ export ที่สร้างไว้ก่อนหน้า (task ที่กำลังส่งออก) — ยังคงค้างสถานะ "กำลังส่งออก" เช่นเดิม ไม่มีการเปลี่ยนแปลง สอดคล้องกับปัญหาที่ทั้งรายงาน 18 ก.ค. และ 19 ก.ค. (rond ก่อนหน้า) ระบุไว้แล้วว่าเป็นปัญหาต่อเนื่องของแพลตฟอร์ม ไม่ใช่ปัญหาชั่วคราว — ไม่ได้ลอง export ใหม่ซ้ำในรอบนี้เพื่อลดภาระระบบที่ซ้ำซ้อนกับรอบก่อนหน้า

## ✅ สินค้า (PRODUCTS cr/vid)

ไฟล์สินค้าล่าสุดที่มีบนดิสก์ยังเป็น `Transaction_Analysis_Product_List_20260701-20260715.xlsx` (ถึง 15 ก.ค. เท่านั้น, เก่ากว่าข้อมูลที่มีอยู่แล้ว) — ไม่มีไฟล์ใหม่กว่านี้ให้ใช้ จึงไม่มีการแก้ไข `PRODUCTS[].cr/.vid` ในรอบนี้ (สอดคล้องกับการตัดสินใจของรอบก่อนหน้า)

## ✅ sw.js / push_now.command / Git

- ไม่มีการแก้ไขไฟล์ dashboard เพิ่มเติมในรอบนี้ จึงไม่ bump เวอร์ชัน sw.js ซ้ำ — คงอยู่ที่ `wibwub-v406` (จากรอบก่อนหน้า)
- `push_now.command` มีอยู่แล้วจากรอบก่อนหน้า พร้อม commit `ebffe14` ที่ยังไม่ push — **รอผู้ใช้กดรันเอง**

---

## ▶️ ขั้นถัดไป (แนะนำ)

1. รัน `push_now.command` เพื่อ push การอัปเดต 1-18 ก.ค. (จากรอบ `wibwub-monday-update`) ขึ้น GitHub — ยังไม่ถูก push
2. รอบหน้า: เมื่อ export ครีเอเตอร์ใช้งานได้อีกครั้ง ให้ตรวจสอบตัวเลข Active Creators ของ 1-18 (495 เป็นค่าประมาณการที่อาจต่ำไป ค่าจริงอาจอยู่ราว 510-520)
3. พิจารณาลดความถี่/รวมรอบ automated affiliate update (`wibwub-thursday-affiliate` และ `wibwub-monday-update`) ที่ทับซ้อนกันในช่วงเวลาใกล้กันมาก (~20 นาที) ในสัปดาห์นี้ เพื่อลดภาระ export ซ้ำซ้อนกับ TikTok ที่กำลังมีปัญหา 503 อยู่แล้ว
