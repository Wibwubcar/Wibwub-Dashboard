# WIBWUB Affiliate Update — วันจันทร์ 20 ก.ค. 2569 (automated run)

**สรุป: อัปเดต dashboard สำเร็จครบทั้ง GMV/Net/Comm/จำนวนครีเอเตอร์ + สินค้า (cr/vid) ด้วยข้อมูลช่วง 1–19 ก.ค. 2569 ✅ วีดีโอ/ไลฟ์สตรีมยังดาวน์โหลดไม่ได้เหมือนเดิม (หน้า export เดิมถูกปิดถาวรแล้ว แต่ไม่กระทบการคำนวณ dashboard)**

---

## ✅ สิ่งที่ทำสำเร็จ

1. **ไฟล์ครีเอเตอร์ (Creator_List)** ช่วง 1–19 ก.ค. 2569 — 13,945 ราย
   - ต้อง retry export หลายรอบเพราะเจอ HTTP 503 ที่ endpoint ดาวน์โหลดไฟล์ (`export/file/{id}v2`) ซ้ำๆ กับหลาย file ID — ในที่สุดมีไฟล์ที่โหลดสำเร็จและมีข้อมูลครบ (13,945 แถว) เก็บไว้ที่ `Data Affiliate/ครีเอเตอร์/Creator_List_20260701-20260719_20260720015258.xlsx`
   - พบไฟล์ export อีกตัวที่โหลดสำเร็จภายหลังแต่ **มีแค่ header ไม่มีข้อมูล** (ผลจากปัญหา 503 ที่ backend) — ได้ย้ายไปเก็บเป็น `_EMPTY_ignore_...xlsx` เพื่อไม่ให้สับสนในอนาคต
   - สรุปยอดจากไฟล์จริง: GMV **฿974,987.78** · ค่าคืนเงิน **฿59,826.99** · Net **฿915,160.79** · ค่าคอมมิชชั่น **฿111,799.30** · ครีเอเตอร์ **13,945 ราย**

2. **ไฟล์สินค้า (ListProducts)** ช่วง 1–19 ก.ค. 2569 (71 สินค้า)
   - อัปเดต `cr` (ยอดขายจากครีเอเตอร์) และ `vid` (วิดีโอ) ของสินค้าหลัก 7 ตัวใน PRODUCTS array:
     - Leather Wipes: cr 31→239, vid 101→141
     - Interior Wipes: cr 23→153, vid 56→75
     - Sugar: cr 13→89, vid 91→115
     - Cleaner: cr 8→39, vid 15→18
     - Interior: cr 5→43, vid 55→70
     - Refresh: cr 0→33, vid 0→29
     - Visible: cr 1→11, vid 10→12
   - หมายเหตุ: ค่า cr/vid เดิมในไฟล์ dashboard ต่ำกว่าที่ควรจะเป็นมาก (ดูเหมือนไม่ได้ถูกอัปเดตมาสักพัก) — รอบนี้ mapping ตาม methodology เดียวกับรายงานวันที่ 17 ก.ค. (คอลัมน์ "ยอดขายจากครีเอเตอร์"/"วิดีโอ" ตรงๆ จากไฟล์ export) และ fuzzy-match ชื่อสินค้าในวงเล็บ ยืนยันตรงกันทั้ง 7 รายการ
   - เก็บไฟล์: `Data Affiliate/สินค้า/ListProducts_2026-07-01-2026-07-19_ALLPlan_20260720065613.xlsx`

3. **อัปเดต dashboard**
   - `WIBWUB_Affiliate_Dashboard.html`: `AF_CR` แก้จาก 13944 → 13945 (ค่า GMV/Net/Comm เดิมตรงกับไฟล์จริงอยู่แล้ว ไม่ต้องแก้) + อัปเดต `PRODUCTS[].cr/.vid` ทั้ง 7 ตัว
   - `WIBWUB_Mobile.html`: ตรวจสอบแล้ว `AFI_GMV/AFI_NET/AFI_COMM` ตรงกับข้อมูลจริงอยู่แล้ว ไม่ต้องแก้
   - ใช้กฎ overwrite เดือนล่าสุด ("ก.ค. (1-19)" มีอยู่แล้ว ไม่ได้ append ใหม่)

4. **Bump sw.js**: `wibwub-v419` → `wibwub-v420`

5. **อัปเดต `push_now.command`** ให้มี `git add` + `git commit` + `git push` ครบ (ของเดิมมีแค่ push อย่างเดียว) — รอผู้ใช้กดรันเอง เพื่อ commit + push การเปลี่ยนแปลงรอบนี้ขึ้น GitHub

---

## ⛔ สิ่งที่ทำไม่ได้ + เหตุผล

### วีดีโอ และ ไลฟ์สตรีม (STEP 2 tab 3-4)
- ยืนยันซ้ำอีกครั้งว่าไม่มีหน้า export เทียบเท่าในเมนูปัจจุบันแล้ว (sidebar มีแค่: ผลการดำเนินงาน, ตัวอย่างสินค้า, การเข้าถึงครีเอเตอร์, สินค้า, ครีเอเตอร์, ข้อเสนอแนะ) — ตรงกับที่รายงานวันที่ 17 ก.ค. เคยพบ
- ลองเข้า URL `/data/video-performance` โดยตรง → ระบบ redirect ไปหน้า "ผลการดำเนินงาน" (Performance overview) ซึ่งมีแค่ยอดรวม ไม่มีรายการต่อวิดีโอ/ไลฟ์ให้ export
- **ไม่กระทบผลลัพธ์รอบนี้** เพราะ dashboard ไม่เคยใช้ไฟล์วีดีโอ/ไลฟ์สตรีมในการคำนวณ (ใช้แค่ไฟล์ครีเอเตอร์และสินค้า)

### ปัญหา HTTP 503 ที่ export ครีเอเตอร์
- เกิดซ้ำกับ 5+ file ID ที่แตกต่างกัน ตลอดช่วง export job หลายรอบ — เป็นปัญหาฝั่ง TikTok backend ไม่ใช่ไฟล์เสียเฉพาะตัว
- ในที่สุดมีไฟล์นึงโหลดสำเร็จและมีข้อมูลครบ จึงใช้ไฟล์นั้นได้ แต่ควรระวังในรอบถัดไปว่าไฟล์ที่โหลดได้อาจว่างเปล่า (มีแค่ header) ต้องเช็คจำนวนแถวก่อนใช้เสมอ

---

## 📊 สถานะ dashboard ปัจจุบัน
- ก.ค. 2569 (1-19): GMV ฿974,988 · Net ฿915,161 · Comm ฿111,799 · ครีเอเตอร์ 13,945
- สินค้า 7 ตัวหลัก: cr/vid อัปเดตครบ ตรงกับ export ล่าสุด (1-19 ก.ค.)
- sw.js = v420, ไฟล์พร้อม push

## ▶️ ขั้นถัดไป (แนะนำ)
1. รัน `push_now.command` เพื่อ commit + push ขึ้น GitHub
2. พิจารณาปรับ workflow ให้เช็คจำนวนแถวของไฟล์ Creator_List อัตโนมัติก่อนใช้ (ป้องกันไฟล์ header-only จากปัญหา 503)
3. Video/Live export ยังหาทางทดแทนไม่ได้ — ถ้าไม่จำเป็นต้องใช้ในอนาคต อาจพิจารณาตัดออกจาก workflow ถาวร
