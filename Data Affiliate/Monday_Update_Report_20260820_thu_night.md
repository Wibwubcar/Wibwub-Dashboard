# WIBWUB Weekly Update — 20 ส.ค. 2569 (รอบกลางคืน)

Scheduled task `wibwub-monday-update` — รันแบบอัตโนมัติ (ผู้ใช้ไม่อยู่)
Commit: `37d9252` · sw.js `wibwub-v749` → `wibwub-v750`
สถานะ push: **ยังไม่ push** (sandbox ออกเน็ตไป GitHub ไม่ได้ — HTTP 403) → ดับเบิลคลิก `push_now.command` เพื่อ push

---

## STEP 0 — Protection check

`const M5` มี 8 label (ม.ค.–ส.ค.) ทั้ง `WIBWUB_Dashboard.html` และ `WIBWUB_Mobile.html` ซึ่ง ≥ เดือนปัจจุบัน (8) จึง **ไม่แตะต้อง** array นี้ ตามกฎเหล็ก

## STEP 1 — Shipnity export

ดึงจาก shipnity.com/data/c/purchase ช่วง 1 ส.ค. – 20 ส.ค. 2569 filter "สินค้าในออเดอร์"
ได้ไฟล์ `Data Shipnity/Data_20-08-2026.xlsx` (20.9 MB, 22,167 แถว, ออเดอร์ล่าสุด AP2276 เวลา 20/08/2026 20:36)
ไฟล์เดิม `Data_สิงหาคม.xlsx` (export 02:20 ของวันเดียวกัน) **ไม่ถูกทับ** — เก็บไว้เป็น backup

## STEP 2 — TikTok Affiliate export

Transaction Analysis → tab ครีเอเตอร์ → ช่วง 01/08 – 18/08/2569 (TikTok ยังอัปเดตข้อมูลถึง 18 ส.ค. เท่านั้น วันที่ 19–20 ยังเป็นสีเทา)
ได้ไฟล์ `Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Creator_List_20260801-20260818.xlsx` (505 KB, 6,073 แถว)

หมายเหตุ: การกดดาวน์โหลดครั้งแรกได้ไฟล์ผิด (`Transaction_Analysis_Core_Metrics_20260812-20260818.xlsx` — คนละ report คนละช่วง) จึงกดใหม่จนได้ Creator List ที่ถูกต้อง ไฟล์ผิดยังค้างอยู่ใน Downloads ลบทิ้งได้

## STEP 3 — Top Products

รวมไฟล์ Shipnity **86 ไฟล์** (ข้าม `Data_07-08-2026_export.xlsx` เพราะไม่ใช่ product-level) → 206,793 แถวหลัง dedup ด้วยคีย์ (เลขที่ออเดอร์, รหัสสินค้า, จำนวน)

ยอดรวมรายเดือน 2569: ม.ค. 9,072,564 · ก.พ. 7,594,735 · มี.ค. 7,213,504 · เม.ย. 7,368,461 · พ.ค. 8,101,284 · มิ.ย. 7,788,602 · ก.ค. 8,697,335 · ส.ค. (1-20) 6,392,448 — รวม ฿62,228,934 / 244,603 ชิ้น

**การจับคู่ชื่อสินค้า** — เดิมไม่มีสคริปต์ไหนสร้าง `ALL_PRODUCTS`/`PROD_MO` และชื่อใน dashboard เป็นชื่อ "จัดกลุ่มแล้ว" ไม่ใช่ชื่อดิบจาก Shipnity รอบนี้จึงสร้าง mapping ขึ้นมาแล้ว **ตรวจสอบย้อนหลัง**: mapping ที่ได้ให้ตัวเลข ม.ค.–ก.ค. **ตรงกับค่าเดิมในไฟล์เป๊ะทั้ง 15 รายการ (ผลต่าง 0 บาท)** จึงยืนยันได้ว่า mapping ถูกต้อง กลุ่มหนึ่งประกอบด้วยชื่อ listing หลัก + ชื่อ listing ภาษาไทยยาว/ภาษาอังกฤษของสินค้าเดียวกัน (+ แพ็ค 10 ผืน สำหรับผ้า) โดยขนาด 3L/5L นับแยกกลุ่ม

อัปเดต **เฉพาะ index 7 (ส.ค.)** ของ `PROD_MO` และคำนวณ `ALL_PRODUCTS.v`/`q` ใหม่เป็นผลรวม — ม.ค.–ก.ค. คงเดิมทุกตัว `mk`/`mkq` (งบการตลาด) คงเดิมเพราะคำนวณจากแถวราคา 0 ซึ่งไม่มีมูลค่าให้รวมใหม่ได้

| สินค้า | ส.ค. เดิม | ส.ค. ใหม่ |
|---|---|---|
| Wool Duster | 244,992 | 248,322 |
| Sugar 500ml | 560,357 | 576,159 |
| Interior 500ml | 415,168 | 424,077 |
| Refresh Wipes | 610,477 | 633,137 |
| Interior Wipes | 347,252 | 358,144 |
| Refresh 500ml | 265,243 | 274,706 |
| Spot 500ml | 299,653 | 306,907 |
| Perfect | 148,361 | 153,094 |
| Reflex 250ml | 117,124 | 120,299 |
| Tire & Trim | 145,110 | 146,566 |
| Martini | 146,964 | 150,497 |
| X-Glass | 176,093 | 180,977 |
| Sugar 3L | 116,850 | 118,320 |
| Quartz 1L | 122,319 | 125,527 |
| Mind 500ml | 68,726 | 70,977 |

Web Dashboard: อัปเดต KPI (ยอดขายรวม ฿62.05M → **฿62.23M**, สินค้าขายดีสุด ฿5.85M·9,176 → **฿5.86M·9,181**, จำนวนชิ้น 242K → **245K**) และคอลัมน์ "รวม (฿)" + "จำนวน" ของตารางทั้ง 15 แถว + แถวรวม

**ข้อจำกัดที่ตั้งใจข้าม:** คอลัมน์แยกช่องทาง (Shopee/TikTok/Lazada/Facebook/…) ในตาราง กราฟ `pr_top10` และโดนัท `pr_channel` **ไม่ได้อัปเดต** — ต้องรวมข้อมูลระดับช่องทาง (column 15 'ช่องทางติดต่อ') ใหม่ทั้ง 86 ไฟล์ ซึ่งเกินงบเวลาของ sandbox รอบนี้ (แต่ละคำสั่ง bash จำกัด 45 วินาที) และวิธีลัดแบบคิดส่วนต่างจากไฟล์ ส.ค. สองไฟล์ให้ผลไม่ตรง (ทดสอบแล้ว: ส่วนต่าง 2,070 vs 3,330 จริง) จึงเลือกไม่แตะแทนที่จะใส่ตัวเลขที่ผิด ตัวเลขช่องทางยังสะท้อนข้อมูลถึง ~19 ส.ค. คลาดเคลื่อนราว 0.3%

## STEP 4 — Affiliate arrays

Label สุดท้ายคือ "ส.ค. (1-18)" ตรงกับช่วงของไฟล์ใหม่ → **เขียนทับ index เดิม** (ไม่ append ไม่สร้าง array ใหม่ ไม่แตะเดือนก่อนหน้า)

| | เดิม | ใหม่ |
|---|---|---|
| GMV | 959,972 | **960,112** |
| Net | 904,366 | **940,905** |
| Commission | 112,674 | **112,989** |
| Creators | 515 | 515 |

ค่า Net เดิมผิดค่อนข้างมาก — GMV − การคืนเงิน จากไฟล์ = 960,112 − 19,208 = 940,905 ซึ่งตรงกับ KPI บนหน้า TikTok (GMV ฿960,112.18 / คืนเงิน ฿19,207.66) ส่วนค่าเดิม 904,366 ไม่ตรงกับไฟล์ใดเลย
อัปเดตทั้ง `AF_GMV/AF_NET/AF_COM/AF_CR` (Affiliate Dashboard) และ `AFI_GMV/AFI_NET/AFI_COMM` (Mobile) ให้ตรงกัน · KPI ข้อความ "฿960K · 515 creators" ยังถูกต้องอยู่แล้ว

## STEP 5 — Verification

- `M5` ยาว 8 ทั้งสองไฟล์ · `AF_*` ยาว 8 ครบทุก array · `AFI_*` ยาว 10 ครบทุก array
- `PROD_MO` ทุกรายการยาว 8 · `ALL_PRODUCTS` 15 รายการ ยังเรียงจากมากไปน้อยถูกต้อง
- diff ยืนยัน: เปลี่ยนเฉพาะ index 7 ของ `PROD_MO` และ index สุดท้ายของ affiliate arrays — ไม่มีเดือนเก่าถูกทับ
- `node --check` ผ่านทุก `<script>` block ของทั้ง 3 ไฟล์ (3 + 3 + 1 blocks, 0 errors)

## สิ่งที่ค้างไว้

1. **ต้อง push เอง** — ดับเบิลคลิก `push_now.command`
2. คอลัมน์/กราฟแยกช่องทางของหน้า Top สินค้าขายดี ควรรันรวมข้อมูลระดับช่องทางใหม่เมื่อมีเวลา
3. ข้อมูล TikTok Affiliate ยังตามหลัง 2 วัน (ถึง 18 ส.ค.) รอ TikTok อัปเดตแล้วค่อยดึงซ้ำ
4. ลบไฟล์ `Transaction_Analysis_Core_Metrics_20260812-20260818*.xlsx` ใน Downloads ได้ (ดาวน์โหลดผิด)
