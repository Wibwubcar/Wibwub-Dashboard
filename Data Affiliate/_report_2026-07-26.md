# WIBWUB Weekly Auto-Update — 2026-07-26 (Sunday run of "wibwub-monday-update")

## Summary: Partial update — Shipnity Top Products refreshed; Affiliate data blocked

### What was updated
- **Shipnity source file** (`Data Shipnity/Data_กรกฎาคม.xlsx`): refreshed export copied in, now covers 01/07/2026–26/07/2026 (27,175 rows).
- **Top Products** (`WIBWUB_Mobile.html` — `ALL_PRODUCTS` + `PROD_MO`, and `WIBWUB_Dashboard.html` — Top Products KPIs + table): recomputed from 7 curated month-to-date Shipnity files (one per month, Jan–Jul; dedup by order/product/qty key — mathematically equivalent to processing every daily snapshot, just faster). New totals: **฿54.08M** revenue / **210,069** units across all products (up from ฿52.99M / 205K). Top 15 list stayed the same 15 products, but two rank swaps occurred vs. the prior snapshot:
  - #5/#6: **Refresh 500ml spray** (฿2.06M) now edges out **Refresh Wipes** (฿2.04M)
  - #10/#11: **Tire&Trim 500ml** (฿1.32M) now edges out **Sugar 3L** (฿1.32M, essentially tied)
  - `mk`/`mkq` (marketing-budget/influencer-seeding fields) were preserved unchanged per product — those aren't part of the Shipnity revenue computation.
- **sw.js**: cache version bumped `wibwub-v465` → `wibwub-v466`.
- **Git**: committed `WIBWUB_Mobile.html`, `WIBWUB_Dashboard.html`, `sw.js` (commit `2c525c7`). `WIBWUB_Affiliate_Dashboard.html` was intentionally left out of this commit since its data wasn't touched (see below).

### What was NOT updated (and why)
- **Affiliate GMV/Net/Comm/Creators** (`AF_MO`/`AF_GMV`/`AF_NET`/`AF_COM`/`AF_CR` in `WIBWUB_Affiliate_Dashboard.html`, `AFI_MONTHS`/`AFI_GMV`/`AFI_NET`/`AFI_COMM` in `WIBWUB_Mobile.html`): **skipped entirely**. TikTok's Transaction Analysis export download (`export_task/export?...task_id=...`) returned **HTTP 503** consistently across 5+ attempts on two separate freshly-created export jobs (task IDs `01KYE5XZQW5QR59WXT7ZE5W347v2` and `01KYE6VKV7YT3Q8Y1HZMS41017v2`), each after a full 60–90s wait — confirmed via network-request inspection, not just UI symptoms, so this is a genuine platform-side outage rather than a stale-job or click issue.
  - **Bigger finding**: every historical `Transaction_Analysis_Creator_List_*.xlsx` file already sitting in `Data Affiliate/ครีเอเตอร์` (spanning weeks, June 18 through today) turned out to be a **1-column "Creator name" list**, not the 12-column GMV/Returns/Commission format this step expects. This suggests the export type may have silently stopped producing financial data some time ago, independent of today's 503s. No existing file was usable as a fallback — using one would have risked writing wrong numbers into the dashboards, so nothing was touched instead. **This needs human eyes on the TikTok Affiliate Center to confirm what changed.**
- **Per-channel breakdown** in the Top Products table (Shopee/TikTok/Lazada/Facebook/Line/etc. columns) and the two related charts (`pr_top10` stacked bar, `pr_channel` doughnut): **left unchanged** (stale). The official aggregation script for this step only reads the Shipnity 20-column product-level export, which has no identifiable per-channel column — recomputing these would have meant guessing, which was avoided. The grand-total row's overall ฿/qty were updated; the channel sub-totals (marked with `*`) were not.

## Files touched
- `Data Shipnity/Data_กรกฎาคม.xlsx` — refreshed (copied in, previous version not deleted)
- `WIBWUB_Mobile.html` — `ALL_PRODUCTS`, `PROD_MO` updated
- `WIBWUB_Dashboard.html` — Top Products KPI row + table (totals/qty/ranking only)
- `sw.js` — cache version bump
- `push_now.command` — already matched the required template, no change needed

## sw.js / git
- Bumped `wibwub-v465` → `v466`; committed `WIBWUB_Mobile.html` + `WIBWUB_Dashboard.html` + `sw.js` as commit `2c525c7`.
- Hit a stuck `.git/index.lock` (0 bytes, un-removable via `rm`/`mv`/`chmod` — the Google Drive-backed mount rejects unlinking it, "Operation not permitted"). Worked around it by pointing `GIT_INDEX_FILE` at a temp path instead of writing through the stuck lock; commit succeeded cleanly. The stale lock file itself is still sitting in `.git/` — if a future run hits the same "File exists" error on `git add`/`commit`, this same workaround (or a manual delete from Finder, which isn't subject to the same sandbox restriction) will clear it.
- `push_now.command` is unchanged and ready — still needs a manual double-click to push (sandbox has no push access, per usual).

## Automation notes
- No destructive actions taken; only additive/corrective edits to the files listed above, using `cp` (never `mv`/`rm`) for any Downloads-adjacent file handling.
- STEP 0 (M5 month-label protection check) was verified clean earlier in this run — no fix needed.

## Recommended follow-up
1. **Investigate TikTok Affiliate Center Transaction Analysis export** — figure out why recent exports are 1-column creator-name-only files instead of the expected 12-column GMV/Returns/Commission format, and whether the 503 downloads are a temporary outage or a longer-lived issue.
2. Once export data is fixable, re-run this step to bring Affiliate arrays current (they're stale from whatever the last successful run was).
3. If a per-channel breakdown of Top Products is wanted going forward, a separate data source/script will be needed — the Shipnity product-level export used here doesn't carry a channel column.
4. Run `push_now.command` to push commit `2c525c7` to GitHub/Netlify.
