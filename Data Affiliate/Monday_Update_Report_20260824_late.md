# WIBWUB Weekly Update — วันจันทร์ 24 ส.ค. 2569 (รอบดึก)

**สถานะ: สำเร็จ** · commit `9a11e3f` · sw.js `wibwub-v809` → `wibwub-v810`

---

## 🛡️ Pre-flight — M5 Protection

| ไฟล์ | `M5` | จำนวนเดือน | ผล |
|---|---|---|---|
| `WIBWUB_Dashboard.html` | `["ม.ค.".."ส.ค."]` | 8 | ✅ ตรงกับเดือนปัจจุบัน (8) |
| `WIBWUB_Mobile.html` | `["ม.ค.".."ส.ค."]` | 8 | ✅ ตรงกับเดือนปัจจุบัน (8) |

ไม่ต้องซ่อม

---

## STEP 1 — Shipnity export ✅

ดึงผ่าน Claude-in-Chrome จาก `shipnity.com/data/c/purchase` ช่วง **1 ส.ค. 2569 – 24 ส.ค. 2569**, filter "สินค้าในออเดอร์", ส่งออกแบบไฟล์เดียว

- `Data Shipnity/Data_24-08-2026.xlsx` — 24.6 MB, 26,154 แถว × 20 คอลัมน์
- คัดลอกทับเป็น `Data Shipnity/Data_สิงหาคม.xlsx`
- หลัง dedup `(เลขที่ออเดอร์, รหัสสินค้า, จำนวน, ราคา)` = 26,143 แถว · ยอดรวม **฿7,135,821** · 178 SKU

**หมายเหตุวิธีทำ (ควรบันทึกไว้ใช้ครั้งหน้า):** ปฏิทิน Vuetify ของ Shipnity คลิกด้วยพิกัดไม่ติด ต้องคลิกที่ *ตัวอักษร* ของช่องวันที่ (≈1320,168) เพื่อเปิดปฏิทิน แล้วสั่ง DOM click ผ่าน `javascript_tool`:
`[...document.querySelectorAll('.v-date-picker-table button')].find(b=>b.textContent.trim()==='1').click()` — เปิดใหม่แล้วทำซ้ำกับ `'24'`

---

## STEP 2 — TikTok Affiliate ⚠️ เปลี่ยนวิธีถาวร

**Transaction Analysis ถูกปิดใช้งานแล้ว** — `/insights/transaction-analysis` เด้งไปหน้าใหม่ "ผลการดำเนินงาน" และ `/data/creator-analysis` ขึ้นแบนเนอร์ "อัปเกรดการวิเคราะห์แล้ว … หน้านี้จะเลิกใช้งานเร็วๆ นี้" จึง **ไม่มีปุ่ม export Creator List ให้กดอีกต่อไป**

แก้โดยอ่านค่า KPI รวมจากหน้า "ผลการดำเนินงาน" โดยตรง (ตั้งช่วง กำหนดเอง 01/08–22/08) แทนการดาวน์โหลดไฟล์

**ข้อจำกัดข้อมูล:** วันที่ 23 และ 24 ถูกปิดในปฏิทิน — header ระบุ "อัปเดตเมื่อ: 22 ส.ค. 2026 0:00 (GMT+7:00)" ดังนั้นข้อมูล affiliate สูงสุดถึง **22 ส.ค.** เท่านั้น (เดือนไม่ครบ — บันทึกไว้ตามกฎ)

➡️ **ควรอัปเดต skill file ให้ใช้หน้า "ผลการดำเนินงาน" แทน Transaction Analysis**

---

## STEP 3 — Top Products: ไม่แก้ไข (no-op โดยตั้งใจ)

รอบเช้าวันนี้ (commit `778c7fd` — *"Top Products recalculated ม.ค.–24 ส.ค. (Shipnity 89 files, SKU alias merge)"*) ได้คำนวณ `ALL_PRODUCTS` + `PROD_MO` ใหม่ครบถึง 24 ส.ค. ไปแล้ว โดยใช้ไฟล์ Shipnity ทั้ง 89 ไฟล์ พร้อม merge SKU alias

รอบนี้ลองสร้างใหม่จากไฟล์รายเดือน 8 ไฟล์เพื่อตรวจสอบ ผลคือ **ตัวเลขที่ได้ต่ำกว่าของเดิมทุกกลุ่ม** จึงไม่เขียนทับ สาเหตุที่ต่ำกว่า:

1. `Data_กรกฎาคม.xlsx` เป็นไฟล์เดียวกับ `Data_31-07-2026.xlsx` (snapshot 31 ก.ค. 02:28) → ขาดยอดวันที่ 30–31 ก.ค.
2. `Data_มิถุนายน.xlsx` = snapshot 30 มิ.ย. 01:39 → ขาดยอดวันที่ 29–30 มิ.ย.
3. วิธีรอบเช้าใช้ SKU alias merge ซึ่งรวม listing ย่อยได้ครบกว่าการ map ด้วยมือ

ส่วนต่างที่พบ (mine − current): ม.ค.–พ.ค. ต่างกัน ±0.3–2% (สลับบวกลบ), มิ.ย. −0.3 ถึง −3%, ก.ค. **−1 ถึง −5% ทุกกลุ่ม** (สัญญาณชัดว่าไฟล์ต้นทางไม่ครบเดือน)

**ยืนตามกฎเหล็ก** — "อัปเดตเฉพาะ index ของเดือนปัจจุบัน ห้ามสร้าง array ใหม่ทั้งหมด" — และไม่เขียนทับข้อมูลที่สมบูรณ์กว่าด้วยข้อมูลที่ด้อยกว่า

**ผลพลอยได้:** ยืนยัน SKU mapping ถูกต้อง (ขนาดตัวเลขตรงกันทุกกลุ่ม) และพบกับดัก 2 จุด — `EWIP000001` (Interior wipes) กับ `EWIP000002` (Refresh wipes) ใช้ prefix 4 ตัวเดียวกันแต่คนละสินค้า ต้อง key ด้วยรหัสเต็ม · `SRFX110003` **ไม่ใช่** ส่วนหนึ่งของกลุ่ม Reflex (ถ้ารวมจะเกินไป 70%)

---

## STEP 4 — Affiliate arrays: ตรวจแล้ว ไม่ต้องแก้

เทียบค่าในไฟล์กับหน้า "ผลการดำเนินงาน" สด (01/08–22/08) → **ตรงกันทุกตัว**

**`WIBWUB_Affiliate_Dashboard.html`** (rolling window, 8 ช่อง)

```
AF_MO  = ["ม.ค.","ก.พ.","มี.ค. (1-31)","เม.ย. (1-30)","พ.ค. (1-31)","มิ.ย. (1-30)","ก.ค. (1-31)","ส.ค. (1-22)"]
AF_GMV = [0,0,655171,724241,951158,660557,1452748,1172768]
AF_NET = [0,0,642865,708855,937203,650042,1428498,1151786]
AF_COM = [0,0,82699,69021,104772,79293,169224,136066]
AF_CR  = [0,0,398,418,558,414,720,612]
```

**`WIBWUB_Mobile.html`** (rolling window, 10 ช่อง) — `AFI_MONTHS` / `AFI_GMV` / `AFI_NET` / `AFI_COMM` ค่าเดียวกัน ต่อท้ายด้วย `'สค.69 (1-22)'`

✅ ป้ายเดือนปัจจุบันอยู่ท้าย array แล้ว → ไม่ต้อง append · ไม่มีเดือนก่อนหน้าถูกแตะ · ความยาว array ตรงกันทุกชุด (8/8/8/8/8 และ 10/10/10/10)

**ข้อมูลรอ commit จากรอบเช้า** ที่เก็บเข้าไปด้วย: อัปเดต `ส.ค. 1-21` → `ส.ค. 1-22` ทั่วทั้งไฟล์ (ครีเอเตอร์ที่มียอด 583 → **612**, GMV รายคลิป/รายสินค้าใน `VIDEOS` + `PRODUCTS` 638 บรรทัด)

---

## STEP 5 — Commit ✅

```
sw.js: wibwub-v809 → wibwub-v810
commit 9a11e3f  auto-update: Monday 2026-08-24 — Shipnity + Affiliate + ภาพรวมธุรกิจ
  M WIBWUB_Affiliate_Dashboard.html  (+638 / −535)
  M sw.js
```

`WIBWUB_Mobile.html` และ `WIBWUB_Dashboard.html` ไม่มีการเปลี่ยนแปลง (commit ไปแล้วรอบเช้า)

⚠️ **sandbox push ไม่ได้ (HTTP 403 proxy)** — สร้าง `push_now.command` ไว้แล้ว **ดับเบิลคลิกเพื่อ push**

---

## STEP 6 — ตรวจสอบ ✅

- ไม่มีเดือนก่อนหน้าถูกเขียนทับในทุก array
- ความยาว array ทุกชุดสอดคล้องกัน
- `M5` ยังเป็น 8 เดือนทั้งสองไฟล์
- ตัวเลข affiliate ในไฟล์ = ตัวเลขบนหน้าเว็บสด
- `AF_CR` ช่องสุดท้าย (612) = ตัวเลข "ครีเอเตอร์ที่มียอด" บนการ์ด KPI ✅

---

## สรุปสิ่งที่ควรทำต่อ

1. **ดับเบิลคลิก `push_now.command`** เพื่อ push commit `9a11e3f`
2. **แก้ skill file STEP 2** — เปลี่ยนจาก Transaction Analysis เป็นหน้า "ผลการดำเนินงาน" (อ่าน KPI ตรง ไม่ต้อง export)
3. **แก้ skill file STEP 1** — เพิ่มวิธีตั้งวันที่ด้วย DOM click (พิกัดคลิกใช้ไม่ได้)
4. **แก้ session path ใน skill file** — ระบุ `/sessions/hopeful-serene-fermi/` แต่ของจริงคือ `/sessions/amazing-nice-bell/` (ควรเขียนเป็น path จริงบน Mac แทน)
5. ครั้งหน้าให้ดึง Shipnity เดือนที่จบแล้วในวันที่ 1–2 ของเดือนถัดไป — ไฟล์ `Data_มิถุนายน` / `Data_กรกฎาคม` ปัจจุบันเป็น snapshot ก่อนสิ้นเดือน ทำให้ยอดขาดไป
