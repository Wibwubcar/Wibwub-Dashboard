# รายงานอัปเดต Affiliate อัตโนมัติ — 2 ก.ย. 2026

**สถานะ: ✅ สำเร็จ (ข้าม STEP 5 โดยตั้งใจ — ดูเหตุผลด้านล่าง)**

---

## ⚠️ ข้อสังเกตสำคัญของรอบนี้

หน้า TikTok Affiliate แสดง **"อัปเดตเมื่อ: 31 ส.ค. 2026 0:00 (GMT+7:00)"**
ปฏิทินเดือน ก.ย. เป็นสีเทาทั้งเดือน (รวมวันที่ 1 ก.ย.) เลือกไม่ได้

จึงดึงช่วง **01/08/2026 – 31/08/2026** = เดือนสิงหาคมเต็มเดือน
**ไม่ append เดือน ก.ย.** เข้า array ใดๆ เพราะยังไม่มีข้อมูลจริง —
เขียนทับที่ index สุดท้าย (label `ส.ค.`) ตามกฎ rolling window
และแก้ label จาก `(1-30)` → `(1-31)` เพราะตอนนี้เป็นข้อมูลเต็มเดือนแล้ว

---

## STEP 1-3: ดาวน์โหลดไฟล์

ดึงครบทั้ง 4 แท็บ ผ่าน Chrome (macOS) — LaunchAgent `com.wibwub.download-mover`
ย้ายไฟล์เข้าโฟลเดอร์ปลายทางอัตโนมัติเรียบร้อย

| แท็บ | ไฟล์ | ขนาด |
|---|---|---|
| ครีเอเตอร์ | `Transaction_Analysis_Creator_List_20260801-20260831.xlsx` | 654,752 B |
| สินค้า | `Transaction_Analysis_Product_List_20260801-20260831.xlsx` | 19,676 B |
| วีดีโอ | `Transaction_Analysis_Video_List_20260801-20260831.xlsx` | 1,439,972 B |
| ไลฟ์สตรีม | `Transaction_Analysis_Live_List_20260801-20260831.xlsx` | 126,280 B |

**หมายเหตุเทคนิค:** แผง "รายงาน" ไม่ auto-refresh — ค้างที่ "กำลังส่งออก" นานกว่า 7 นาที
ต้อง reload หน้าเว็บถึงจะเห็นปุ่มดาวน์โหลด (การ reload ทำให้ตัวเลือกวันที่บนจอรีเซ็ตเป็น
"7 วันที่ผ่านมา" แต่งานที่ queue ไว้แล้วยังคงช่วง 1-31 ส.ค. ตามเดิม — ยืนยันจากชื่อไฟล์)

---

## STEP 4: อัปเดต KPI รายเดือน (ส.ค. เต็มเดือน 1-31)

| ตัวชี้วัด | ค่าเดิม (1-30) | ค่าใหม่ (1-31) | ส่วนต่าง |
|---|---|---|---|
| GMV | 1,680,573 | **1,730,879** | +50,306 |
| Net (GMV − คืนเงิน) | 1,653,141 | **1,701,979** | +48,838 |
| ค่าคอมมิชชั่น | 193,114 | **199,254** | +6,140 |
| ครีเอเตอร์ที่มียอดขาย | 828 | **853** | +25 |

แก้ในทั้ง 2 ไฟล์:
- `WIBWUB_Affiliate_Dashboard.html` → `AF_MO` / `AF_GMV` / `AF_NET` / `AF_COM` / `AF_CR` (บรรทัด 12017-12021) + KPI ครีเอเตอร์ (บรรทัด 299)
- `WIBWUB_Mobile.html` → `AFI_MONTHS` / `AFI_GMV` / `AFI_NET` / `AFI_COMM` (บรรทัด 879-882) + การ์ด Affiliate GMV (บรรทัด 391)

**แก้ column mapping ผิดในไฟล์งาน:** task file ระบุ `col[2]=คืนเงิน, col[10]=คอมมิชชั่น, iloc[1:]`
แต่ไฟล์ export จริงคือ `col[4]=การคืนเงิน, col[21]=ค่าคอมมิชชั่นโดยประมาณ, iloc[2:]`
(แถวที่ 1 เป็นแถวคำอธิบาย tooltip) — ตรวจสอบด้วยการรัน parser ที่แก้แล้วกับไฟล์ของเมื่อวาน
(`20260801-20260830`) ได้ 1,680,781 / 1,653,348 / 193,567 / 828 เทียบกับค่าที่เก็บไว้
1,680,573 / 1,653,141 / 193,114 / 828 — ตรงกันเกือบเป๊ะ ยืนยันว่า mapping ใหม่ถูกต้อง
👉 **ควรแก้ task file ให้ใช้ col 4 / 21 และ `iloc[2:]` ถาวร**

---

## STEP 5: PRODUCTS (cr / vid) — ⛔ ข้าม ไม่แก้ไข

**เหตุผล:** หา column ในไฟล์ export ที่ให้ค่าตรงกับที่เก็บไว้ไม่เจอเลย

ค่าที่เก็บในแดชบอร์ด: Leather Wipes `cr:42, vid:39` · Interior Wipes `24/23` · Sugar `15/15` ·
CLEANER `5/6` · Interior `5/5` · Refresh `3/3` · Visible `2/1`

ค่าจริงจาก export ช่วง 1-31 ส.ค. (Leather Wipes): `วิดีโอ=500`, `วิดีโอที่มียอดขาย=335`,
`ครีเอเตอร์ที่โพสต์=274`, `ครีเอเตอร์ที่มียอดขาย=451` — **ต่างกันประมาณ 10 เท่า**

ตรวจเพิ่มเติมแล้วว่าไม่ได้มาจาก array `VIDEOS` ของแดชบอร์ดเองเช่นกัน
(นับจาก VIDEOS ได้ 438 creators / 956 videos สะสม, หรือ 160/272 เฉพาะเดือน ส.ค.)

ปัญหาซ้อน: ชื่อสินค้าใน export ไม่ตรงกับ `PRODUCTS[].name`
(export มี `WIBWUB Sugar V.3`, `WIBWUB Refresh Leather Cleaner`, `WIBWUB Cleaner`
แต่ไม่มี `WIBWUB Interior wipes` / `WIBWUB Sugar` / `WIBWUB CLEANER` / `WIBWUB Refresh` เลย)

ตามกฎ *"ถ้าไม่แน่ใจ ให้หยุดและ log แทนการเดา"* จึง **ไม่แตะ `PRODUCTS`**
(รายงานรอบ 1 ก.ย. ก็สรุปว่า "ไม่ต้องแก้" เช่นกัน — สอดคล้องกัน)

👉 **ต้องให้คนตัดสินใจ:** ค่า `cr`/`vid` ควรหมายถึงอะไร และมาจาก column ไหน
ก่อนที่รอบถัดๆ ไปจะเขียนค่าได้จริง

---

## STEP 5B: รวมข้อมูลวิดีโอเข้า VIDEOS

รันผ่าน `scripts/vid_merge.py` (ตรวจแล้วว่าเวอร์ชันนี้ดึงเดือนจากชื่อไฟล์แล้ว → `aug` ถูกต้อง)

- อ่านจากไฟล์: **7,223** แถว
- VIDEOS เดิม **7,452** → ปัจจุบัน **7,554** รายการ (เพิ่มใหม่ **102**)
- อัปเดตค่า `aug` ของรายการเดิม: **91** รายการ
- ยอดรวม `monthly.aug` ทั้ง array: **1,447,082** (ก.ค. = 1,243,970)
- Safety guard ผ่าน: `entry_re` จับได้ 7,452 รายการ (> 0) จึงเขียนไฟล์
- ตรวจ invariant `gmv == sum(monthly)` → ไม่พบรายการผิดพลาด (0 mismatch)

---

## STEP 6: Service Worker

`sw.js` — `wibwub-v950` → **`wibwub-v951`**
สร้าง `push_now.command` ใหม่ (git add ทั้ง 3 ไฟล์ + commit + push)

---

## การตรวจสอบ (Verification)

- ✅ `git diff --numstat` — Affiliate Dashboard +327/-225, Mobile +5/-5, sw.js +1/-1 (ต่ำกว่าเกณฑ์ 50% มาก)
- ✅ ตรวจทุก inline `<script>` ด้วย `node --check` → Affiliate 1 block, Mobile 3 blocks, **0 error**
- ✅ eval บล็อก `VIDEOS` ด้วย node → parse ผ่าน 7,554 รายการ
- ✅ สำรองไฟล์ก่อนแก้ไว้ที่ outputs (`AFF_backup_20260902.html`)

---

## สิ่งที่ต้องทำต่อ

1. ดับเบิลคลิก **`push_now.command`** เพื่อ commit + push ขึ้น GitHub
2. **แก้ column mapping ใน task file** เป็น col 4 / col 21 / `iloc[2:]` (ดู STEP 4)
3. **ตัดสินใจเรื่อง `PRODUCTS.cr` / `.vid`** ว่านิยามคืออะไรและมาจาก column ไหน (ดู STEP 5)
4. รอบหน้า เมื่อ TikTok ปล่อยข้อมูล ก.ย. แล้ว ให้ **append** `"ก.ย. (1-N)"` เข้าทุก array พร้อมกัน
