# WIBWUB Weekly Auto-Update — 2026-07-26, run 2 (Sunday, "wibwub-monday-update")

## Summary: Partial update — Shipnity Top Products re-refreshed (with a data-quality fix); Affiliate step skipped

### Important: found and fixed a bad export from earlier in this same run
While preparing the STEP 1 export, the file copied into `Data Shipnity/Data_กรกฎาคม.xlsx` turned out to be the **wrong Shipnity report** — a 77-column order/customer-detail export (with PII: names, addresses, phone numbers, national ID) instead of the expected 20-column product-level export. This was caught during structure verification before any aggregation ran. Root cause: multiple xlsx exports were downloaded in the same browser session and the wrong one was picked up as "newest."

Fix applied: found `Data_26-07-2026.xlsx` already in the same folder — correct 20-column product-level format (`รหัสสินค้า`, `สินค้า`, `ราคา`, `จำนวน`, `เลขที่ออเดอร์`, ...), 27,653 rows, data current through 26 Jul 20:15 — and copied that over `Data_กรกฎาคม.xlsx` instead. No file was deleted; the bad copy was simply overwritten with the correct one before it could be used downstream.

### What was updated
- **Top Products** (`WIBWUB_Mobile.html` — `ALL_PRODUCTS` + `PROD_MO`; `WIBWUB_Dashboard.html` — Top Products KPIs + table): recomputed from 7 curated month-to-date Shipnity files (Jan–Jul, dedup by order/product/qty/price key). New totals: **฿54.32M** revenue / **211,099** units (up from ฿54.08M / 210,069 earlier today). Same top-15 product list and ranking as the morning run — no rank changes this time, just incremental growth from ~10 more hours of July order data.
  - `mk`/`mkq` fields preserved unchanged per product.
  - Per-channel breakdown columns/charts (`pr_top10`, `pr_channel`) intentionally left stale, same as the morning run — the Shipnity product-level export has no channel column.
- **sw.js**: cache version bumped `wibwub-v469` → `wibwub-v470`.
- **Git**: committed `WIBWUB_Mobile.html`, `WIBWUB_Dashboard.html`, `sw.js` as commit `5684a76`.

### What was NOT updated (and why)
- **Affiliate arrays** (`AF_*` in `WIBWUB_Affiliate_Dashboard.html`, `AFI_*` in `WIBWUB_Mobile.html`): skipped. These were already refreshed earlier today (commit `9e228c0`) and TikTok's Transaction Analysis export is still blocked (session expired / 503s per the other reports from today). No new attempt was made to re-login (outside policy), and reprocessing with partial data would risk degrading the already-good data from the morning run.

## Files touched
- `Data Shipnity/Data_กรกฎาคม.xlsx` — replaced with correct product-level export (was briefly wrong mid-session; fixed before use)
- `WIBWUB_Mobile.html` — `ALL_PRODUCTS`, `PROD_MO` updated
- `WIBWUB_Dashboard.html` — Top Products KPI row + table (totals/qty/ranking only)
- `sw.js` — cache version bump v469 → v470

## Git
- Commit `5684a76` on top of `45c6dc3`. Local branch remains diverged from origin (26 ahead / 1 behind) — consistent with earlier reports today; unrelated to this change.
- Hit the same known `.git/index.lock` / Google-Drive-mount unlink restriction as prior runs. Worked around via a temporary `GIT_INDEX_FILE` for `add`+`commit`, then copied that temp index back over the real `.git/index` so `git status` reflects the true post-commit state cleanly (previous runs' reports didn't mention this follow-up step, but it's needed so the working tree doesn't appear to have phantom staged changes afterward).
- `push_now.command` already exists and is correctly configured (add+commit+pull+push via double-click) — unchanged, still needs a manual push by the user.

## Verification performed
- `M5` array: still 7 entries (`ม.ค.`–`ก.ค.`) in both dashboard files — correct, no fix needed.
- Top-15 product `v`/`q` sum (฿32.26M / 143,728 units) is consistent with being a subset of the ฿54.32M / 211,099 grand total.
- Confirmed via `git show --stat` that the commit touched only the 3 intended files.
- Confirmed Affiliate arrays were not touched by this commit (untouched since `9e228c0`).

## Recommended follow-up
1. Run `push_now.command` to push commits `2c525c7`, `5684a76`, and others to GitHub/Netlify (26 commits currently ahead of origin).
2. TikTok Affiliate Center login still needs a human to re-authenticate — automated attempts continue to hit expired-session redirects / 503s on Transaction Analysis exports.
3. Consider adding a stricter file-identification check to the Shipnity export step (e.g., verify column count/header before copying into `Data_<month>.xlsx`) to prevent a repeat of today's near-miss with the wrong export format.
