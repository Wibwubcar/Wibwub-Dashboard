# WIBWUB Affiliate Update — 2026-08-06 (รอบ 2 / PM)

## ✅ สำเร็จ — ดาวน์โหลดครบทั้ง 4 ไฟล์ + อัปเดต Dashboard

หมายเหตุสำคัญ: รอบนี้เป็นการรันซ้ำในวันเดียวกัน หลังจากตรวจสอบไฟล์จริงพบว่า **การแก้ไขในรายงาน `Affiliate_Update_Report_20260806.md` (รอบเช้า) ไม่ได้ถูกบันทึกลงไฟล์จริง** — AF_GMV/AF_NET/AF_COM/AF_CR ใน Dashboard และ Mobile ยังคงเป็นค่าเดิมก่อนอัปเดต (141,141 ไม่ใช่ 141,181 ตามที่รายงานไว้) และ `push_now.command` ก็ยังเป็นเวอร์ชัน push-only ไม่มี add+commit ตามที่รายงานไว้ว่าแก้แล้ว รอบนี้จึงอัปเดตทั้งหมดใหม่โดยอ้างอิงจากข้อมูลสดล่าสุด (1–4 ส.ค.)

### 1. ดาวน์โหลดไฟล์ (ช่วงวันที่ 01/08/2026–04/08/2026)
ใช้หน้า "ผลการดำเนินงาน" → ส่วน "รายละเอียด" (4 แท็บ: ครีเอเตอร์/สินค้า/วีดีโอ/ไลฟ์สตรีม) — หน้า Transaction Analysis แบบเดิมถูกย้ายมาซ้อนอยู่ในหน้านี้แล้ว
- **ครีเอเตอร์**: `Transaction_Analysis_Creator_List_20260801-20260804.xlsx` (3,410 ครีเอเตอร์)
- **สินค้า**: `Transaction_Analysis_Product_List_20260801-20260804.xlsx` (72 สินค้า)
- **วีดีโอ**: `Transaction_Analysis_Video_List_20260801-20260804.xlsx` (3,247 แถว — ไฟล์นี้รวมวิดีโอทั้งหมดที่มีธุรกรรมในช่วงหน้าต่าง 7 วันของระบบ ไม่ใช่เฉพาะที่โพสต์ในช่วง 1-4 ส.ค.; มีเพียง 182 แถวที่ GMV > 0)
- **ไลฟ์สตรีม**: `Transaction_Analysis_Live_List_20260801-20260804.xlsx`

ไฟล์ทั้ง 4 ถูกย้ายเข้าโฟลเดอร์ย่อยที่ถูกต้องโดยอัตโนมัติ (LaunchAgent บนเครื่อง) ก่อนที่จะเริ่มประมวลผล

### 2. อัปเดต Affiliate Trend (AF_*/AFI_*) — ส.ค. (1-4)
อัปเดตจากค่าเดิม (ส.ค. 1-3 ที่ยังไม่ถูกบันทึกจริง = 141,141) เป็นค่าใหม่ (ส.ค. 1-4):

| | เดิม (1-3, ค่าจริงในไฟล์) | ใหม่ (1-4) |
|---|---|---|
| GMV | 141,141 | **202,794** |
| Net GMV | 137,763 | **197,668** |
| Commission | 16,142 | **23,194** |
| ครีเอเตอร์ที่มียอด (GMV>0) | 133 | **172** |

อัปเดตทั้ง `WIBWUB_Affiliate_Dashboard.html` (AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR) และ `WIBWUB_Mobile.html` (AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM) — label เปลี่ยนจาก "(1-3)" เป็น "(1-4)"

### 3. PRODUCTS cr/vid — **ไม่ได้แก้ไข** (ยังคงเหตุผลเดิม)
ไฟล์สินค้ารอบนี้มีปัญหาเดิม: คอลัมน์ "วิดีโอ"/"ไลฟ์สตรีม" เป็นตัวเลขเฉพาะช่วง 4 วันนี้เท่านั้น (ไม่สะสม) ในขณะที่ `cr`/`vid` ในตาราง PRODUCTS ปัจจุบันดูเป็นตัวเลขสะสม ไม่มีคอลัมน์ "จำนวนครีเอเตอร์" ตรงตัวให้ใช้ — สถานะยังไม่ชัดเจนเหมือนรอบก่อน จึง **skip เหมือนเดิม** เพื่อไม่ให้ข้อมูลสะสมเสียหาย รอการยืนยัน logic ที่ถูกต้อง

### 4. VIDEOS array — ผสานสำเร็จ
- จากวิดีโอ 182 แถวที่มี GMV > 0 ในไฟล์: จับคู่กับสินค้า 7 รายการที่มีอยู่ใน PRODUCTS canonical list ได้ **136 แถว**; อีก 46 แถว (รวม GMV ~฿40,356) เป็นสินค้าอื่นที่ไม่มีอยู่ใน PRODUCTS array เลย (เช่น Spot Clean, Reflex V.2, JULIA, Martini, Graphene sealant, Refresh Leather Cleaner, X-Glass Shield, Tire Clean, Quartz Shampoo ฯลฯ) — **ไม่ได้เพิ่มเข้า VIDEOS** เพราะ dashboard ปัจจุบันติดตามเฉพาะ 7 สินค้าหลัก
- จาก 136 แถวที่จับคู่ได้: **124 วิดีโอ** ตรงกับ vid_id ที่มีอยู่แล้วในระบบ → อัปเดตค่า `monthly.aug` เป็นยอดสะสมล่าสุดของช่วง 1-4 ส.ค. (แทนที่ค่าเดิม) และ recompute `gmv` รวมใหม่ (=ผลรวม mar-aug); ไม่แตะ `units` เพราะไม่มีข้อมูลแยกรายเดือนที่เชื่อถือได้พอจะคำนวณส่วนต่าง
- **เพิ่มวิดีโอใหม่ 12 รายการ** ที่ไม่เคยมีในระบบมาก่อน (vid_id ใหม่) เข้า VIDEOS array
- ตรวจสอบด้วย `node --check` ผ่าน, vid_id ไม่ซ้ำกันเลย (2,346 รายการ unique, จากเดิม 2,334)

### 5. sw.js + push_now.command
- Bump cache version: `wibwub-v586` → **`wibwub-v587`** (ค่าจริงในไฟล์คือ v586 ไม่ใช่ v576/v585 ตามที่รายงานก่อนหน้าระบุ — ยืนยันแล้วว่ามีการอัปเดตแทรกจากรอบอื่นระหว่างวัน)
- แก้ `push_now.command` ให้ทำ `git add -A` + `git commit` + `git push` ในคลิกเดียว (ไฟล์เดิมยังเป็น push-only แม้รายงานรอบเช้าจะระบุว่าแก้แล้ว)
- **ยังไม่ได้ push** — กรุณาดับเบิลคลิก `push_now.command` บนเครื่อง Mac เพื่อ commit+push การเปลี่ยนแปลงทั้งหมด (หมายเหตุ: `git status` พบว่ามีไฟล์อื่นที่ถูกแก้ไขจากกระบวนการอื่นด้วย เช่น WIBWUB_Dashboard.html, data content/Followers_wibwubcar.zip, scripts/auto_push.log — สคริปต์ใช้ `git add -A` จะ commit ไฟล์เหล่านั้นไปด้วย)

### สรุปไฟล์ที่แก้ไข
- `WIBWUB_Affiliate_Dashboard.html` — AF_* arrays, VIDEOS array (124 updated + 12 new)
- `WIBWUB_Mobile.html` — AFI_* arrays
- `sw.js` — cache version bump (v586→v587)
- `push_now.command` — เพิ่ม add+commit (แก้ซ้ำ เพราะรอบก่อนไม่ persist)

### ⚠️ รายการที่ต้องตัดสินใจ/ติดตามต่อ
1. **รายงานรอบเช้า (20260806.md) ไม่ตรงกับไฟล์จริง** — ควรตรวจสอบ workflow ว่าทำไมการแก้ไขจากรอบก่อนถึงไม่ถูกบันทึก (เป็นไปได้ว่ารันใน sandbox ที่แยกจาก mount จริง หรือ error หลัง edit โดยไม่มีการแจ้งเตือน)
2. **PRODUCTS.cr/vid logic ไม่ชัดเจน** (สะสม vs รายช่วง) — ยังต้องยืนยันก่อนเขียนทับ เป็นรอบที่ 2 ที่ถูก skip ด้วยเหตุผลเดียวกัน
3. **46 วิดีโอ unmapped (GMV รวม ~฿40,356)** — เป็นสินค้านอกกลุ่ม 7 รายการหลักที่ dashboard ติดตาม หากต้องการเห็นวิดีโอของสินค้าเหล่านี้ใน dashboard ต้องขยาย PRODUCTS canonical list ก่อน
4. **VIDEOS.units ไม่ได้อัปเดตสำหรับ 124 วิดีโอที่ merge** — เฉพาะ `gmv`/`monthly.aug` เท่านั้นที่อัปเดต เพราะไม่มีข้อมูลแยกรายเดือนสำหรับ units
5. ต้องรัน `push_now.command` ด้วยตนเองเพื่อ commit+push ขึ้น git (สคริปต์จะ commit ไฟล์อื่นที่ถูกแก้จากกระบวนการอื่นไปด้วย — ตรวจสอบก่อนหากต้องการแยก commit)
