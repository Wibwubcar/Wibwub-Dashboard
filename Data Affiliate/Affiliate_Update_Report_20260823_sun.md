# WIBWUB Affiliate Auto-Update — อาทิตย์ 23 ส.ค. 2569

**สถานะ: สำเร็จทุกขั้นตอน** · commit `66d3c6b`

## 1. ไฟล์ที่ดาวน์โหลด (ช่วง 1–20 ส.ค. 2569)

วันที่ล่าสุดที่ TikTok เปิดให้เลือกยังเป็น **20 ส.ค.** (ข้อมูล affiliate ดีเลย์ ~3 วัน) จึงเป็นช่วงเดียวกับรอบเมื่อวาน แต่ตัวเลขถูก settle ใหม่แล้ว

| Tab | ไฟล์ | ขนาด |
|---|---|---|
| ครีเอเตอร์ | `Transaction_Analysis_Creator_List_20260801-20260820.xlsx` | 556 KB |
| สินค้า | `Transaction_Analysis_Product_List_20260801-20260820.xlsx` | 19 KB |
| วีดีโอ | `Transaction_Analysis_Video_List_20260801-20260820.xlsx` | 1.21 MB |
| ไลฟ์สตรีม | `Transaction_Analysis_Live_List_20260801-20260820.xlsx` | 82 KB |

ทั้ง 4 ไฟล์ถูก LaunchAgent `com.wibwub.download-mover` ย้ายเข้าโฟลเดอร์ย่อยของ `Data Affiliate/` เรียบร้อย

## 2. ตัวเลขหลัก ส.ค. 1–20

| รายการ | ค่า | รอบก่อน |
|---|---|---|
| GMV | ฿1,078,838 | ฿1,078,689 |
| Net GMV | ฿1,058,962 | ฿1,058,813 |
| Returns | ฿19,876 | — |
| Commission | ฿125,714 | ฿125,424 |
| ครีเอเตอร์ที่มียอด | 566 | 566 |

อัปเดตแล้วใน `AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR` (Affiliate Dashboard) และ `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM` (Mobile) — ตรวจ label ตัวสุดท้ายเป็น `"ส.ค. (1-20)"` / `'สค.69 (1-20)'` ก่อนเขียนทับ index สุดท้าย ไม่มีการแตะเดือนก่อนหน้า

## 3. PRODUCTS (cr / vid)

| สินค้า | cr | vid |
|---|---|---|
| Leather Wipes | 37 → **38** | 35 |
| Interior Wipes | 23 | 22 |
| Sugar | 15 | 16 → **15** |
| Cleaner | 6 | 7 |
| Interior | 5 | 5 |
| Refresh | 3 → **4** | 4 |
| Visible | 2 | 2 |

แก้เฉพาะ `cr`/`vid` — ไม่แตะ gmv/units/monthly/ret

## 4. VIDEOS

- entries เดิม 6,304 → **6,306**
- อัปเดตค่า ส.ค. **82 รายการ** (ในนั้น 14 รายการเพิ่งมียอด ส.ค. ครั้งแรก จึงอัปเดต `date` label ด้วย)
- เพิ่มใหม่ **2 รายการ** (`ap_review999`, `bambamflukesrii`)
- ยอด ส.ค. รวมในอาร์เรย์: ฿849,754 → **฿897,216** (ตรงกับไฟล์ export ฿897,235 ต่างเพราะปัดเศษ)
- ตรวจแล้ว: `gmv == sum(monthly)` ครบ 6,306 รายการ, ไม่มีรายการไหนถูกลดค่าลง, ไม่มีรายการเดิมหาย

## 5. sw.js

`wibwub-v785` → **`wibwub-v786`** · `push_now.command` ถูกต้องอยู่แล้ว ไม่ต้องแก้

---

## ⚠️ สิ่งที่ต้องแก้ใน SKILL.md (column mapping เก่าใช้ไม่ได้แล้ว)

**ไฟล์ครีเอเตอร์** — SKILL.md ระบุ `col[2]=returns, col[3]=orders, col[10]=commission, df.iloc[1:]` ซึ่งผิดหมด ถ้าใช้ตามนั้นจะได้ net = GMV − LIVE_GMV และ commission = ค่า CTOR% ที่ถูกต้องคือ:

```
col[1]=GMV  col[4]=การคืนเงิน  col[21]=ค่าคอมมิชชั่นโดยประมาณ   → ใช้ df.iloc[2:] (มี header 2 แถว)
```

**ไฟล์สินค้า** — `vid` ต้องอ่านจาก `col[13] (วิดีโอที่มียอดขายเฉลี่ยรายวัน)` ไม่ใช่ `col[9] (วิดีโอ)` ซึ่งเป็นจำนวนวิดีโอทั้งหมด (237/257/122…) และ `cr` มาจาก `col[19] (ครีเอเตอร์ที่มียอดขายเฉลี่ยรายวัน)` ไม่ใช่ col[18]

**KPI strip แท็บสินค้า** — ตอนนี้เป็น dynamic (id `kk-prod-*` เติมด้วย JS จาก PRODUCTS) ไม่มีข้อความ `ผ่าน X,XXX creators` แบบ hardcode แล้ว จึงไม่ต้องแก้มือ

**ไฟล์วีดีโอ** — คอลัมน์ `รหัสสินค้า` (F) อาจมี product id หลายตัวคั่นด้วย comma ต้อง split ก่อน map ชื่อสินค้า ไม่งั้นจะได้ `product:'Unknown'`

## หมายเหตุอื่น

- แผง reports ค้างที่ "กำลังส่งออก" ต้องกด F5 รีโหลดหน้าถึงจะเห็นปุ่มดาวน์โหลด (date picker จะรีเซ็ต แต่ไม่มีผล เพราะ export ถูก queue ด้วยช่วงที่ถูกต้องไปแล้ว)
- นาฬิกา sandbox เป็น UTC ส่วน Mac เป็น GMT+7 — timestamp ไฟล์จะดูช้ากว่าจริง 7 ชม.

---

## 👉 ขั้นตอนถัดไป (ต้องทำเอง)

**ดับเบิลคลิก `push_now.command`** เพื่อ push ขึ้น GitHub — ยังไม่ได้ push ให้
