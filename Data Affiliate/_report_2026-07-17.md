# WIBWUB Affiliate Update — วันศุกร์ 17 ก.ค. 2569 (automated run)

**สรุป: อัปเดต dashboard สำเร็จด้วยข้อมูล GMV/Net/Comm + สินค้า (cr/vid) แต่ "จำนวนครีเอเตอร์ (AF_CR)" อัปเดตไม่ได้ — TikTok เปลี่ยน UI และ export per-creator list พังถาวร (503)**

---

## ✅ สิ่งที่ทำสำเร็จ

1. **ดึงข้อมูล GMV/Net/Comm รวม (Core Stats)** ช่วง 1–16 ก.ค. 2569
   - GMV **฿788,438** · Net **฿740,580** · ค่าคอมมิชชั่น **฿90,785**
   - ตรวจสอบไขว้กับหน้า Performance overview แล้วตรงกัน (GMV, Comm match)
   - เซฟไฟล์: `Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Core_Metrics_20260701-20260716.xlsx`

2. **ดึงข้อมูลสินค้า (Product List)** ช่วง 1–16 ก.ค. 2569 (71 สินค้า)
   - อัปเดต `cr` (ยอดขายจากครีเอเตอร์) และ `vid` (วิดีโอ) ของสินค้าหลัก 7 ตัวใน PRODUCTS array:
     - Leather Wipes: cr 160→192, vid 84→104
     - Interior Wipes: cr 119→131, vid 51→58
     - Sugar: cr 65→77, vid 74→95
     - Cleaner: cr 37→39, vid 11→15
     - Interior: cr 30→37, vid 43→53
     - Refresh: cr 29→32, vid 21→25
     - Visible: cr 9→9, vid 7→9
   - เซฟไฟล์: `Data Affiliate/สินค้า/Transaction_Analysis_Product_List_20260701-20260716.xlsx`

3. **อัปเดต dashboard ทั้ง 2 ไฟล์** (overwrite index ก.ค. เดิม เพราะเป็นเดือนล่าสุดอยู่แล้ว ไม่ได้ append ใหม่)
   - `WIBWUB_Affiliate_Dashboard.html`: `AF_MO/AF_GMV/AF_NET/AF_COM` + `PRODUCTS[].cr/.vid`
   - `WIBWUB_Mobile.html`: `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM`
   - Label เปลี่ยนจาก "(1-15)" → "(1-16)"

4. **Bump sw.js**: `wibwub-v394` → `wibwub-v395`

5. **สร้าง `push_now.command`** ให้พร้อม (git add + commit + push) — รอผู้ใช้กดรันเอง

---

## ⛔ สิ่งที่ทำไม่ได้ + เหตุผล

### จำนวนครีเอเตอร์ (AF_CR / "435") — **ไม่ได้อัปเดต ยังคงค่าเดิมจากช่วง 1-15 ก.ค.**
- หน้าเก่า "Transaction Analysis" (4-tab) ที่ task นี้อ้างอิงถูก TikTok **redesign ใหม่** แล้ว — เปลี่ยนเป็นหน้า "ผลการดำเนินงาน" (มี banner แจ้งว่าหน้าเก่าจะเลิกใช้เร็วๆ นี้)
- ปุ่ม export ที่ตาราง "ครีเอเตอร์" (per-creator list, 12,243 แถว) **export job สำเร็จ แต่ download link ตอบ HTTP 503 ทุกครั้ง** (ลองแล้ว 2 export jobs, retry หลายรอบ) — คาดว่าเป็นข้อจำกัดขนาดไฟล์/timeout จาก backend TikTok
- ลองทางเลือกอื่น: หน้า Performance/custom-range โชว์แค่ "ค่าเฉลี่ยรายวัน" ไม่ใช่ยอดรวมทั้งช่วง จึงใช้แทนไม่ได้
- หน้า creator-analysis เดิม (ที่ยังพอมี fallback) ก็โหลดตารางไม่ขึ้นแล้วเพราะถูก deprecate
- **ตัดสินใจ: ไม่เดาตัวเลข** — ปล่อย AF_CR = 435 (ค่าจากรอบ 1-15 ก.ค.) ไว้ก่อน ตาม principle ของ task เอง ("ถ้าไม่แน่ใจ ให้หยุดและ log แทนการเดา")

### วีดีโอ (STEP 2 tab 3) และ ไลฟ์สตรีม (STEP 2 tab 4)
- ไม่มีหน้า export เทียบเท่าในเมนูใหม่แล้ว (sidebar ตอนนี้มีแค่: ผลการดำเนินงาน, ตัวอย่างสินค้า, การเข้าถึงครีเอเตอร์, สินค้า, ครีเอเตอร์, ข้อเสนอแนะ)
- **ไม่กระทบผลลัพธ์รอบนี้** เพราะสูตรอัปเดต dashboard (STEP 4/5 เดิม) ใช้แค่ไฟล์ ครีเอเตอร์ และ สินค้า เท่านั้น — วีดีโอ/ไลฟ์สตรีมไม่เคยถูกใช้คำนวณอะไรใน dashboard

---

## 📊 สถานะ dashboard ปัจจุบัน
- ก.ค. 2569 (1-16): GMV ฿788,438 · Net ฿740,580 · Comm ฿90,785 · ครีเอเตอร์ 435 (ค่าเก่า รอ verify)
- สินค้า 7 ตัวหลัก: cr/vid อัปเดตครบ ตรงกับ export ล่าสุด
- sw.js = v395, ไฟล์พร้อม push

## ▶️ ขั้นถัดไป (แนะนำ)
1. รัน `push_now.command` เพื่อ commit + push ขึ้น GitHub
2. รอบหน้า: ลอง export "ครีเอเตอร์" ใหม่อีกครั้ง — ถ้า 503 ยังไม่หาย อาจต้องพิจารณา manual download จาก TikTok Seller Center โดยตรง หรือแจ้ง TikTok support เรื่อง export ใหญ่ผิดพลาด
3. ควรพิจารณาปรับ workflow script ให้ใช้หน้า "ครีเอเตอร์"/"สินค้า" ใหม่ (`/data/creator-analysis`, `/data/product-performance`) แทนหน้าเก่าที่กำลังจะถูกปิด
