✅ WIBWUB Affiliate Auto-Update — 20 ก.ค. 2026 21:02

**ช่วงข้อมูล:** 1–19 กรกฎาคม 2026

**ไฟล์ที่ดาวน์โหลด/ย้ายแล้ว (4 ไฟล์):**
- ครีเอเตอร์: `Transaction_Analysis_Creator_List_20260701-20260719.xlsx`
- สินค้า: `Transaction_Analysis_Product_List_20260701-20260719.xlsx`
- วีดีโอ: `Transaction_Analysis_Video_List_20260701-20260719.xlsx`
- ไลฟ์สตรีม: `Transaction_Analysis_Live_List_20260713-20260719.xlsx` (ช่วงวันที่คลาดเคลื่อนจากที่ตั้งไว้ — ดูหมายเหตุด้านล่าง)

**สรุปยอด Affiliate (ก.ค. 1-19):**
- GMV: ฿974,988 (ไม่เปลี่ยนแปลง — ตรงกับข้อมูลเดิม)
- Net: ฿960,762 (อัปเดตจาก ฿915,161)
- ค่าคอมมิชชั่น: ฿111,863 (อัปเดตจาก ฿111,799)
- ครีเอเตอร์ที่มียอด: 547 คน (ไม่เปลี่ยนแปลง)

อัปเดตแล้วทั้งในไฟล์ WIBWUB_Affiliate_Dashboard.html (AF_NET, AF_COM) และ WIBWUB_Mobile.html (AFI_NET, AFI_COMM)

**สินค้า (PRODUCTS cr/vid):** ไม่ได้แก้ไขในรอบนี้ — ตรวจสอบแล้วพบว่าค่าที่มีอยู่ (อัปเดตโดยบอทรอบเช้าวันนี้) ยังสอดคล้องกับข้อมูลปัจจุบัน แต่คอลัมน์ "วิดีโอ" ในไฟล์สินค้าที่ export ใหม่ให้ค่าต่ำกว่าค่า `vid` ที่มีอยู่ในแดชบอร์ด (ไม่สมเหตุสมผลถ้าเป็นยอดสะสม) จึงตัดสินใจไม่ overwrite เพื่อความปลอดภัย — คอลัมน์นี้อาจไม่ใช่แหล่งข้อมูลที่ถูกต้องสำหรับฟิลด์ cr/vid ควรตรวจสอบวิธี mapping ที่ถูกต้องอีกครั้ง

**วิดีโอ (VIDEOS array):** ตรวจสอบไฟล์วิดีโอที่ export ใหม่ (526 วิดีโอที่มียอด GMV > 0) เทียบกับข้อมูลใน VIDEOS array — ตรงกันทั้งหมด 100% ไม่มีการเปลี่ยนแปลง (ข้อมูลถูกอัปเดตแล้วโดยบอทรอบก่อนหน้าในวันนี้)

**ไลฟ์สตรีม:** ดาวน์โหลด/ย้ายไฟล์แล้ว แต่ไม่มีขั้นตอนประมวลผลเข้าแดชบอร์ด (ไม่มีฟิลด์ใดในแดชบอร์ดที่ map กับข้อมูลนี้) — เก็บไว้เป็นข้อมูลอ้างอิงเท่านั้น

**sw.js:** bump เวอร์ชัน v427 → v428 (เนื่องจากมีการแก้ไขข้อมูลใน dashboard)

**push_now.command:** พร้อมใช้งาน — กรุณาเปิดไฟล์นี้เพื่อ commit และ push ขึ้น GitHub ด้วยตนเอง

---
**หมายเหตุ/การตัดสินใจอัตโนมัติที่สำคัญ:**
1. พบว่า UI ของ TikTok Affiliate Center เปลี่ยนโครงสร้าง — แท็บ ครีเอเตอร์/สินค้า/วีดีโอ/ไลฟ์สตรีม ทั้งหมดอยู่ใต้ section "รายละเอียด" ในหน้า "ผลการดำเนินงาน" (ไม่ใช่หน้าแยกต่างหากตามที่ SKILL.md เดิมระบุ) — บอทรอบก่อนหน้าเข้าใจผิดว่า export วิดีโอ/ไลฟ์สตรีมทำไม่ได้ แต่จริงๆ ทำได้
2. ไฟล์ไลฟ์สตรีมที่ได้มีช่วงวันที่ 13-19 ก.ค. แทนที่จะเป็น 1-19 ก.ค. ตามที่ตั้งไว้ — คาดว่าเกิดจากปัญหาการรีเซ็ตช่วงวันที่ของ UI หลังคลิกแท็บ แต่เนื่องจากข้อมูลนี้ไม่ถูกนำไปประมวลผลต่อ จึงไม่กระทบข้อมูลแดชบอร์ด
3. Net/Commission ที่อัปเดต มาจากไฟล์ครีเอเตอร์ที่ export ใหม่ ซึ่งมียอดคืนเงิน (refund) ต่ำกว่าที่คำนวณได้จากค่า Net เดิมในแดชบอร์ดมาก (14,226 vs 59,827 บาท) — ตรวจสอบแล้วว่าไม่ใช่ข้อผิดพลาดจากการ parse ไฟล์ (GMV และจำนวนครีเอเตอร์ตรงกันเป๊ะ) จึงสรุปว่าเป็นข้อมูลที่ถูกต้องและอัปเดตตาม
4. เนื่องจากพบว่ามีบอทอื่นทำงานกับไฟล์เดียวกันในวันเดียวกันหลายรอบ (พบ commit ที่เคยทำให้ค่า Commission เพี้ยนเป็น ฿9 มาก่อน) จึงเลือกวิธีอัปเดตแบบระมัดระวัง — แก้เฉพาะฟิลด์ที่ตรวจสอบแล้วว่าค่าจริงต่างจากค่าปัจจุบัน เท่านั้น
