# WIBWUB Weekly Update — 19 ส.ค. 2569 (พุธ)

Commit: `87f06ee` · sw cache: `wibwub-v724` · ยังไม่ push (sandbox proxy 403 → รัน `push_now.command`)

## STEP 0 — Protection check
`const M5` มี 8 เดือนทั้งสองไฟล์ (`WIBWUB_Dashboard.html`, `WIBWUB_Mobile.html`) = เท่ากับเดือนปัจจุบัน (ส.ค. = 8) ผ่าน ไม่ต้องแก้

## STEP 1 — Shipnity
ดาวน์โหลด purchase export ช่วง 1 ส.ค. – 19 ส.ค. 2569 ผ่าน Claude-in-Chrome สำเร็จ ได้ไฟล์ `Data Shipnity/Data_19-08-2026.xlsx` (19,488,861 bytes · 20,661 แถว × 20 คอลัมน์) และคัดลอกเป็น `Data_สิงหาคม.xlsx`

หมายเหตุ UI: v-menu ของ date picker มองไม่เห็นใน screenshot ต้องอ่าน bounding rect ผ่าน `javascript_tool` แล้วสั่ง click แบบ programmatic ปุ่มลัด "เดือนนี้" เลือกแค่วันที่ 31 วันเดียว ห้ามใช้

## STEP 3 — Top Products (`WIBWUB_Mobile.html`)
รวมข้อมูล 83 ไฟล์ product-level ได้ **205,242 unique line items** (รอบก่อน 18 ส.ค. = 204,083 → +1,159)

ข้ามไฟล์ 2 ไฟล์เพราะไม่ใช่ product-level: `Data_07-08-2026_export.xlsx`, `ROI product 2JUL26.xlsx`

วิธีคำนวณ (เหมือนรอบก่อนทุกประการ):
- จัดกลุ่มด้วย **รหัส SKU เต็ม** (คอลัมน์ 0) ไม่ใช่ชื่อสินค้า ไม่ใช่ prefix
- dedup key = `(เลขที่ออเดอร์, รหัสสินค้า, จำนวน)` เจอครั้งแรกชนะ เรียงไฟล์ตาม `sorted()`
- รายได้ = `ราคา × จำนวน` · เดือนจากคอลัมน์ 19 (`วันที่สร้าง`, `DD/MM/YYYY HH:MM`)
- `mk`/`mkq` = เฉพาะช่องทาง `สินค้าสำหรับทำการตลาด` + `เบิกของ`

**ยืนยันความถูกต้อง:** ค่า `mk`/`mkq` ที่คำนวณได้ตรงกับที่เผยแพร่อยู่เดิมทุกตัว (Wool Duster 13,188/20 · Sugar 46,568/168 · Interior 66,991/182 · Refresh Wipes 15,991/206) แปลว่า pipeline ตรงกับรอบก่อน

อันดับ Top 15 ไม่เปลี่ยน SKU และไม่สลับลำดับ อันดับ 16 คือ `SRFX110003` Reflex 500ml (฿1,199,180) ยังตามหลังอันดับ 15 Mind (฿1,236,409) อยู่

อัปเดตแล้ว: `ALL_PRODUCTS`, `PROD_MO`, `PROD_MO_LBL` → `ส.ค. (1-19)` และคอมเมนต์หัวบล็อกทั้งสอง

**ไม่แตะ `WIBWUB_Dashboard.html`** — คำสั่งใน SKILL.md ที่ให้อัปเดตกราฟ `pr_top10` + ตาราง Top Products จาก Shipnity ล้าสมัยแล้วตั้งแต่ commit `836c636` ตาราง Top Products ในไฟล์นั้นเป็นข้อมูลรวมหลายแพลตฟอร์ม ไม่ใช่ Shipnity อย่างเดียว

## STEP 2 + 4 — Affiliate: อยู่ที่ 1-16 ส.ค. ไม่ใช่ 1-18 (สำคัญ)

### สิ่งที่เกิดขึ้น
หน้า Transaction Analysis เดิม (`/data/transaction-analysis`) ถูกยกเลิกแล้ว redirect ไปหน้า "ผลการดำเนินงาน" หน้าที่ยังใช้ได้คือ `การวิเคราะห์ > ครีเอเตอร์` (`/data/creator-analysis`) ซึ่งมีแบนเนอร์ "อัปเกรดการวิเคราะห์แล้ว — หน้านี้จะเลิกใช้งานเร็วๆ นี้"

สั่ง export ช่วง 1-18 ส.ค. สำเร็จ ได้ `Data Affiliate/ครีเอเตอร์/Creator_List_20260801-20260818_20260819045226.xlsx` (1,272,331 bytes · 15,306 แถว × 24 คอลัมน์)

### ทำไมถึงใช้ไฟล์ 1-18 ไม่ได้
ไฟล์รูปแบบใหม่ (`Creator_List`, 24 คอลัมน์) **นิยาม "การคืนเงิน" ต่างจากรูปแบบเดิม** (`Transaction_Analysis_Creator_List`, 22 คอลัมน์) ซึ่งเป็นตัวที่ AF_NET / AFI_NET ใช้มาตลอด

ทดสอบบนช่วงเวลาเดียวกัน (1-10 ส.ค.) ทั้งสองรูปแบบ:

| ตัวชี้วัด | รูปแบบเดิม | รูปแบบใหม่ | ตรงกัน |
|---|---|---|---|
| GMV | 550,824 | 550,824 | ตรง |
| ครีเอเตอร์ที่มียอด | 355 | 355 | ตรง |
| ค่าคอมมิชชั่น | 64,485 | 64,424 | ต่าง 0.09% |
| **การคืนเงิน** | **12,513** | **36,261** | **ต่าง 2.9 เท่า** |

รูปแบบเดิมคอลัมน์ 4 = `การคืนเงิน` (ยอดคืนเงินที่เกิดขึ้นในช่วงเวลานั้น) ส่วนรูปแบบใหม่คอลัมน์ 21 = `GMV ของการคืนเงินจากแอฟฟิลิเอต` (GMV ของออเดอร์ในช่วงนั้นที่ถูกคืนภายหลัง) คนละนิยามกัน ถ้าเอามาใช้จะทำให้ NET ของ ส.ค. ขาดความต่อเนื่องกับเดือนก่อนหน้าทั้งชุด

### สิ่งที่ทำแทน
คงค่า Affiliate ไว้ที่ **1-16 ส.ค.** ซึ่งมาจากไฟล์รูปแบบเดิม `Transaction_Analysis_Creator_List_20260801-20260816.xlsx` และตรวจสอบซ้ำแล้วว่าคำนวณใหม่ได้ตรงเป๊ะทุกตัว:

```
GMV = 846,422   การคืนเงิน = 17,930   NET = 828,492   ค่าคอม = 99,416   ครีเอเตอร์ = 478
```

สถานะปัจจุบัน (ไม่มีการแก้เดือนก่อนหน้า ส.ค. เป็นสมาชิกตัวสุดท้าย → เขียนทับในที่เดิม):

`WIBWUB_Affiliate_Dashboard.html`
```
AF_MO  = [..., "ก.ค. (1-31)", "ส.ค. (1-16)]
AF_GMV = [0,0,655171,724241,951158,660557,1452748,846422]
AF_NET = [0,0,642865,708855,937203,650042,1428498,828492]
AF_COM = [0,0,82699,69021,104772,79293,169224,99416]
AF_CR  = [0,0,398,418,558,414,720,478]
```

`WIBWUB_Mobile.html`
```
AFI_MONTHS = [..., 'กค.69 (1-31)','สค.69 (1-16)']
AFI_GMV  = [0,0,0,0,655171,724241,951158,660557,1452748,846422]
AFI_NET  = [0,0,0,0,642865,708855,937203,650042,1428498,828492]
AFI_COMM = [0,0,0,0,82699,69021,104772,79293,169224,99416]
```

### ข้อมูล 1-18 ที่ยืนยันได้แล้ว (เก็บไว้อ้างอิง ยังไม่ได้ใส่ dashboard)
GMV `฿960,112.18` · ค่าคอมมิชชั่น `฿112,928.24` · ครีเอเตอร์ที่มียอด `515` — ทั้งสองตัวตรงกับ KPI บนหน้าเว็บเป๊ะ ขาดแค่ยอด `การคืนเงิน` แบบนิยามเดิมจึงยังคำนวณ NET ไม่ได้

### ต้องตัดสินใจ
ต้องเลือกทางใดทางหนึ่งก่อนที่หน้าเดิมจะปิดถาวร:
1. หาหน้า/ปุ่ม export ที่ยังให้คอลัมน์ `การคืนเงิน` แบบเดิม (ลองหน้า "ผลการดำเนินงาน" ที่เป็นตัวแทน)
2. ย้ายนิยาม NET ทั้งชุดไปใช้ `GMV ของการคืนเงินจากแอฟฟิลิเอต` แล้วคำนวณย้อนหลังทุกเดือนใหม่ให้เป็นนิยามเดียวกัน
3. เลิกแสดง NET แล้วใช้ GMV กับค่าคอมมิชชั่นอย่างเดียว (สองตัวนี้ตรงกันทั้งสองรูปแบบ)

## STEP 5 — Commit
- `sw.js`: `wibwub-v723` → `wibwub-v724`
- `git commit` = `87f06ee` (WIBWUB_Mobile.html, WIBWUB_Affiliate_Dashboard.html, sw.js, push_now.command)
- **ยังไม่ push** — sandbox proxy คืน HTTP 403 สร้าง `push_now.command` ใหม่ไว้แล้ว ดับเบิลคลิกเพื่อ push

ไฟล์ข้อมูลที่ยัง uncommitted (ตั้งใจไม่ commit ตามรอบก่อน): `Data Shipnity/stock/stock_snapshot.json`, `data content/Followers_wibwubcar.zip`, `data ยอดขาย plaform/Shopee/Order.all...zip`, `scripts/auto_push.log`

## STEP 6 — ตรวจสอบ
- ความยาว array: `AFI_*` = 10 ทุกตัว · `AF_*` = 8 ทุกตัว · `M5` = 8 ทั้งสองไฟล์ · `PROD_MO_LBL` = 8
- `ALL_PRODUCTS` = 15 แถว · `PROD_MO` = 15 แถว
- `sum(PROD_MO[i].mo) == ALL_PRODUCTS[i].v` ทั้ง 15 รายการ (คลาดเคลื่อนสูงสุด 1 บาทจากการปัดเศษแยกกัน เท่ากับพฤติกรรมเดิม)
- ไม่มีเดือนก่อนหน้าถูกเขียนทับในทุก array
- `node --check` บน JS block หลักของ `WIBWUB_Mobile.html` ผ่าน
- `git diff --stat` = 2 dashboard + sw.js + push_now.command เท่านั้น

## บันทึกไว้สำหรับรอบหน้า
- การรวมข้อมูล Shipnity ใช้เวลาเกิน 45 วินาทีที่ bash tool จำกัด และ sandbox ใช้ `--unshare-pid` ทำให้ process เบื้องหลังถูกฆ่าทุกครั้งที่คำสั่งจบ → ต้องประมวลผลเป็นช่วงๆ แล้ว persist state ลง pickle (สคริปต์อยู่ที่ `/tmp/agg2.py` รับ time budget เป็น argv[1])
- export ของ TikTok ใช้เวลาประมาณ 10-15 นาที ต้อง reload หน้าแล้วเปิด panel "บันทึกการดาวน์โหลดไฟล์" ใหม่ ถึงจะเห็นสถานะเปลี่ยนจาก `กำลังส่งออก` เป็น `ดาวน์โหลด`
- LaunchAgent `com.wibwub.download-mover` ย้ายไฟล์เข้าโฟลเดอร์ปลายทางเองภายใน ~5 วินาที ไม่ต้อง cp เอง
