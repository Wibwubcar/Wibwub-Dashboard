# รายงานอัปเดต Affiliate อัตโนมัติ — 1 ก.ย. 2026

**สถานะ: ✅ สำเร็จครบทุกขั้นตอน**

---

## ⚠️ ข้อสังเกตสำคัญของรอบนี้

วันนี้เป็นวันที่ 1 ก.ย. 2026 — เดือนกันยายนยังไม่มีข้อมูลเลย และหน้า TikTok Affiliate
(ส่วน "รายละเอียด") ล็อกวันสุดท้ายที่เลือกได้ไว้ที่ **29 ส.ค. 2026**
(วันที่ 30-31 ส.ค. และทั้งเดือน ก.ย. เป็นสีเทา เลือกไม่ได้ — "อัปเดตเมื่อ: 29 ส.ค. 2026 0:00 GMT+7:00")

จึงดึงข้อมูลช่วง **01/08/2026 – 29/08/2026** ซึ่งเป็นช่วงเดียวกับรอบวันที่ 31 ส.ค.
**ไม่มีการเพิ่มเดือน ก.ย. เข้า array ใดๆ** เพราะยังไม่มีข้อมูลจริง — แก้ค่าที่ index สุดท้าย
(label `"ส.ค. (1-29)"` / `'สค.69 (1-29)'`) ตามกฎ rolling window

---

## STEP 1-3: ดาวน์โหลดไฟล์

ดึงครบทั้ง 4 แท็บ ผ่าน Chrome (macOS) และ LaunchAgent ย้ายไฟล์เข้าโฟลเดอร์อัตโนมัติแล้ว

| แท็บ | ไฟล์ | ขนาด | แถวข้อมูล |
|---|---|---|---|
| ครีเอเตอร์ | `Transaction_Analysis_Creator_List_20260801-20260829.xlsx` | 646,546 B | 7,803 |
| สินค้า | `Transaction_Analysis_Product_List_20260801-20260829.xlsx` | 19,460 B | 74 |
| วีดีโอ | `Transaction_Analysis_Video_List_20260801-20260829.xlsx` | 1,401,209 B | 6,978 |
| ไลฟ์สตรีม | `Transaction_Analysis_Live_List_20260801-20260829.xlsx` | 117,824 B | 671 |

ทุกไฟล์อยู่ใน `Data Affiliate/{ครีเอเตอร์,สินค้า,วีดีโอ,ไลฟ์สตรีม}/` เรียบร้อย

---

## STEP 4: อัปเดต KPI รายเดือน (ส.ค. 1-29)

พบว่าตัวเลขในแดชบอร์ดยัง **ไม่ตรง** กับไฟล์ที่ดึงมาใหม่ (TikTok ปรับข้อมูลย้อนหลังเล็กน้อย)
จึงเขียนทับที่ index สุดท้าย:

| ตัวชี้วัด | ค่าเดิม | ค่าใหม่ | ส่วนต่าง |
|---|---|---|---|
| GMV | 1,603,364 | **1,603,562** | +198 |
| Net (GMV − คืนเงิน) | 1,578,281 | **1,578,477** | +196 |
| ค่าคอมมิชชั่น | 184,279 | **184,715** | +436 |
| ครีเอเตอร์ที่มียอดขาย | 797 | 797 | ไม่เปลี่ยน |

ยอดคืนเงินรวม: ฿25,085

แก้ในทั้ง 2 ไฟล์:
- `WIBWUB_Affiliate_Dashboard.html` → `AF_GMV` / `AF_NET` / `AF_COM` / `AF_CR` (บรรทัด 11746-11749)
- `WIBWUB_Mobile.html` → `AFI_GMV` / `AFI_NET` / `AFI_COMM` (บรรทัด 877-879)

ป้าย "ข้อมูลถึงวันที่" ดึงมาจาก `AF_MO[AF_MO.length-1]` อยู่แล้ว ไม่ต้องแก้แยก

> **หมายเหตุ:** รายงานรอบวันที่ 31 ส.ค. ระบุว่าซิงก์ค่า 1,603,562 / 1,578,477 / 184,715 ไว้แล้ว
> แต่ในไฟล์จริงกลับเป็น 1,603,364 / 1,578,281 / 184,279 — แปลว่ารอบนั้น **เขียนไม่สำเร็จ**
> รอบนี้ตรวจสอบซ้ำและเขียนจริงแล้ว (ยืนยันด้วย `git diff`)

---

## STEP 5: PRODUCTS (cr / vid)

เทียบครบทั้ง 7 รายการ — **ค่าตรงกับไฟล์ export ทุกตัว ไม่ต้องแก้**

| สินค้า | cr | vid |
|---|---|---|
| Refresh Leather Wipes | 42 | 39 |
| Interior wipes | 24 | 23 |
| Sugar | 15 | 15 |
| CLEANER | 5 | 6 |
| Interior | 5 | 5 |
| Refresh | 3 | 3 |
| Visible | 2 | 1 |

---

## STEP 5B: รวมข้อมูลวิดีโอเข้า VIDEOS

- เดิม **7,313** รายการ → ปัจจุบัน **7,315** รายการ (เพิ่มใหม่ 2)
- อัปเดตค่า `aug` ของรายการเดิม: 0 รายการ (GMV รายวิดีโอไม่เปลี่ยน)
- ยอดรวม `monthly.aug` ทั้ง array: 1,342,852
- วิดีโอใหม่ 2 รายการจาก `.plee1991` (WIBWUB Sugar) — ยังไม่มียอดขาย
- ปรับ label `date` ของรายการที่ GMV = 0 จำนวน 91 รายการ ให้เป็นค่าว่าง (เดิมแสดง "ส.ค." ทั้งที่ไม่มียอด)

**แก้บั๊กสำคัญ:** `scripts/vid_merge.py` ใช้ `date.today().month` หาคอลัมน์เดือนปลายทาง
วันนี้เป็น 1 ก.ย. จึงจะสร้างคอลัมน์ `sep` แล้วยัดยอดของเดือน ส.ค. ลงไป — **ผิด**
รอบนี้แก้ให้ดึงเดือนจากชื่อไฟล์ (`...-20260829` → `aug`) แทน
👉 **ควรแก้ `scripts/vid_merge.py` ตัวจริงด้วย** เพราะทุกครั้งที่รันข้ามเดือนจะพังแบบเดียวกัน

---

## STEP 6: Service Worker

`sw.js` — `wibwub-v924` → `wibwub-v925` → **`wibwub-v926`** (มีอีก process bump ต่ออีกครั้งระหว่างรัน)
สร้าง `push_now.command` ใหม่แล้ว (รวม `stock_snapshot.json` ของรอบสต๊อกที่ยังค้าง commit ไว้ด้วย)

---

## การตรวจสอบ (Verification)

- ✅ `git diff --numstat` — Affiliate Dashboard +96/-94, Mobile +3/-3, sw.js +1/-1
- ✅ รัน `node` eval บนบล็อก `VIDEOS` และ `AF_*` ทั้งหมด → parse ผ่าน ไม่มี syntax error
- ✅ ตรวจทุก inline `<script>` ในทั้ง 2 ไฟล์ด้วย `new Function()` → 0 error
- ✅ สำรองไฟล์ก่อนแก้ไว้ที่ outputs (`AFF_backup_20260901.html`)
- ✅ Safety guard: `entry_re` จับได้ 7,313 รายการ (> 0) จึงเขียนไฟล์ได้

---

## สิ่งที่ต้องทำต่อ

1. ดับเบิลคลิก **`push_now.command`** เพื่อ commit + push ขึ้น GitHub
2. พิจารณาแก้ `scripts/vid_merge.py` ให้ดึงเดือนจากชื่อไฟล์ถาวร (ดู STEP 5B)
3. รอบหน้า (หลัง 1-2 ก.ย.) ข้อมูลเดือน ก.ย. จะเริ่มมี — ตอนนั้นค่อย **append** `"ก.ย. (1-N)"` เข้า `AF_MO` / `AFI_MONTHS` พร้อมกันทุก array
