# WIBWUB Monday Update — 17 ส.ค. 2569 (รอบบ่าย/ค่ำ)

รันอัตโนมัติแบบไม่มี user อยู่ — ตัดสินใจเองตามที่ระบุด้านล่าง

---

## สรุปสิ่งที่ทำ

| STEP | สถานะ | หมายเหตุ |
|---|---|---|
| Protection check (M5) | ผ่าน | ทั้ง Dashboard และ Mobile มี 8 เดือน (ม.ค.–ส.ค.) ครบตามเดือนปัจจุบัน ไม่แตะ |
| STEP 1 — Shipnity export | ทำใหม่ | ดึงข้อมูล 1–17 ส.ค. ถึง 20:15 น. |
| STEP 2 — TikTok Affiliate | ข้าม | รอบ 13:12 ของวันเดียวกันดึงไปแล้ว (ข้อมูลถึง 15 ส.ค. ซึ่งเป็นวันล่าสุดที่ TikTok มีให้) |
| STEP 3 — Top Products | อัปเดต `ALL_PRODUCTS` ใน Mobile | ไม่แตะตาราง/กราฟใน Dashboard — เหตุผลด้านล่าง |
| STEP 4 — Affiliate arrays | ไม่แตะ | ค่าปัจจุบันตรงกับรอบ 13:12 อยู่แล้ว |
| STEP 5 — sw.js + commit | ทำแล้ว | v711 → v712, commit `cac9af4` |

---

## STEP 1 — Shipnity export

ดึงผ่าน Claude-in-Chrome จาก `shipnity.com/data/c/purchase` ช่วง 01/08/2026 – 17/08/2026 filter "สินค้าในออเดอร์"

- ไฟล์: `Data Shipnity/Data_17-08-2026.xlsx` (18.1 MB) → คัดลอกเป็น `Data_สิงหาคม.xlsx`
- 19,148 แถว × 20 คอลัมน์ ครอบคลุม 01/08 00:01 → 17/08 20:15

**ข้อควรรู้:** modal ส่งออกตั้งค่า default เป็น "แยกไฟล์" ซึ่งจะได้ไฟล์ย่อยขนาด ~470 KB ต้องเลือก **"ไฟล์เดียว"** ทุกครั้งเพื่อให้ได้ export เต็มแบบเดิม — ควรเพิ่มข้อนี้ใน SKILL.md

---

## STEP 3 — Top Products (ALL_PRODUCTS)

รวมข้อมูลจากไฟล์ระดับสินค้าทั้งหมด 82 ไฟล์ ได้ line-item ที่ไม่ซ้ำ 203,693 รายการ (dedup key = order_id + sku + qty, first-occurrence-wins)

ค่าใหม่ 15 อันดับแรก (ยอดสะสม ม.ค.–17 ส.ค.):

| # | สินค้า | ยอดขาย | ชิ้น |
|---|---|---|---|
| 1 | Wool Duster ไม้ปัดฝุ่นขนแกะ | 5,790,642 | 9,077 |
| 2 | Sugar สเปรย์แวกซ์เคลือบสีรถ | 5,158,508 | 17,164 |
| 3 | Interior สเปรย์ทำความสะอาดภายในรถ | 3,989,199 | 11,516 |
| 4 | Interior Wipe ทิชชู่เปียกภายในรถ | 2,580,630 | 35,400 |
| 5 | Refresh Wipes ทิชชู่เปียกเบาะหนัง | 2,576,776 | 33,003 |
| 6 | Refresh สเปรย์โฟมทำความสะอาด | 2,382,488 | 6,621 |
| 7 | Spot Clean น้ำยาขจัดคราบน้ำ | 2,233,978 | 5,939 |
| 8 | Perfect ผ้าไมโครไฟเบอร์ BOA 500gsm | 1,833,594 | 12,046 |
| 9 | Reflex Ceramic Coating สเปรย์เคลือบสี | 1,605,496 | 4,177 |
| 10 | Tire & Trim เจลเคลือบพลาสติกและยาง | 1,484,734 | 3,279 |
| 11 | Sugar สเปรย์แวกซ์ขนาด 3L | 1,454,830 | 1,228 |
| 12 | X-Glass Shield น้ำยาเคลือบกระจก | 1,450,420 | 3,912 |
| 13 | Martini ผ้าเช็ดน้ำยาเคลือบสี | 1,430,339 | 11,445 |
| 14 | Quartz Shampoo น้ำยาล้างรถ 1L | 1,293,723 | 5,410 |
| 15 | Mind Detailer สเปรย์เช็ดฝุ่นภายนอก | 1,225,850 | 3,059 |

ลำดับไม่เปลี่ยนจากเดิม (อันดับ 16 คือ Reflex 500ml 1,175,659 ยังไม่แซงอันดับ 15)

### ทำไมบางตัวยอด "ลดลง" จากค่าที่เผยแพร่อยู่

เช่น Wool Duster 5,815,933 → 5,790,642 (−25,291) และบางตัวจำนวนชิ้นติดลบ

ตรวจสอบแล้ว 3 สมมติฐาน:

1. ลำดับ dedup / การผูกราคา — **ตัดออก** สร้างใหม่ตามลำดับ `sorted(os.listdir())` ได้ผลรวมเท่ากันเป๊ะ
2. production ใช้ dedup key ต่างกัน — `wibwub_update.py` ใช้ `(order_id, name)` แต่ฟังก์ชันนั้นไม่ได้ผลิต `ALL_PRODUCTS` จึงไม่เกี่ยว
3. **สาเหตุจริง** — `git log -S "5815933"` ชี้ไปที่ commit `06abee6` วันเดียวกัน ซึ่งใช้ export รอบ 02:20 น. (ไฟล์ถูกทับด้วย export ใหม่แล้ว) ส่วนต่างติดลบคือ **ออเดอร์ที่ถูกยกเลิก/ลบใน Shipnity ระหว่างเช้าถึงค่ำวันนี้**

ยืนยันความถูกต้องอีกทาง: สินค้า 6 ตัวที่ไม่มีออเดอร์การตลาดใหม่วันนี้ ค่า `mk`/`mkq` ที่คำนวณตรงกับค่าที่เผยแพร่ทุกตัว (Δ 0/0) → สูตรและ pipeline ถูกต้อง

`mk`/`mkq` = ผลรวมช่องทาง `สินค้าสำหรับทำการตลาด` + `เบิกของ` (ตรวจสอบกับ Wool Duster 13,188 / 20 ชิ้น ตรงเป๊ะ)

---

## ตัดสินใจเอง: ไม่แตะตาราง/กราฟ Top Products ใน Dashboard

SKILL.md STEP 3 สั่งให้อัปเดตกราฟ `pr_top10` และตาราง Top Products ใน `WIBWUB_Dashboard.html` จากข้อมูล Shipnity — **คำสั่งนี้ล้าสมัยแล้ว**

ตั้งแต่ commit `836c636` ตารางนั้นถูกป้อนจากไฟล์ออเดอร์ 5 แพลตฟอร์ม ไม่ใช่ Shipnity อย่างเดียว หลักฐาน: คอลัมน์ Lazada ในตารางคือ 45,396 แต่ค่าที่ได้จาก Shipnity คือ 6,642 ถ้าเขียนทับจะทำให้คอลัมน์แพลตฟอร์มเสียหาย

จึงอัปเดตเฉพาะ `ALL_PRODUCTS` ใน Mobile ซึ่งเป็นส่วนที่มาจาก Shipnity ล้วน ๆ

**หมายเหตุ:** คอลัมน์แพลตฟอร์มในตาราง Dashboard รวมกันได้ ~5,789,000 ซึ่งสอดคล้องกับ 5,790,642 อยู่แล้ว แต่คอลัมน์ "รวม" กับ KPI บรรทัด 1012 ยังค้างที่ ฿5.82M — ควรให้คนแก้เอง หรือแก้ SKILL.md ให้ระบุแหล่งข้อมูลที่ถูกต้อง

---

## STEP 5 — Commit

- `sw.js` v711 → **v712**
- commit `cac9af4` — `auto-update: Monday 2026-08-17 — Shipnity Top Products (data ถึง 17.08 20:15) + Affiliate; sw v712`
- ไฟล์ที่ commit: `WIBWUB_Mobile.html`, `WIBWUB_Affiliate_Dashboard.html`, `sw.js`
- **ยังไม่ push** — sandbox proxy คืน HTTP 403 กรุณากด `push_now.command` เอง

---

## ตรวจสอบหลังแก้

- `M5` ทั้ง 2 ไฟล์ = 8 เดือน ครบ ไม่ถูกแตะ
- `AFI_MONTHS` 10 ช่อง ปิดท้าย `สค.69 (1-15)` — เดือนก่อนหน้าไม่ถูกทับ
- `AF_MO` 8 ช่อง ปิดท้าย `ส.ค. (1-15)` — เดือนก่อนหน้าไม่ถูกทับ
- `ALL_PRODUCTS` 15 รายการ ค่าตรงกับที่คำนวณ
- JS parse ผ่านทั้ง 6 script block ใน `WIBWUB_Mobile.html`

---

## เรื่องที่ควรแก้ใน SKILL.md

1. STEP 3 — ตาราง/กราฟ Top Products ใน Dashboard ไม่ได้มาจาก Shipnity แล้ว ควรลบคำสั่งนั้นออกหรือเขียนใหม่ให้ชี้ไปที่ไฟล์ 5 แพลตฟอร์ม
2. STEP 1 — เพิ่มขั้นตอน "เลือก 'ไฟล์เดียว' ใน modal ส่งออก" (default คือ 'แยกไฟล์')
3. STEP 2 — คอลัมน์ Creator List เปลี่ยนตำแหน่ง: returns ไปอยู่ index 4, commission ไปอยู่ index 21 (พบตั้งแต่รอบ 13:12)
4. Chrome — ถ้าแท็บปัจจุบันเป็นหน้า internal (`chrome://newtab/`) ต้องเรียก `navigate` เดี่ยว ๆ 1 ครั้งก่อน แล้วค่อยใช้ `browser_batch` ตามปกติ
