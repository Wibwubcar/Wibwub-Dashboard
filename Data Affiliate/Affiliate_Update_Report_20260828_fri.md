# รายงานอัปเดต Affiliate — ศุกร์ 28 ส.ค. 2569

**สถานะ: สำเร็จ** (เป็นรอบ *ยืนยันข้อมูล* เป็นหลัก — ตัวเลขส่วนใหญ่ถูกเขียนไว้แล้วโดย task "Monday" ที่รันเช้าวันเดียวกัน)

---

## 1. ช่วงข้อมูลที่ดึง

`01/08/2026 – 25/08/2026`

TikTok ตัดข้อมูล ณ **25 ส.ค. 2569 00:00 (GMT+7)** — วันที่ 26, 27, 28 ยังถูกล็อก (เลือกไม่ได้ในปฏิทิน) จึงใช้ 25 ส.ค. เป็นวันสิ้นสุด ทั้งที่วันนี้คือ 28 ส.ค.

## 2. ไฟล์ที่ดาวน์โหลด (4 แท็บ)

| แท็บ | ไฟล์ | ขนาด |
|---|---|---|
| ครีเอเตอร์ | `Transaction_Analysis_Creator_List_20260801-20260825.xlsx` | 606,950 B |
| สินค้า | `Transaction_Analysis_Product_List_20260801-20260825.xlsx` | 19,301 B |
| วีดีโอ | `Transaction_Analysis_Video_List_20260801-20260825.xlsx` | 1,313,966 B |
| ไลฟ์สตรีม | `Transaction_Analysis_Live_List_20260801-20260825.xlsx` | 100,909 B |

ทั้งหมดถูกคัดลอกเข้าโฟลเดอร์ย่อยของ `Data Affiliate/` เรียบร้อย

## 3. ตัวเลขที่คำนวณได้ (1–25 ส.ค. 2569)

จากไฟล์ครีเอเตอร์ 7,328 แถว

- GMV: **฿1,375,920**
- คืนเงิน: ฿22,949
- GMV สุทธิ: **฿1,352,971**
- ค่าคอมมิชชั่นโดยประมาณ: **฿158,103**
- ครีเอเตอร์ที่มียอดขาย: **704 คน**

## 4. สิ่งที่แก้ในไฟล์

| ไฟล์ | ผลลัพธ์ |
|---|---|
| `WIBWUB_Affiliate_Dashboard.html` — `AF_MO/GMV/NET/COM/CR` | **ตรงอยู่แล้ว ไม่ต้องแก้** |
| `WIBWUB_Mobile.html` — `AFI_MONTHS/GMV/NET/COMM` | **ตรงอยู่แล้ว ไม่ต้องแก้** |
| `PRODUCTS` (`cr`, `vid`) | **ตรงอยู่แล้วทั้ง 7 รายการ ไม่ต้องแก้** |
| `VIDEOS` | 6,913 รายการ · อัปเดตค่าเดือน ส.ค. 0 รายการ · เพิ่มใหม่ 0 รายการ · ปรับ label `date` 91 รายการ |
| `sw.js` | `wibwub-v865` → `wibwub-v866` |

Commit: `a243bd1` — push ขึ้น `origin/main` แล้ว

## 5. การตรวจสอบ

- `node -e` eval อาร์เรย์ `VIDEOS` และ `PRODUCTS` — ผ่าน ไม่ throw
- `git diff --stat` = 98 บรรทัดจาก ~28,000 บรรทัด (~0.35%) — อยู่ในเกณฑ์ปกติมาก
- `entry_re` match 6,913 / raw 6,913 (ตรงกัน 100%) — ผ่าน safety gate ก่อนเขียนทับ
- GMV รวมของ `VIDEOS` เดือน ส.ค. = ฿1,154,719 (เป็น subset ของ ฿1,375,920 ตามปกติ เพราะไม่รวมไลฟ์/ช่องทางอื่น)

---

## การตัดสินใจเองระหว่างรัน (ไม่มีคนตอบ)

1. **วันสิ้นสุด 25 ส.ค. แทน 28 ส.ค.** — ถูกบังคับโดย data cutoff ของ TikTok ไม่ใช่ตัวเลือก
2. **ไม่แก้ AF_* / AFI_* / PRODUCTS** — เพราะ task "Monday" รันเช้าวันนี้ (commit `59dede2`) เขียนตัวเลขชุดเดียวกันไว้แล้ว ค่าที่คำนวณใหม่ตรงกันทุกตัว จึงถือเป็นการยืนยัน ไม่ใช่การเปลี่ยนแปลง
3. **แก้ปัญหา backslash ทวีคูณใน caption** — พบว่าฟังก์ชัน `esc()` ใน step5b escape ซ้ำทุกครั้งที่รัน ทำให้ `\ud83d` กลายเป็น backslash ยาวขึ้นเรื่อย ๆ ได้ซ่อมเป็นอีโมจิจริง (👀) แล้ว **แต่ต้นเหตุยังอยู่ใน script — ควรแก้ `esc()` ไม่ให้ escape ซ้ำ ก่อนรอบหน้า**
4. **ไฟล์ครีเอเตอร์ส่วนเกิน 2 ไฟล์** (`_20260819-20260825`, `_20260818-20260824`) หลงเหลือในโฟลเดอร์ ครีเอเตอร์ จาก task Monday — ไม่กระทบ เพราะ script เลือกไฟล์ใหม่สุดตาม mtime
5. **คอลัมน์ของไฟล์สินค้า** — ยืนยันอีกครั้งว่า `cr` = คอลัมน์ 19 ("ครีเอเตอร์ที่มียอดขายเฉลี่ยรายวัน") และ `vid` = คอลัมน์ 13 ("วิดีโอที่มียอดขายเฉลี่ยรายวัน") **ไม่ใช่** คอลัมน์ 9 ตามที่ SKILL.md ระบุ ควรอัปเดต SKILL.md
6. **git index.lock ชน** — มี task อื่นรัน git พร้อมกัน sandbox ลบ lock ไม่ได้ (permission) จึงรอจน lock หลุดเอง สุดท้าย task นั้น `git add -A` เก็บงานของรอบนี้ไปด้วยใน commit `a243bd1` — ผลลัพธ์ถูกต้องครบถ้วน แต่ **ควรจัดตารางไม่ให้ task Monday กับ Thursday รันทับกัน**

## ข้อควรแก้ก่อนรอบหน้า

- แก้ `esc()` ใน step5b ไม่ให้ escape backslash ซ้ำ
- แก้คอลัมน์ `cr`/`vid` ใน SKILL.md เป็น 19/13
- แยกเวลารันของ task Monday กับ Thursday
