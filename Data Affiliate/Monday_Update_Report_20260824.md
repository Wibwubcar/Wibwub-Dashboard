# WIBWUB Monday Update — 24 ส.ค. 2569

รันอัตโนมัติ (scheduled task `wibwub-monday-update`) · commit `778c7fd` · sw.js `wibwub-v802`

## สรุปผล

| STEP | ผล |
|---|---|
| 0 — M5 protection | ✅ ทั้ง 2 ไฟล์มี 8 สมาชิก ตรงกับเดือนปัจจุบัน — **ไม่ได้แก้ไข** |
| 1 — Shipnity export | ✅ ดาวน์โหลด + `cp` → `Data Shipnity/Data_สิงหาคม.xlsx` (23.9 MB) |
| 2 — TikTok Affiliate | ⚠️ ใช้ไฟล์เดิมช่วงเดียวกัน (ดูหมายเหตุ) |
| 3 — Top Products | ✅ อัปเดต `ALL_PRODUCTS` + `PROD_MO` + ตาราง/กราฟ Web Dashboard |
| 4 — Affiliate arrays | ✅ ตรวจแล้วค่าตรงทุกตัว — ไม่ต้องแก้ |
| 5 — commit / push | ✅ commit แล้ว · รอรัน `push_now.command` |

## STEP 3 — Top Products (คำนวณใหม่ทั้งหมด)

ประมวลผลไฟล์ Shipnity **89 ไฟล์** (ข้าม 1 ไฟล์ `Data_07-08-2026_export.xlsx` เพราะเป็นรายการ orderItemId ของ TikTok ไม่ใช่ข้อมูลสินค้า) รวม dedup แล้ว **210,207 แถว**

ยอดรวมสะสม ม.ค.–24 ส.ค. 2569 = **฿62,546,654** · **245,840 ชิ้น**

### สิ่งที่ค้นพบและแก้ไขในรอบนี้

**1. ความหมายของ `mk` / `mkq`** — เดิมไม่มีเอกสารกำกับ ตรวจสอบจากโค้ดพบว่าเป็น "งบการตลาด" ซึ่งมาจากช่องทาง `สินค้าสำหรับทำการตลาด` **+** `เบิกของ` รวมกัน ยืนยันความถูกต้องได้โดยคำนวณแล้วตรงกับค่าเดิมบนแดชบอร์ดแบบเป๊ะ 12 จาก 15 รายการ (เช่น Wool Duster 13,188/20 · Sugar 46,568/168)

**2. Lazada ใช้ชื่อสินค้าคนละชุด** — จุดนี้เกือบทำให้ตัวเลข Lazada ผิดหนัก ออเดอร์ Lazada บันทึกด้วยชื่อ listing ยาว (เช่น `ไม้ปัดฝุ่นขน Wool duster`, `น้ำยาเคลือบสีแบบสเปรย์แวกซ์ (WIBWUB Sugar)`) แยกจาก SKU หลัก ถ้าไม่รวมกลุ่ม คอลัมน์ Lazada จะเหลือแค่ ฿102K แทนที่จะเป็น ฿495K จึงสร้าง alias map 49 รายการเพื่อรวมชื่อเหล่านี้เข้ากับ SKU หลัก (ขนาด 3L/5L ยังนับแยกกลุ่มตามเดิม)

### อันดับที่เปลี่ยน

Martini ตกจากอันดับ 11 → 13 โดย X-Glass Shield และ Sugar 3L ขยับขึ้นแทน

ค่าที่ได้ทั้ง 15 รายการต่างจากค่าเดิมบนแดชบอร์ดไม่เกิน ~1% ในเกือบทุกตัว (ส่วนต่างมาจากออเดอร์ที่ถูกยกเลิก/แก้ไขในไฟล์ export รอบใหม่ บวกกับ cutoff ที่ขยับจาก 23 → 24 ส.ค.)

### จุดที่แก้ในไฟล์

- `WIBWUB_Mobile.html` — `ALL_PRODUCTS` (15 แถว), `PROD_MO` (15 แถว × 8 เดือน)
- `WIBWUB_Dashboard.html` — ตาราง 15 แถว + แถวรวม, กราฟ `pr_top10` (9 ช่องทาง × 10 สินค้า), โดนัท `pr_channel`, KPI 4 ตัว, ข้อความหัวข้อ/วันที่

KPI ที่ปรับ: ยอดขายรวม ฿62.83M → **฿62.55M** · จำนวนชิ้น 245K → **246K** · สัดส่วนช่องทาง Shopee 59% → **56%**, Facebook 14% → **16%**, TikTok 18% → **23%**

## STEP 4 — Affiliate (ไม่ได้แก้ไข — ถูกต้องแล้ว)

ตรวจสอบตาม rolling-window rule: label เดือนปัจจุบัน `ส.ค. (1-21)` เป็นสมาชิกตัวสุดท้ายอยู่แล้ว → กรณีนี้ต้อง "แก้ที่ index สุดท้าย" แต่คำนวณใหม่จากไฟล์แล้วได้ค่าตรงกับของเดิมทุกตัว จึงไม่มีการเปลี่ยนแปลง และ **ไม่มีเดือนก่อนหน้าถูกเขียนทับ**

| ค่า | จากไฟล์ | บนแดชบอร์ด |
|---|---|---|
| GMV | ฿1,120,518.28 | 1120518 ✅ |
| การคืนเงิน | ฿20,724.26 | — |
| NET | ฿1,099,794.02 | 1099794 ✅ |
| ค่าคอมมิชชั่น | ฿130,107.57 | 130108 ✅ |
| ครีเอเตอร์ที่มี GMV | 583 | 583 ✅ |

GMV และการคืนเงินตรงกับตัวเลขบนหน้า TikTok Affiliate Center แบบเป๊ะ

> ⚠️ **ข้อมูลบางส่วน** — TikTok แสดง "อัปเดตเมื่อ: 21 ส.ค. 2026" วันที่ 22–24 ยังถูกล็อก ดังนั้นเดือน ส.ค. ครอบคลุมเพียง 1–21 ส.ค. (21 จาก 31 วัน) ตัวเลขเดือนนี้จึงยังเทียบกับเดือนเต็มไม่ได้

## หมายเหตุ / เรื่องที่ควรทราบ

**การส่งออกไฟล์ TikTok ค้าง** — สั่ง export `Transaction_Analysis_Creator_List` แล้วสถานะค้างที่ "กำลังส่งออก" นานกว่า 10 นาที กดปุ่มดาวน์โหลดไฟล์ที่พร้อมแล้วก็ไม่มีไฟล์ลงเครื่อง

ตัดสินใจใช้ไฟล์ `Transaction_Analysis_Creator_List_20260801-20260821.xlsx` ที่มีอยู่แล้วใน `Data Affiliate/` เนื่องจาก **ช่วงข้อมูลเหมือนกันทุกประการ** (01–21 ส.ค.) และข้อมูล TikTok ยังไม่ขยับเกิน 21 ส.ค. จึงไม่มีข้อมูลใหม่ที่ตกหล่น — ไม่กระทบความถูกต้องของรอบนี้

**ยังไม่ได้ push** — sandbox ออกเน็ตไม่ได้ (proxy 403) กรุณาดับเบิลคลิก `push_now.command` เพื่อ push commit `778c7fd` ขึ้น remote

## การตรวจสอบ

- M5 ทั้ง 2 ไฟล์ = 8 สมาชิก ✅
- `ALL_PRODUCTS` 15 แถว · `PROD_MO` 15 แถว × 8 คอลัมน์ครบ ✅
- กราฟ `pr_top10`: labels 10 · dataset ทั้ง 9 ช่องทางมี 10 ค่าเท่ากันหมด ✅
- ตรวจ syntax JavaScript ทั้ง 6 script block ด้วย `node --check` — ผ่านทั้งหมด ✅
- ไม่มีเดือนก่อนหน้าใน array ใดถูกเขียนทับ ✅
