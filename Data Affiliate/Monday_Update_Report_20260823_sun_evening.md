# WIBWUB Monday Update — อาทิตย์ 23 ส.ค. 2569 (รอบเย็น)

Commit: `c8f7122` — `auto-update: Monday 2026-08-23 (evening) — Shipnity Top Products + Affiliate verify`
sw.js: `wibwub-v792` → `wibwub-v793`
สถานะ push: **ยังไม่ push** — ดับเบิลคลิก `push_now.command`

> รอบนี้เป็นรอบที่ **2 ของวันเดียวกัน** (รอบแรก commit `5466069` เวลา ~02:29) ไฟล์ Shipnity รอบเช้า export ตอน ~09:15 รอบนี้ export ตอน ~20:22 จึงได้ออเดอร์ของวันที่ 23 เพิ่มมาอีกครึ่งวัน

---

## Protection step — M5

`const M5` ทั้ง `WIBWUB_Dashboard.html` และ `WIBWUB_Mobile.html` มี 8 entries (= เดือนปัจจุบัน ส.ค.) ตรวจทั้งก่อนและหลังแก้ ได้ 8 เท่าเดิม ไม่ต้องแก้

## STEP 1 — Shipnity

`Data Shipnity/Data_23-08-2026.xlsx` (23,512,004 bytes, 25,040 แถว) ดาวน์โหลดผ่าน Chrome (macOS) ช่วง 1–31 ส.ค. 2569 โหมด "ไฟล์เดียว" LaunchAgent `com.wibwub.download-mover` ย้ายไฟล์ให้อัตโนมัติ แล้ว `cp` เป็น `Data_สิงหาคม.xlsx`

**หมายเหตุ:** ไฟล์นี้ **เขียนทับ** ไฟล์รอบเช้า (22,838,534 bytes) เพราะชื่อเดียวกัน — วิธีแก้ที่ใช้อยู่ในหัวข้อ STEP 3

## STEP 2 — TikTok Affiliate

`Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Creator_List_20260801-20260821.xlsx` (565,305 bytes, 6,819 แถวข้อมูล) — copy ไป `Data Affiliate/` แล้ว

ข้อมูล TikTok ยังสดถึง **21 ส.ค.** เท่านั้น (`อัปเดตเมื่อ: 21 ส.ค. 2026 0:00 GMT+7`, ปฏิทินปิดวันที่ 22–23) เท่ากับรอบก่อน

## STEP 3 — Top Products (ภาพรวมธุรกิจ)

จัดกลุ่มด้วย **SKU (คอลัมน์ 0)** dedup ด้วยคีย์ `(เลขที่ออเดอร์, SKU, จำนวน)` กรองเฉพาะเดือน ส.ค. ตามวิธีที่ยืนยันไว้รอบก่อน

**ปัญหาและวิธีแก้:** ไฟล์ฐานของรอบเช้าถูกเขียนทับไปแล้ว จึง diff ตรง ๆ ไม่ได้ วิธีที่ใช้คือ

```
ส่วนต่างที่ต้องบวกรอบนี้ = delta(Data_22-08 → Data_23-08 รอบเย็น) − ส่วนต่างที่รอบเช้าบวกไปแล้ว
```

โดยตัวเลข "รอบเช้าบวกไปแล้ว" อ่านจากตารางในรายงาน `Monday_Update_Report_20260823_sun.md` วิธีนี้ให้ผลเท่ากับการ diff ไฟล์ต่อไฟล์ทุกประการ และยังคงหลักการ "บวกส่วนต่าง ไม่เขียนทับฐาน" เพื่อไม่ให้เกิด drift

ตรวจสอบความถูกต้อง: ผลรวมส่วนต่างแยกช่องทาง = ผลรวมส่วนต่างทั้งธุรกิจพอดี (฿180,232) และส่วนต่างทุกรายการเป็นบวก ไม่มีค่าติดลบผิดปกติ

รวมทั้งธุรกิจรอบนี้: **+฿180,232 · +927 ชิ้น** (แถวรวม ฿62.57M → ฿62.75M, 246,404 → 247,331 ชิ้น) KPI ยอดขายรวม ฿62.65M → **฿62.83M**
เฉพาะ Top 15: +฿116,537 · +753 ชิ้น
แยกช่องทาง: Shopee +100,562 · TikTok +62,556 · Facebook +14,873 · Lazada +1,261 · Line Shopping +980

ส่วนต่างรายสินค้า (รอบเย็นนี้เท่านั้น):

| สินค้า | +ยอดขาย | +ชิ้น | รวมใหม่ |
|---|---|---|---|
| Wool Duster ไม้ปัดฝุ่นขนแกะ | 5,975 | 9 | 5,873,221 |
| Sugar สเปรย์แวกซ์เคลือบสีรถ | 14,321 | 43 | 5,336,763 |
| Interior สเปรย์ทำความสะอาดภายในรถ | 11,929 | 33 | 4,123,317 |
| Refresh Wipes ทิชชู่เปียกเบาะหนัง | 30,699 | 355 | 2,786,293 |
| Interior Wipe ทิชชู่เปียกภายในรถ | 13,039 | 174 | 2,680,559 |
| Refresh สเปรย์โฟมทำความสะอาด | 5,902 | 17 | 2,454,835 |
| Spot Clean น้ำยาขจัดคราบน้ำ | 7,818 | 20 | 2,317,672 |
| Perfect ผ้าไมโครไฟเบอร์ BOA 500gsm | 3,572 | 23 | 1,907,702 |
| Reflex Ceramic Coating สเปรย์เคลือบสี | 2,761 | 7 | 1,679,940 |
| Tire & Trim เจลเคลือบพลาสติกและยาง | 2,821 | 6 | 1,526,875 |
| Martini ผ้าเช็ดน้ำยาเคลือบสี | 4,659 | 32 | 1,503,436 |
| X-Glass Shield น้ำยาเคลือบกระจก | 4,648 | 13 | 1,503,054 |
| Sugar สเปรย์แวกซ์ขนาด 3L | 2,806 | 2 | 1,470,162 |
| Quartz Shampoo น้ำยาล้างรถ 1L | 3,533 | 14 | 1,360,422 |
| Mind Detailer สเปรย์เช็ดฝุ่นภายนอก | 2,054 | 5 | 1,258,642 |

จุดที่แก้:

- `WIBWUB_Mobile.html` — `ALL_PRODUCTS` (v/q ครบ 15 แถว; mk/mkq ไม่เปลี่ยนเพราะส่วนต่างงบการตลาด = 0 ทุก SKU ตรวจแล้ว), `PROD_MO` **index 7 เท่านั้น** ครบ 15 แถว
- `WIBWUB_Dashboard.html` — chart `pr_top10` (dataset Shopee/TikTok/Facebook), doughnut `pr_channel`, ตาราง Top Products คอลัมน์ "รวม" + "จำนวน" ครบแถว 1–15 + แถวรวม (Shopee/TikTok/Lazada/Facebook), KPI ยอดขายรวม
- ไม่ต้องแก้ข้อความวันที่ — ยังเป็น "23 ส.ค." และ `PROD_MO_LBL` ยังเป็น `'ส.ค. (1-23)'` เหมือนเดิม
- `data-updated="23 ส.ค. 2569"` ถูกต้องอยู่แล้ว

## STEP 4 — Affiliate arrays

**ไม่มีการแก้ไข (no-op)** — คำนวณซ้ำจากไฟล์ `20260801-20260821` ได้

| ค่า | จากไฟล์ | ที่เก็บไว้ในแดชบอร์ด |
|---|---|---|
| GMV | 1,120,518 | 1,120,518 ✓ |
| NET | 1,099,794 | 1,099,794 ✓ |
| COMM | 130,108 | 130,108 ✓ |
| creators | 583 | 583 ✓ |

label ตัวสุดท้ายของ `AF_MO` = `"ส.ค. (1-21)"` และ `AFI_MONTHS` = `'สค.69 (1-21)'` ตรงกับไฟล์แล้ว ไม่มีเดือนใหม่ให้ append ไม่มีเดือนก่อนหน้าถูกแตะ

## ⚠️ เรื่องที่ต้องให้คนตรวจ

1. **ฟอร์แมตไฟล์ Affiliate ยังไม่ตรงกับเอกสาร (ปัญหาเดิมจากรอบก่อน ยังไม่ถูกแก้)** SKILL.md และ task file ระบุว่าไฟล์มี 12 คอลัมน์ / col2 = การคืนเงิน / col10 = ค่าคอมฯ แต่ไฟล์จริงมี **22 คอลัมน์ / header 2 แถว** ค่าที่ถูกต้องคือ `.iloc[2:]` · col1 = GMV · col4 = การคืนเงิน · col21 = ค่าคอมมิชชั่นโดยประมาณ **รบกวนแก้ SKILL.md** — รอบนี้เลี่ยงปัญหาด้วยการอ่านคอลัมน์จากชื่อ header แทน index ซึ่งทนต่อการเปลี่ยนฟอร์แมตมากกว่า

2. **ไฟล์ Shipnity ชื่อซ้ำถูกเขียนทับเมื่อรันซ้ำในวันเดียวกัน** ทำให้ diff ไฟล์ต่อไฟล์ทำไม่ได้ ควรพิจารณาให้ LaunchAgent เติม timestamp ต่อท้ายชื่อไฟล์ หรือให้ skill เก็บสำเนาฐานไว้ก่อนเขียนทับ

3. **คอลัมน์แยกช่องทางของตาราง Top Products แถว 1–15 ไม่ได้แก้** (แก้เฉพาะ chart `pr_top10` ซึ่งเก็บตัวเลขดิบ) เพราะค่าในตารางถูกปัดเป็นหน่วย K/M แล้ว และส่วนที่รอบเช้าบวกไปยังไม่รู้การกระจายรายช่องทางที่แน่นอน — เลือกไม่เดา คอลัมน์ "รวม" และ "จำนวน" ของทุกแถวอัปเดตครบและแม่นยำ

4. **ส่วนต่างของ chart `pr_top10` ใช้การกระจายตามสัดส่วน** ส่วนที่รอบเช้าบวกไปแล้วถูกหักออกโดยกระจายตามสัดส่วนช่องทางของส่วนต่าง 22→23 (ยอดรวมรายสินค้าถูกต้องแน่นอน แต่การแบ่งรายช่องทางคลาดเคลื่อนได้เล็กน้อย ระดับหลักร้อยบาท)

5. **KPI ยอดขายรวม (฿62.83M) กับแถวรวมในตาราง (฿62.75M) ต่างกัน ฿0.08M** เป็นส่วนต่างเดิมที่มีมาตั้งแต่รอบก่อน ๆ (คนละฐานการนับ) รอบนี้บวกส่วนต่างเท่ากันทั้งคู่จึงห่างเท่าเดิม

6. **มีไฟล์ `.git/*.lock` และ `tmp_obj_*` ค้าง** ลบจาก sandbox ไม่ได้ (Google Drive FUSE คืน `Operation not permitted`) — `push_now.command` ถูกเขียนใหม่ให้ลบไฟล์เหล่านี้ให้อัตโนมัติก่อน push แล้ว

7. **commit นี้มีงานของรอบอื่นติดมาด้วย** ตอน `git add WIBWUB_Affiliate_Dashboard.html` พบว่าไฟล์นี้มีการแก้ค้างอยู่ใน working tree จากการรัน affiliate อีกรอบของวันนี้ (มีรายงาน `Affiliate_Update_Report_20260823_sun.md` เป็นหลักฐาน) การแก้นั้นคือ `ส.ค. (1-20)` → `ส.ค. (1-21)` และ KPI/`AF_*` index 7 **ตรวจแล้วถูกต้องทุกตัว ตรงกับที่คำนวณใหม่จากไฟล์เอง (1,120,518 / 1,099,794 / 130,108 / 583) และเดือน ม.ค.–ก.ค. ไม่ถูกแตะ** จึง commit รวมไปได้ไม่มีปัญหา แต่บันทึกไว้ให้ทราบว่า commit `c8f7122` กว้างกว่าที่ระบุในหัวข้อ STEP 3/4

8. **ยังไม่ push** — ดับเบิลคลิก `push_now.command`

## การตรวจสอบที่ผ่านแล้ว

- `M5` ทั้ง 2 ไฟล์มี 8 entries ทั้งก่อนและหลังแก้
- เทียบ `PROD_MO` ก่อน/หลัง: **index 0–6 (ม.ค.–ก.ค.) ไม่เปลี่ยนแม้แต่แถวเดียว** ชื่อและลำดับสินค้าเหมือนเดิมทุกตัว
- `ALL_PRODUCTS` 15 แถว · `PROD_MO` 15 แถว × 8 เดือนเท่ากันทุกแถว
- `node --check` ผ่านทั้ง `WIBWUB_Mobile.html`, `WIBWUB_Dashboard.html`, `WIBWUB_Affiliate_Dashboard.html` (แยก `<script>` ออกมาตรวจ)
- ส่วนต่างแยกช่องทางรวมกันได้เท่ากับส่วนต่างทั้งธุรกิจพอดี (฿180,232)
- ค่า Affiliate ที่คำนวณใหม่ตรงกับที่เก็บไว้ครบทั้ง 4 ตัว
- การแก้แต่ละจุดใช้ regex ที่บังคับว่าต้องแมตช์ **ครั้งเดียวเท่านั้น** (assert count == 1) รวม 22 จุดใน Dashboard และ 30 จุดใน Mobile ไม่มีจุดใดแก้พลาดหรือแก้ซ้ำ
