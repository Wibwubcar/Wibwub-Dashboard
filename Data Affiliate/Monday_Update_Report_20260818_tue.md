# WIBWUB Monday Update — 18 ส.ค. 2569 (อังคาร)

รันอัตโนมัติแบบไม่มีคนดูแล (scheduled task `wibwub-monday-update`)

## สรุปผล

| STEP | งาน | ผล |
|---|---|---|
| 0 | ตรวจ `M5` ทั้ง 2 ไฟล์ | ✅ ผ่าน (8 เดือน ≥ เดือนปัจจุบัน 8) — ไม่ต้องแก้ |
| 1 | โหลด Shipnity purchase export 1–18 ส.ค. | ✅ `Data Shipnity/Data_18-08-2026.xlsx` (18.4 MB, 19,527 แถว) + copy เป็น `Data_สิงหาคม.xlsx` |
| 2 | โหลด TikTok Affiliate Transaction Analysis | ✅ `Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Creator_List_20260801-20260815.xlsx` |
| 3 | Top Products จาก Shipnity | ✅ อัปเดต `ALL_PRODUCTS` + `PROD_MO` ใน `WIBWUB_Mobile.html` |
| 4 | Affiliate arrays | ⏭️ ข้าม (no-op) — ข้อมูลปัจจุบันแล้ว |
| 5 | sw.js + git | ⚠️ bump v718→v719 สำเร็จ / commit ทำไม่ได้ใน sandbox — ย้ายไปทำใน `push_now.command` |

---

## STEP 3 — Top Products

รวมข้อมูลจากไฟล์ Shipnity ระดับสินค้า **83 ไฟล์** (ข้าม 1 ไฟล์ `Data_07-08-2026_export.xlsx` ที่ไม่ใช่ product-level)
ได้ line item ไม่ซ้ำ **204,083 รายการ**

วิธีคิด (ตามที่บันทึกไว้ในรายงาน 17 ส.ค.):
- dedup key = `(เลขที่ออเดอร์, รหัสสินค้า, จำนวน)` เจอครั้งแรกชนะ เรียงไฟล์ตาม `sorted()`
- จัดกลุ่มด้วย **รหัส SKU เต็ม** (ไม่ใช่ชื่อสินค้า / ไม่ใช่ prefix)
- `mk`/`mkq` = ช่องทาง `สินค้าสำหรับทำการตลาด` + `เบิกของ`

**การตรวจสอบความถูกต้องของวิธีคิด:** ค่า `mk`/`mkq` ทั้ง 15 ตัวที่คำนวณได้ **ตรงกับที่ published อยู่เดิมทุกตัว** (Wool Duster 13,188/20 · Sugar 46,568/168 · Interior 66,991/182 · Refresh Wipes 15,991/206 ฯลฯ) → ยืนยันว่าการ map SKU→ชื่อสินค้าถูกต้อง

**การเปลี่ยนแปลงอันดับ:** Top 15 สมาชิกเท่าเดิม มีสลับอันดับ 1 คู่ — Refresh Wipes ขึ้นที่ 4 แซง Interior Wipe ไป 317 บาท
อันดับ 16 (`SRFX110003`, 1,190,605) ยังตามอันดับ 15 (Mind Detailer 1,224,752) อยู่

อัปเดต label: `PROD_MO_LBL` `ส.ค. (1-16)` → `ส.ค. (1-18)`

### ⚠️ ข้อสังเกตที่ควรให้คนตรวจ

ค่าเดือน **ม.ค.–ก.ค.** ที่คำนวณรอบนี้ต่างจากที่ published อยู่เดิมเล็กน้อย (~0.3–3%) เช่น
Wool Duster ม.ค. 1,507,099 → 1,502,269 · Sugar ม.ค. 831,809 → 807,439

ทั้งที่ไฟล์ใหม่ 2 ไฟล์มีแต่ข้อมูลเดือน ส.ค. อย่างเดียวและ sort อยู่ท้ายสุด (first-occurrence-wins ไม่ควรกระทบเดือนก่อน)
สันนิษฐานว่ารอบก่อนใช้ชุดไฟล์/วิธี aggregate ต่างออกไปเล็กน้อย

**การตัดสินใจ:** เขียนทั้ง `ALL_PRODUCTS` และ `PROD_MO` จาก aggregate รอบเดียวกัน เพื่อให้ตัวเลขสองชุดสอดคล้องกัน
(ตรวจแล้ว `sum(PROD_MO.mo)` เท่ากับ `ALL_PRODUCTS.v` ทุกตัว ต่างกันไม่เกิน ±1 บาทจากการปัดเศษ)
ถ้าต้องการยึดค่าเดือนเก่าตามเดิม ให้ย้อน commit นี้แล้วแก้เฉพาะ index 7 (ส.ค.)

### ไม่แตะ `WIBWUB_Dashboard.html`

คำสั่งใน SKILL.md ที่ให้อัปเดตกราฟ `pr_top10` + ตาราง Top Products จาก Shipnity **ล้าสมัยแล้ว**
ตั้งแต่ commit `836c636` ส่วนนั้นดึงจากไฟล์ออเดอร์ 5 แพลตฟอร์ม ไม่ใช่ Shipnity อย่างเดียว
(หลักฐาน: คอลัมน์ Lazada 45,396 vs Shipnity 6,642) — ถ้าเขียนทับจะทำให้คอลัมน์แพลตฟอร์มพัง

---

## STEP 4 — Affiliate (ข้าม)

รอบ 09:07 น. วันนี้ (ดู `Affiliate_Update_Report_20260818_tue.md`) ได้ ingest ไฟล์ `Transaction_Analysis_Creator_List_20260801-20260815.xlsx` ตัวเดียวกันนี้ไปแล้ว
TikTok ยังไม่ปล่อยข้อมูลเกิน 15 ส.ค. เป็นวันที่ 3 ติดต่อกัน → รันซ้ำจะเป็น no-op และเสี่ยงทำ rolling window พัง

ตรวจค่าปัจจุบันแล้วตรงกับที่รายงานรอบเช้าไว้:

```
AF_MO  [..., "ก.ค. (1-31)", "ส.ค. (1-15)"]
AF_GMV [0,0,655171,724241,951158,660557,1452748,793803]
AF_NET [0,0,642865,708855,937203,650042,1428498,776827]
AF_COM [0,0,82699,69021,104772,79293,169224,93464]
AF_CR  [0,0,398,418,558,414,720,459]
```

Mobile `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM` ลงท้าย `สค.69 (1-15)` / 793803 / 776827 / 93464 — ตรงกัน
ตรวจแล้ว **ไม่มีเดือนก่อนหน้าถูกเขียนทับ**

---

## STEP 5 — sw.js + git

- `sw.js` bump `wibwub-v718` → `wibwub-v719` ✅
- `git add` / `git commit` **ทำไม่ได้** — มี `.git/index.lock` ค้างจากเวลา 02:33 และ sandbox ลบไม่ได้ (`Operation not permitted` บน mount ของ Google Drive)
- แก้โดยเขียน `push_now.command` ใหม่ให้ทำครบทั้ง ลบ lock → `git add` → `git commit` → `git push`

**ต้องทำเอง:** ดับเบิลคลิก `push_now.command` เพื่อ commit + push
(ไม่ push จาก sandbox อยู่แล้วตามกติกา — proxy ตอบ HTTP 403)

---

## ตรวจสอบผลลัพธ์

- `ALL_PRODUCTS` 15 แถว / `PROD_MO` 15 แถว ชื่อสินค้าตรงกันทุกแถวและเรียงลำดับเดียวกัน ✅
- `ALL_PRODUCTS` เรียงจากมากไปน้อยตาม `v` ✅
- ทุกแถวใน `PROD_MO` มี 8 เดือนพอดี ✅
- `sum(mo)` = `v` ทุกแถว (คลาดเคลื่อน ≤ ±1 บาท) ✅
- `M5` ทั้ง 2 ไฟล์ยังเป็น 8 เดือน ✅
- `git diff --stat`: `WIBWUB_Mobile.html` 34+/34− , `sw.js` 1+/1− — ไม่มีการแก้ส่วนอื่น ✅
