# WIBWUB Affiliate Auto-Update — 2026-07-24 (Thursday run of "wibwub-thursday-affiliate")

## Summary: Partial update — Creator + Product data refreshed; Video and Livestream blocked

### What was updated
- **Affiliate GMV/Net/Comm/Creators** (`WIBWUB_Affiliate_Dashboard.html` + `WIBWUB_Mobile.html`): "ก.ค." entry extended from (1-22) to **(1-23)**:
  - GMV: ฿1,134,955 → **฿1,178,515**
  - Net: ฿1,067,759 → **฿1,109,544**
  - Commission: ฿130,451 → **฿135,724**
  - Active creators: 625 → **644**
  - Source: `Creator_List_20260701-20260723_20260724015535.xlsx` (15,646 creator rows)
- **Products (`cr`/`vid`)**: checked against `ListProducts_2026-07-01-2026-07-23_ALLPlan_20260724015700.xlsx` — all 7 tracked products' `cr`/`vid` values already matched the latest export exactly, so no edit was needed (Leather Wipes 305/191, Interior Wipes 182/98, Sugar 104/142, Cleaner 46/28, Interior 57/87, Refresh 37/33, Visible 13/12).

### What was NOT updated (and why)
- **Video (`VIDEOS` array)**: no fresher data available. TikTok's export-file download endpoint (`/api/v1/insights/export/file/...`) returned **HTTP 503** on every attempt today (6+ retries across 2 different export job IDs, over ~5 minutes) for both the Creator and Video "detail list" exports. The Creator export eventually succeeded after several retries; the Video export never did. `VIDEOS` remains at its last successfully-processed state (through July 22).
- **Livestream (ไลฟ์สตรีม)**: TikTok's Affiliate Center has been redesigned (banner: "อัปเกรดการวิเคราะห์แล้ว") and the old tabbed "Transaction Analysis" livestream view no longer exists. No equivalent page could be found under `/data/*` or `/insights/*` routes despite an extensive search (sidebar, stat-card links, URL guessing, JS anchor scan, full-page scroll). No livestream file has been downloadable since ~July 20. This should be flagged to TikTok support or re-checked manually in the UI.

## Platform issue detail (for future runs)
The redesigned Affiliate Center's export pipeline appears degraded: the "quick" exports (Core Stats KPI summary, Product List) succeeded normally, but the "detail list" exports (Creator List, Video List) intermittently 503 on download, even after the job shows "ระบบเตรียมข้อมูลสำเร็จแล้ว" (ready). This is a backend issue, not a UI/automation problem — worth monitoring on the next run to see if it's resolved.

## Files moved
- `Creator_List_20260701-20260723_20260724015535.xlsx` → `Data Affiliate/ครีเอเตอร์/` (auto-moved by the Downloads LaunchAgent)
- `ListProducts_2026-07-01-2026-07-23_ALLPlan_20260724015700.xlsx` → `Data Affiliate/สินค้า/` (moved manually via `cp`, LaunchAgent had not picked it up after 10+ minutes)
- No video or livestream files to move (exports failed / page unavailable)

## sw.js / git
- Bumped `wibwub-v453` → `v454`; a separate, already-running automated process on this machine (visible in `git log` as "auto: update 2026-07-24 09:02" etc.) subsequently bumped it further to **v455** and committed `WIBWUB_Mobile.html` + `sw.js` on its own. Those two files are already committed at HEAD — no action needed.
- `WIBWUB_Affiliate_Dashboard.html` has the GMV/Net/Comm/Creators edit staged locally, but `git commit` could not run — `.git/index.lock` was held by that same concurrent auto-process for the duration of this run. The edit is safely saved on disk; it just needs a commit+push. `push_now.command` (pre-existing in this folder) is ready to use once the lock clears — or the auto-process's next cycle will likely pick it up on its own, consistent with today's git history.
- Note: local `main` and `origin/main` had already diverged (8 local commits vs 1 remote-only commit) before this run started — unrelated to my changes, but worth a manual `git pull`/merge check before pushing.

## Automation notes
- Browser: local macOS Chrome via the Claude-in-Chrome connector, no user present to confirm.
- Date range used: 2026-07-01 to 2026-07-23 (creator page allowed up to 23; product and video pages also allowed up to 23 today).
- No destructive actions taken; only additive edits to the three dashboard files plus two file moves.

## Recommended follow-up
1. Re-run (or manually retry) the Video export once TikTok's 503 issue clears, to bring `VIDEOS` current through July 23.
2. Manually check the TikTok Affiliate Center for wherever the livestream breakdown moved to in the redesign — automation could not locate it.
3. Run `push_now.command` (or verify the auto-process already pushed) to sync `WIBWUB_Affiliate_Dashboard.html` to GitHub/Netlify.
