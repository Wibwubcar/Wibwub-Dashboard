# WIBWUB Affiliate Auto-Update — 2 ก.ย. 2569 (พุธ)

## สรุป: ไม่มีข้อมูลใหม่ให้ sync — ไม่ได้แก้ไฟล์ใด ๆ

TikTok Affiliate Center ยังอัปเดตข้อมูลถึงแค่ **30 ส.ค. 2026** เท่านั้น (หน้า Performance แสดง
"อัปเดตเมื่อ: 30 ส.ค. 2026 0:00") ในปฏิทิน date picker วันที่ **31 ส.ค. และทุกวันของเดือน ก.ย.
ถูก disable** — คลิกเลือกไม่ได้ ทดสอบแล้วยืนยัน

ช่วงที่เลือกได้สูงสุดคือ 01/08/2026 – 30/08/2026 ซึ่งเป็นช่วงเดียวกับที่รอบวันที่ 1 ก.ย. ดึงมาแล้ว
จึงไม่ได้ export ซ้ำ (จะได้ไฟล์ที่เนื้อหาเหมือนเดิมทุกประการ และทำให้โฟลเดอร์รก)

## ตรวจสอบความตรงกันของ Dashboard กับตัวเลขจริงบนเว็บ

ตัวเลขบนหน้า TikTok (1 ส.ค. – 30 ส.ค.) เทียบกับ `WIBWUB_Affiliate_Dashboard.html`:

| รายการ | TikTok (สด) | Dashboard | ตรงกัน |
|---|---|---|---|
| GMV จากครีเอเตอร์ | ฿1,680,781.02 | AF_GMV = 1680781 | ✅ |
| การคืนเงิน | ฿27,432.94 | AF_NET = 1653348 (= GMV − คืนเงิน) | ✅ |
| ค่าคอมมิชชั่นโดยประมาณ | ฿193,331.74 | AF_COM = 193332 | ✅ |
| ครีเอเตอร์ที่มียอดขาย | 828 | AF_CR = 828 | ✅ |

`WIBWUB_Mobile.html` (AFI_GMV / AFI_NET / AFI_COMM) ตรงกับชุดเดียวกัน
label เดือนล่าสุดคือ `ส.ค. (1-30)` / `สค.69 (1-30)` ซึ่งสะท้อนช่วงข้อมูลจริงถูกต้องแล้ว

**VIDEOS array:** parse ไฟล์ `Transaction_Analysis_Video_List_20260801-20260830.xlsx` (7,118 แถว)
เทียบกับ 7,452 entries ใน dashboard — ค่าเดือน ส.ค. ต่างกัน **0 รายการ**, วิดีโอใหม่ที่ควรเพิ่ม
**0 รายการ** อยู่ในสถานะ sync สมบูรณ์

สรุปคือไม่มีอะไรต้องแก้ จึง**ไม่ bump sw.js และไม่ต้อง push** (git ยืนยันว่า
`WIBWUB_Affiliate_Dashboard.html` และ `WIBWUB_Mobile.html` ไม่มีการแก้ไขค้างอยู่)

## ⚠️ พบปัญหาที่ต้องให้คนตรวจ — PRODUCTS cr/vid ไม่เคยถูกอัปเดตจริง

STEP 5 ของ schedule (อัปเดต `PRODUCTS[].cr` และ `.vid`) ใช้ mapping แบบ keyword ภาษาอังกฤษ
(Leather / interior wipe / Sugar / CLEANER / …) แต่ชื่อสินค้าในไฟล์ export ของ TikTok
เป็น**ภาษาไทยล้วน** เช่น "ทิชชู่เปียกเช็ดเบาะหนังรถยนต์ สูตร pH-Balance…" ทำให้ mapping
match ไม่ได้เลย และ step นี้เป็น **no-op เงียบ ๆ มานาน** (อาการเดียวกับบั๊ก gmvD/netD ที่เคยแก้ไป
เมื่อ 6 ก.ค.)

ค่าปัจจุบันใน dashboard ต่ำกว่าค่าจริงราว 10 เท่า:

| PRODUCTS[].name | cr/vid ใน dashboard | ค่าจริงจากไฟล์ (ครีเอเตอร์ที่มียอดขาย / วิดีโอ) |
|---|---|---|
| WIBWUB Refresh Leather Wipes | 42 / 39 | 433 / 471 |
| WIBWUB Interior wipes | 24 / 23 | 237 / 260 |
| WIBWUB Sugar | 15 / 15 | 149 / 370 |
| WIBWUB Interior | 5 / 5 | 62 / 162 |
| WIBWUB CLEANER | 5 / 6 | 42 / 100 |
| WIBWUB Refresh | 3 / 3 | 48 / 52 |
| WIBWUB Visible | 2 / 1 | 19 / 30 |

**ยังไม่ได้แก้ให้** เพราะการจับคู่ชื่อไทย→ชื่อใน PRODUCTS เป็นการเดาของ Claude และตรวจยืนยัน
ด้วย GMV/ret ไม่ได้ (ฟิลด์ `monthly`/`ret` ใน PRODUCTS มาจากอีกแหล่ง ไม่ใช่ไฟล์ affiliate)
การเขียนทับด้วยตัวเลขที่ต่างเดิม 10 เท่าโดยไม่มีคนยืนยัน mapping เสี่ยงเกินไปสำหรับรอบอัตโนมัติ

**สิ่งที่ควรทำ:** ยืนยัน mapping 7 คู่ข้างบน แล้วให้ผมแก้ทั้ง PRODUCTS array และแก้ mapping table
ใน SKILL.md ให้ใช้ keyword ภาษาไทย (หรือใช้ `รหัสสินค้า` แทนชื่อ) จะได้ไม่ no-op อีก

## หมายเหตุ

- pipeline อื่น ๆ (Shopee / TikTok Ads / stock) มีข้อมูลถึง 1–2 ก.ย. ตามปกติ — ที่ค้างคือฝั่ง
  Affiliate transaction-analysis ของ TikTok เท่านั้น หากพรุ่งนี้ยังค้างที่ 30 ส.ค. อีก
  น่าจะเป็นปัญหาฝั่ง TikTok ที่ควรแจ้ง support
- ไฟล์ล่าสุดในโฟลเดอร์ (ทั้ง 4 tab) ยังเป็นชุด `20260801-20260830` จากรอบ 1 ก.ย.
