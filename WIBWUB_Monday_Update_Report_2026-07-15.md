# WIBWUB Weekly Update — 15 กรกฎาคม 2026 (รันอัตโนมัติ)

## สรุปผล

| Step | สถานะ | รายละเอียด |
|---|---|---|
| 0. ตรวจสอบ M5 array | ✅ ไม่ต้องแก้ | `M5` มี 7 รายการ (ม.ค.–ก.ค.) ใน WIBWUB_Dashboard.html และ WIBWUB_Mobile.html อยู่แล้ว ถูกต้องสำหรับเดือน 7 |
| 1. Export Shipnity | ✅ สำเร็จ | Export ช่วง 1–15 ก.ค. 2569 จาก shipnity.com สำเร็จ → บันทึกเป็น `Data Shipnity/Data_กรกฎาคม.xlsx` (15,023,814 bytes) |
| 2. Export TikTok Affiliate | ✅ สำเร็จ | Export "Transaction Analysis — Creator List" ช่วง 1–13 ก.ค. 2569 → `Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Creator_List_20260701-20260713.xlsx` |
| 3. อัปเดต Top Products | ⏭️ ข้าม (เหมือนสัปดาห์ก่อน) | ยังไม่มีตารางแม็บชื่อสินค้าดิบจาก Shipnity → ชื่อที่ใช้แสดงใน `ALL_PRODUCTS`/`PROD_MO` ยืนยันแล้วว่ามีความกำกวมจริง (เช่น สินค้า "Sugar" หลายไซส์ 500ml/3L/5L ต้องแม็บไปคนละแถวใน dashboard) ยังไม่ปลอดภัยที่จะ auto-update โดยไม่มี mapping table — ควรทำ mapping table ก่อนเปิดใช้ step นี้ในรันถัดไป |
| 4. อัปเดต Affiliate arrays | ✅ สำเร็จ | อัปเดต index เดือน ก.ค. (index สุดท้าย) เท่านั้น ไม่แตะเดือนก่อนหน้า — ดูตัวเลขด้านล่าง |
| 5. Bump sw.js + commit | ✅ สำเร็จ | `wibwub-v382` → `wibwub-v383`, commit `f11d8ef` |
| 6. Push ขึ้น GitHub | ⏳ รอผู้ใช้ | Sandbox push ติด proxy 403 เหมือนเดิม — รัน `push_now.command` (ดับเบิลคลิกในเครื่อง) เพื่อ push |

## ตัวเลข Affiliate เดือน ก.ค. (อัปเดตจาก 1-12 → 1-13 ก.ค.)

อ้างอิงจากไฟล์ Creator List ที่ export ใหม่ (5,038 creator rows, 381 รายที่มียอดขายจริง):

| | เดิม (1-12 ก.ค.) | ใหม่ (1-13 ก.ค.) |
|---|---|---|
| GMV | ฿575,217 | ฿619,016 |
| NET (หลังหักคืนเงิน) | ฿559,906 | ฿609,646 |
| ค่าคอมมิชชั่นประมาณ | ฿0 *(ไม่ได้บันทึกไว้ก่อนหน้า)* | ฿71,180 |
| จำนวนครีเอเตอร์ที่มียอดขาย | 359 | 381 |

**ตรวจสอบไขว้:** ยอด GMV ที่คำนวณได้ (฿619,015.58) ตรงกับตัวเลข GMV ที่แสดงสดบนหน้า TikTok Affiliate Center (ช่วง 1-13 ก.ค.) พอดี — ยืนยันว่าการอ่านไฟล์และสูตรคำนวณถูกต้อง

ไฟล์ที่แก้ไข: `WIBWUB_Affiliate_Dashboard.html`, `WIBWUB_Mobile.html` (เฉพาะ index เดือน ก.ค. — ตรวจสอบด้วย git diff แล้วว่าเดือนอื่นไม่ถูกแตะ)

## หมายเหตุ

- มีไฟล์ทดสอบเล็ก ๆ `__test_del__.txt` ค้างอยู่ในโฟลเดอร์ All/ (ใช้ตรวจสอบสิทธิ์การลบไฟล์ระหว่างรัน) ไม่ได้ track ใน git และไม่กระทบระบบ — ลบทิ้งได้เมื่อสะดวก
- Export ของ TikTok Affiliate รอบนี้ใช้เวลานานผิดปกติ (~8 นาทีกว่าจะเสร็จ เทียบกับ 60-90 วิที่เคยเจอ) — ไม่แน่ใจสาเหตุ อาจเป็นความหนาแน่นของข้อมูลช่วงนั้นหรือระบบ TikTok ช้า
