# WIBWUB Weekly Dashboard Update — 14 กรกฎาคม 2026

## สรุปสิ่งที่ทำ

### 1. ตรวจสอบ M5/month-index protection
ตรวจสอบ `WIBWUB_Dashboard.html` และ `WIBWUB_Mobile.html` — โครงสร้างป้องกัน index เดือนถูกต้องอยู่แล้ว ไม่ต้องแก้ไข

### 2. Export ข้อมูล Affiliate ใหม่ (TikTok Creator_List)
ใช้ปุ่ม "ส่งออก" ที่ตารางครีเอเตอร์ (ไม่ใช่ปุ่ม KPI ด้านบนที่เคยทำให้เกิดบัคสัปดาห์ก่อน) ช่วง 1-12 ก.ค. 2569:
`Data Affiliate/ครีเอเตอร์/Creator_List_20260701-20260712_20260714020437.xlsx` (24 คอลัมน์, 10,163 แถว) — ยอดตรงกับ KPI สดบนหน้าเว็บพอดี

| | ค่าเดิม (1-12 มิ.ย. เทียบ) | ค่าใหม่ (1-12 ก.ค.) |
|---|---|---|
| GMV | 563,130 | 575,302 |
| Net | 554,101 | 545,087 |
| Commission | 65,815 | 66,167 |
| Creators | 338 | 359 |

อัปเดตทั้งใน `WIBWUB_Affiliate_Dashboard.html` (AF_GMV/AF_NET/AF_COM/AF_CR index สุดท้าย) และ `WIBWUB_Mobile.html` (AFI_GMV/AFI_NET/AFI_COMM index สุดท้าย) ให้ตรงกัน — ตรวจ diff แล้วยืนยันว่าไม่ได้แตะเดือนก่อนหน้า

### 3. Top Products (ภาพรวมธุรกิจ) — ไม่มีการเปลี่ยนแปลง
พยายาม export ข้อมูล Shipnity ใหม่ผ่าน Chrome แต่ติดปัญหา Chrome บล็อกการดาวน์โหลดอัตโนมัติหลังไฟล์แรกของ session (เจอปัญหาเดียวกับที่เอกสารสัปดาห์ก่อนพบ) ไฟล์ rollup รายเดือนล่าสุดในคลัง (`Data Shipnity/Data_กรกฎาคม.xlsx`) มีวันที่ 13 ก.ค. — ตรงกับ comment "updated 13.07.69" ที่มีอยู่แล้วใน `ALL_PRODUCTS`/`PROD_MO` ของ `WIBWUB_Mobile.html` แปลว่าข้อมูล Top Products เป็นปัจจุบันเท่าที่มีข้อมูลเต็มเดือนล่าสุดอยู่แล้ว จึง**ไม่แก้ไข**ค่าในสัปดาห์นี้เพื่อป้องกันความเสี่ยงจากการ map ชื่อสินค้าดิบผิดพลาด (ไม่พบตาราง mapping รหัสสินค้า→ชื่อที่ใช้แสดงผลในไฟล์ dashboard)

มีไฟล์ Shipnity บางส่วน (`Data-Page-1_14-07-2026.xlsx`, ~500 แถว ช่วง 13 ก.ค. 17:16 – 14 ก.ค. 08:36) ที่ยังไม่รวมเข้าไฟล์ rollup — ผลกระทบต่อยอดสะสม 7 เดือนน้อยมาก จึงข้ามไปเพื่อความเรียบร้อยของข้อมูล รอ export เต็มเดือนครั้งถัดไป

### 4. Cache version + Git
- Bump `sw.js`: `wibwub-v376` → `wibwub-v377`
- Commit `e17dcc1`: 3 ไฟล์ (Affiliate Dashboard, Mobile, sw.js), +8/-8 บรรทัด
- ใช้ `push_now.command` เดิมที่มีอยู่แล้ว (สร้างไว้ตั้งแต่ 13 ก.ค.) — **ต้องดับเบิลคลิกเพื่อ push ขึ้น GitHub** (sandbox push เองไม่ได้ ติด proxy 403)

## หมายเหตุ / ข้อจำกัดของรอบนี้
- Shipnity export รอบนี้ล้มเหลว (Chrome บล็อกดาวน์โหลดอัตโนมัติหลังไฟล์แรก) — ใช้คลังไฟล์เดิมแทน ไม่มีผลต่อ Top Products เพราะข้อมูลเป็นปัจจุบันอยู่แล้ว
- ไม่พบตาราง mapping รหัสสินค้า Shipnity → ชื่อสินค้าที่ใช้แสดงผล — หากต้องการรวมข้อมูล Shipnity ใหม่ในอนาคต ควรสร้าง mapping table นี้ไว้ล่วงหน้าเพื่อลดความเสี่ยงจากการจับคู่ชื่อผิด
- TikTok Affiliate export ใช้ปุ่มที่ถูกต้อง (Creator_List) ตรวจสอบยอดตรงกับ KPI สดแล้ว
