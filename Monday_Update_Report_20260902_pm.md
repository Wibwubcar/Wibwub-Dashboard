# WIBWUB Weekly Update — 2 ก.ย. 2569 (พุธ, รอบเย็น 20:15 น.)

**สถานะ: สำเร็จ** — ดึง Shipnity รอบใหม่ที่ครบวันกว่าเดิม, ยืนยัน Affiliate ตรงกับต้นทาง,
และ **commit งาน Affiliate ที่ค้างมาทั้งวันได้แล้ว** (`8d97cc9`)

รอ user กด `push_now.command` — sandbox push ไม่ได้ (proxy 403)

---

## STEP 0 — M5 guard

ไม่แก้ไข โดยตั้งใจ ตรวจแล้ว `M5` ยาว 8 เท่ากับ `SH_REV` ทั้งสองไฟล์ = สถานะถูกต้อง

สคริปต์ใน runbook (`required_months = today.month`) จะเติม `"ก.ย."` ให้เป็น 9 ซึ่งผิด —
เหมือนที่รอบเช้าและรอบ 1 ก.ย. เจอแล้วต้อง revert มือ **ผมจึงไม่รันสคริปต์นั้นตามตัวอักษร**
แต่ใช้เงื่อนไขที่ถูกต้องแทนคือ `len(M5) == len(SH_REV)` ซึ่งผ่าน

> ยังยืนยันข้อเสนอเดิม: ควรแก้ runbook เป็น `required_months = len(SH_REV)` จะได้เลิก revert ทุกต้นเดือน

## STEP 1 — Shipnity export: **สำเร็จ ได้ข้อมูลใหม่กว่ารอบเช้า**

ดึงช่วง 1–2 ก.ย. 2569 ผ่าน Claude Chrome (date picker แบบเลือกวันเอง → Export File → ไฟล์เดียว)
LaunchAgent `com.wibwub.download-mover` ย้ายไฟล์เข้า `Data Shipnity/Data_02-09-2026.xlsx` เองเรียบร้อย
(ทับไฟล์รอบ 02:18 น. — 1.37 MB → 1.98 MB)

2,156 แถว (2,152 หลัง dedup), 20 คอลัมน์, product-level ✅

| วันที่ | รอบเช้า 02:18 | รอบนี้ 20:18 | เพิ่มขึ้น |
|---|---|---|---|
| 1 ก.ย. 2026 | ฿384,815 | ฿378,750 | −6,065 * |
| 2 ก.ย. 2026 | ฿72,596 | ฿256,156 | +183,560 |
| **รวม** | **฿457,411** | **฿634,906** | **+177,495** |

\* ยอด 1 ก.ย. ลดลงเล็กน้อยเพราะออเดอร์ที่ถูกยกเลิก/ลบออกหลังรอบเช้า ไม่ใช่ความผิดพลาดของการดึง

Top 5 SKU ของกันยายน (2 วัน): Sugar 500ml ฿67,044 · Refresh wipes ฿61,643 ·
Interior 500ml ฿35,834 · Interior wipes ฿30,543 · Anna nano diamond ฿28,640

## STEP 2 — Affiliate export: **ไม่ต้องดึง — มี job อื่นดึงไปแล้วเมื่อ 20:03 น.**

ตอนผมเริ่มรอบนี้ (20:13 น.) พบว่ามี job อื่นเพิ่งดึงไฟล์ `20260801-20260831` ครบทั้ง 4 tab
(ครีเอเตอร์ / วีดีโอ / สินค้า / ไลฟ์สตรีม) เมื่อ 20:03 น. — **TikTok ปลดล็อกวันที่ 31 ส.ค. แล้ว**
หลังค้างอยู่ที่ 30 ส.ค. มาสามวัน (blocker ที่รายงานเช้านี้ปิดได้แล้ว)

ตรวจหน้าเว็บซ้ำในรอบนี้: "อัปเดตเมื่อ: **31 ส.ค. 2026**" และในปฏิทิน **ทุกวันของ ก.ย. ยังถูก disable**
(ทดสอบเปิด date picker แล้วเห็น 01–02/09 เป็นสีเทา) → **ยังไม่มีข้อมูลกันยายนให้ดึง** จึงไม่ export ซ้ำ

## STEP 3 — Top Products: **ไม่แก้ไข โดยตั้งใจ**

`ALL_PRODUCTS` / `PROD_MO` ผูกกับ `M5` ที่ยาว 8 (ม.ค.–ส.ค.) การเติมกันยายนต้องขยาย `M5`
ซึ่งขัดกับ STEP 0 และจะพัง `SH_REV`/`TK_REV`/`LZ_REV` (ยาว 8 ทั้งหมด)

กันยายนมีข้อมูลแค่ 2 วันและวันที่ 2 ยังไม่จบวัน · ฝั่งสิงหาคมไม่มีไฟล์ export ใหม่วันนี้ จึงไม่มี delta
ยืนแผนเดิม: เติมกันยายนพร้อมกันทั้ง M5 + PROD_MO + SH/TK/LZ ในรอบที่มีข้อมูลกันยายนครบทุกช่องทาง

## STEP 4 — Affiliate arrays: **ยืนยันถูกต้องแล้ว ไม่ต้องแก้**

job เมื่อ 20:03 น. เขียนค่าใหม่ไว้ ผม parse ไฟล์ต้นทางเองแล้วเทียบทีละตัว:

| รายการ | คำนวณจากไฟล์ | ในไฟล์ dashboard | ตรงกัน |
|---|---|---|---|
| GMV | 1,730,879 | AF_GMV / AFI_GMV = 1,730,879 | ✅ |
| Net (GMV − คืนเงิน 28,900) | 1,701,979 | AF_NET / AFI_NET = 1,701,979 | ✅ |
| ค่าคอมมิชชั่น | 199,254 | AF_COM / AFI_COMM = 199,254 | ✅ |
| ครีเอเตอร์ที่มียอดขาย | 853 | AF_CR = 853 | ✅ |

label เดือนล่าสุดเปลี่ยนเป็น `ส.ค. (1-31)` / `สค.69 (1-31)` แล้วทั้งสองไฟล์ ตรวจแล้วไม่มี
`(1-30)` หรือ `30 ส.ค.` ค้างอยู่ที่ไหนอีก และ array ทุกตัวยาวเท่าเดิม (AF_* = 8, AFI_* = 10)
= **append/overwrite ถูกตำแหน่ง ไม่มีเดือนก่อนหน้าถูกทับ**

### ⚠️ คอลัมน์ไฟล์ Affiliate เปลี่ยนจาก 12 → 22 คอลัมน์ — runbook ล้าสมัยแล้ว

ไฟล์ `Transaction_Analysis_Creator_List_*.xlsx` ตอนนี้มี **22 คอลัมน์** ไม่ใช่ 12
index ที่ runbook ระบุไว้ผิดหมด:

| ค่า | runbook (12 คอล) | ของจริงตอนนี้ (22 คอล) |
|---|---|---|
| GMV จากครีเอเตอร์ | col[1] | col[1] ✅ (บังเอิญตรง) |
| การคืนเงิน | col[2] | **col[4]** ❌ (col[2] = GMV จาก LIVE) |
| ค่าคอมมิชชั่น | col[10] | **col[21]** ❌ (col[10] = CTOR) |

ถ้าใครรัน STEP 4 ตาม runbook ตรง ๆ จะได้ค่าคอมมิชชั่น = **0** และ net เพี้ยน
(ผมทดสอบแล้ว: ใช้ col[10] ได้ COMM=0, ใช้ col[21] ได้ 199,254 ตรงกับเว็บ)
**ควรแก้ SKILL.md ให้อ้างอิงจากชื่อหัวคอลัมน์แทน index**

## STEP 5 — commit: **สำเร็จ (สิ่งที่ค้างมาทั้งวัน)**

`WIBWUB_Affiliate_Dashboard.html` ที่รอบเช้ารายงานว่า "มีการแก้ค้างอยู่ ไม่ทราบที่มา ไม่กล้า commit"
— ตอนนี้ทราบที่มาแล้ว (job affiliate ที่รัน 20:03) และผม **ตรวจตัวเลขทุกตัวกับไฟล์ต้นทางแล้วว่าถูกต้อง**
จึง commit ให้:

```
8d97cc9 affiliate: sync ส.ค. 1-31 (GMV 1,730,879 / net 1,701,979 / comm 199,254 / 853 creators)
        + CREATORS & VIDEOS refresh          1 file changed, 327 insertions(+), 225 deletions(-)
```

- `WIBWUB_Mobile.html` — job อื่น commit ไปแล้วระหว่างที่ผมทำงาน (ค่าตรงกับที่ผมตรวจ) ✅
- `sw.js` — **ไม่แตะ** มี job อื่น bump ถึง `wibwub-v953` แล้ว และ index กับ worktree กำลังขัดกันอยู่
  (staged v952 / worktree v953) การไป bump ซ้ำจะยิ่งชนกัน
- `WIBWUB_Dashboard.html` — ไม่มีการแก้ ✅

**ค้าง 1 commit รอ push** → กด `push_now.command`

---

## ⚠️ เรื่องที่ต้องให้คนดู

**1. มี lock ค้างใน `.git/` ที่ sandbox ลบไม่ได้ — จะบล็อก job ถัดไป**

ตอน commit สำเร็จ git สร้าง `.git/index.lock`, `.git/HEAD.lock`, `.git/next-index-6.lock` แล้ว
**ลบตัวเองไม่ได้** (`Operation not permitted` — ข้อจำกัดเดิมของ sandbox บน Google Drive
เหมือนที่ลบไฟล์ใน Downloads ไม่ได้) ผลคือ **job ไหนที่จะ commit ต่อจากนี้จะเจอ
"Another git process seems to be running"**

ผมแก้ `push_now.command` ให้ลบ `next-index-*.lock` เพิ่มด้วยแล้ว — **กดไฟล์นั้นหนึ่งครั้ง
จะเคลียร์ lock ทั้งหมดพร้อม push ในทีเดียว**

หมายเหตุ: รอบนี้มี job หลายตัวแย่งกัน commit จริง (เห็น lock ของคนอื่นเกิด-ดับหลายรอบ
และมี commit `44ee649`, `16e8bd6`, `bbc7a01` แทรกเข้ามาระหว่างที่ผมทำงาน)
ถ้าเป็นไปได้ควรจัดตารางไม่ให้ job ชนกันขนาดนี้

**2. PRODUCTS[].cr / .vid ยังไม่ถูกแก้** — blocker เดิม รอคนยืนยัน mapping ชื่อไทย↔อังกฤษ 7 คู่
(รายละเอียดอยู่ใน `Affiliate_Update_Report_20260902.md`) ยังไม่มีใครแตะ

**3. runbook 2 จุดที่ควรแก้** (สรุปจาก STEP 0 และ STEP 4 ข้างบน)
- `required_months = today.month` → `len(SH_REV)`
- column index ของไฟล์ affiliate → อ้างอิงชื่อหัวคอลัมน์แทน (ตอนนี้ 22 คอลัมน์)
