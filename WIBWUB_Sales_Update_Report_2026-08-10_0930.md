# WIBWUB Sales Sheet Update — รายงานรอบ 2026-08-10 09:30

## สรุปผล
พบว่าไฟล์ dashboard อยู่ใน**สถานะไม่สมบูรณ์จากรอบก่อนหน้า** (ไม่ใช่จากรอบนี้) — มีการเพิ่ม label เดือน "ส.ค." เข้า `M5` และเพิ่มค่า `SH_ADS` ตัวที่ 8 (294645.33, ไม่ผ่านการยืนยัน) โดยไม่ได้เพิ่มข้อมูลจริงให้ครบทุก array ที่เกี่ยวข้อง (`SH_REV`, `TK_REV`, `LZ_REV`, `SH_ORD`, `SH_FEE`, `LZ_*`, `TK_*` ยังอยู่ที่ 7 elements) — ตรงกับ bug class ที่ CRITICAL PROTECTION ระบุไว้ว่าห้ามเกิดขึ้น (label ไม่ตรงกับ data arrays → chart เดือนล่าสุดพัง)

**การแก้ไขรอบนี้:** revert `M5` และ `SH_ADS` กลับเป็น 7 elements (ถึง ก.ค. เท่านั้น) ในทั้ง `WIBWUB_Dashboard.html` และ `WIBWUB_Mobile.html` เพื่อให้ทุก array กลับมามีความยาวตรงกัน — dashboard กลับมาแสดงผลถูกต้องแล้ว

## ข้อมูลที่ตรวจสอบแล้ว (ยืนยันตรงกับ Sheet)
- **Shopee** ก.ค.: revenue, orders, cancel%, ads, fee ตรงกับค่าที่มีอยู่ใน dashboard ทุกตัว — ไม่ต้องแก้ไข
- **Lazada** ก.ค.: revenue, ads, fee, coupon, cost% ตรงกับค่าที่มีอยู่ใน dashboard ทุกตัว — ไม่ต้องแก้ไข
- **TikTok**: ไฟล์ต้นทาง "ส่งเสริมการขายTIKTOK.xlsx" มีขนาดใหญ่เกินกว่าเครื่องมือที่มีจะอ่านได้ในรอบนี้ (มี multi-tab, ไม่มีวิธี export เฉพาะ tab/ช่วงข้อมูลที่ต้องการ) — **ไม่สามารถตรวจสอบหรือดึงข้อมูลเดือน ส.ค. ได้ในรอบนี้**

## เหตุผลที่ไม่เพิ่มเดือน ส.ค. เข้าไปในรอบนี้
ตามกฎ CRITICAL PROTECTION: ห้าม push label เดือนใหม่เข้า `M5` เว้นแต่จะมีข้อมูลจริงครบทุก platform (Shopee/TikTok/Lazada) พร้อมกัน เนื่องจากไม่สามารถยืนยันข้อมูล TikTok ได้ในรอบนี้ จึงเลือก**ไม่เพิ่มเดือน ส.ค.** ให้แพลตฟอร์มไหนเลย (แม้ Shopee/Lazada จะมีข้อมูลบางส่วนที่อ่านได้) เพื่อไม่ให้เกิด array-length mismatch ซ้ำอีก — คงไว้ที่ ม.ค.–ก.ค. ทั้งหมด

นอกจากนี้ระบบไฮไลท์เดือนปัจจุบันในกราฟ (`i===6` hardcode ในหลายจุดของทั้งสองไฟล์) ยังอ้างอิง index 6 (ก.ค.) ตายตัว ซึ่งจะต้องอัปเดตควบคู่กันถ้าจะเพิ่มเดือน ส.ค. จริง — เป็นอีกเหตุผลที่ยืนยันว่าการเพิ่มเดือน ส.ค. บางส่วนก่อนหน้านี้ยังไม่สมบูรณ์และควร revert

## ไฟล์ที่แก้ไข
- `WIBWUB_Dashboard.html`: `M5` และ `SH_ADS` กลับเป็น 7 elements
- `WIBWUB_Mobile.html`: `M5` และ `SH_ADS` กลับเป็น 7 elements
- `sw.js`: bump cache version `wibwub-v622` → `wibwub-v623`
- Git commit แล้ว (`532c8f6`) — ยังไม่ push (sandbox push ไม่ได้เนื่องจากข้อจำกัด proxy) กรุณารัน `push_now.command` เพื่อ push ขึ้น GitHub

## แนะนำสำหรับรอบถัดไป
1. ต้องหาวิธีดึงข้อมูล TikTok เฉพาะ tab สรุปรายเดือน (เช่น export เฉพาะ sheet/gid เป็น CSV แยกจากไฟล์ xlsx เต็ม) หรือแยกไฟล์สรุปเล็กๆ ไว้ต่างหาก เพื่อให้ดึงข้อมูลได้ในรอบถัดไป
2. เมื่อมีข้อมูลครบทั้ง 3 platform สำหรับเดือน ส.ค. แล้ว ต้องอัปเดต `i===6` → ต้องพิจารณาปรับ logic ไฮไลท์เดือนปัจจุบันด้วย (ไม่ใช่แค่เพิ่ม array element)
3. พบไฟล์ debris จำนวนมากใน `.git/` (lock file .bak/.old/.stale หลายร้อยไฟล์) จากปัญหา filesystem permission ที่ทำให้ unlink lock file ไม่ได้ — ไม่กระทบการทำงาน (ใช้ rename แทนได้) แต่ควรพิจารณา cleanup ในโอกาสหน้า
