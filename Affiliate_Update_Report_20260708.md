# WIBWUB Affiliate Auto-Update Report — 8 ก.ค. 2026 (Thursday run)

## สรุปสิ่งที่อัปเดต

### 1. ดึงข้อมูลจาก TikTok Affiliate Center
ดาวน์โหลดไฟล์ export 4 ไฟล์จากหน้า Transaction Analysis ช่วงวันที่ 1–6 ก.ค. 2026 (เดือนถึงปัจจุบัน):
- ครีเอเตอร์: `Transaction_Analysis_Creator_List_20260630-20260706.xlsx`
- สินค้า: `Transaction_Analysis_Product_List_20260630-20260706.xlsx`
- วีดีโอ: `Transaction_Analysis_Video_List_20260630-20260706.xlsx`
- ไลฟ์สตรีม: `Transaction_Analysis_Live_List_20260630-20260706.xlsx`

หมายเหตุ: ชื่อไฟล์ระบุช่วง 30 มิ.ย.–6 ก.ค. แต่ตรวจสอบข้อมูลจริงในไฟล์แล้วพบว่าเป็นข้อมูลของช่วง 1–6 ก.ค. ตามที่เลือกไว้จริง (ยอด GMV รวมในไฟล์ตรงกับตัวเลขบนการ์ด KPI หน้าเว็บ ห่างกันเพียง ~88 บาท ซึ่งเป็นความคลาดเคลื่อนจาก rounding/cache ไม่ใช่ข้อมูลเกิน 1 วัน) — ชื่อไฟล์ TikTok ตั้งไม่ตรงกับ custom range ที่เลือกจริง แต่เนื้อหาไฟล์ถูกต้อง

ไฟล์ทั้ง 4 ถูกย้ายเข้าโฟลเดอร์ `Data Affiliate/{หมวด}/` ที่ถูกต้องเรียบร้อย (ผ่าน LaunchAgent อัตโนมัติ)

### 2. อัปเดตข้อมูลครีเอเตอร์ (ก.ค. 1-6)
คำนวณจากไฟล์ครีเอเตอร์:
- GMV รวม: ฿282,442
- Net GMV: ฿276,937
- ค่าคอมมิชชั่น: ฿32,200
- จำนวนครีเอเตอร์ที่มียอดขาย: 238 คน

อัปเดต index สุดท้ายของเดือน ก.ค. (rolling window, overwrite เพราะ label "ก.ค." มีอยู่แล้วเป็นตัวสุดท้าย) ใน:
- `WIBWUB_Affiliate_Dashboard.html`: AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR + KPI strip การ์ดด้านบน (Affiliate GMV, Net GMV, ครีเอเตอร์ที่มียอด, ค่าคอมมิชชั่น) → เปลี่ยน label จาก "(1-5)" เป็น "(1-6)"
- `WIBWUB_Mobile.html`: AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM

### 3. สินค้า (PRODUCTS cr/vid) — **ข้าม ไม่ได้อัปเดตรอบนี้**
ตรวจสอบไฟล์สินค้าและเทียบกับ commit ครั้งก่อน (7 ก.ค.) พบความไม่สอดคล้องที่ต้องระวัง:
- คอลัมน์ "วีดีโอ" (col 13) ในไฟล์ใหม่ให้ค่าที่สมเหตุสมผล (เช่น Leather Wipes 31→35, เพิ่มขึ้นทีละน้อยตามที่คาด)
- แต่คอลัมน์ "ครีเอเตอร์ที่มียอดขายเฉลี่ยรายวัน" (col 9) ให้ค่าต่ำกว่าค่าปัจจุบันในแดชบอร์ดมาก (เช่น Leather Wipes ปัจจุบัน cr=82 แต่ไฟล์ใหม่ให้แค่ 27) ซึ่งขัดกับ pattern ที่ควรเพิ่มขึ้นเรื่อยๆ (สัปดาห์ก่อน 27→82)

เพื่อป้องกันการเขียนทับด้วยตัวเลขที่ผิด จึงตัดสินใจ **ไม่แตะ field cr/vid ใน PRODUCTS array รอบนี้** — ต้องตรวจสอบ mapping คอลัมน์/สูตรคำนวณ cr ให้แน่ชัดก่อน (อาจต้องเป็นผลรวมสะสมจากหลายสัปดาห์ ไม่ใช่ค่าดิบจากไฟล์ล่าสุดตรงๆ)

### 4. Cache version
Bump `sw.js`: wibwub-v343 → **wibwub-v344**

### 5. Push script
อัปเดต `push_now.command` ให้ทำ git add + commit + push ครบในตัว (เดิมมีแค่ push) เนื่องจาก sandbox ไม่มีสิทธิ์เขียน `.git/index.lock` บน Google Drive path — ต้องรันสคริปต์นี้บนเครื่อง Mac จริงเพื่อ commit และ push การเปลี่ยนแปลงขึ้น git

## Action ที่ต้องทำต่อ (สำหรับผู้ใช้)
1. ดับเบิลคลิก `push_now.command` ในโฟลเดอร์ All เพื่อ commit + push ขึ้น git (เนื่องจาก sandbox push ไม่ได้)
2. ตรวจสอบ mapping คอลัมน์ cr ในไฟล์สินค้าให้ถูกต้อง แล้วรัน sync PRODUCTS cr/vid แยกอีกครั้ง
