# รายงาน Affiliate Auto-Update — 28 ส.ค. 2569

ช่วงข้อมูล **1–26 ส.ค. 2569** (วันที่ล่าสุดที่ TikTok Affiliate Center ให้เลือกได้) ดาวน์โหลดครบทั้ง 4 ไฟล์ (ครีเอเตอร์ / สินค้า / วีดีโอ / ไลฟ์สตรีม) และไฟล์ถูกย้ายเข้าโฟลเดอร์ `Data Affiliate/` แต่ละหมวดเรียบร้อยโดย LaunchAgent

## ตัวเลขหลักเดือน ส.ค. (1–26)

| | ใหม่ | เดิม (1–25) |
|---|---|---|
| GMV | ฿1,417,693 | ฿1,375,745 |
| Net (หัก refund) | ฿1,394,395 | ฿1,352,796 |
| ค่าคอมมิชชั่น | ฿162,637 | ฿157,723 |
| ครีเอเตอร์ที่มียอดขาย | **618** | 704 |
| LIVE GMV | ฿82,265 | ฿80,462 |
| ไลฟ์ / วิดีโอ | 645 / 1,281 | 566 / — |

อัปเดตทั้ง `WIBWUB_Affiliate_Dashboard.html` (AF_MO / AF_GMV / AF_NET / AF_COM / AF_CR + KPI strip + ป้ายวันที่) และ `WIBWUB_Mobile.html` (AFI_MONTHS / AFI_GMV / AFI_NET / AFI_COMM + KPI บรรทัด 391) ทุกจุดเป็นการเขียนทับตำแหน่งสุดท้าย (ส.ค.) ไม่มีการแตะเดือนก่อนหน้า

## ⚠️ เรื่องที่ต้องดู — จำนวนครีเอเตอร์ลดจาก 704 เหลือ 618

ทั้งที่ช่วงข้อมูลยาวขึ้น 1 วัน จำนวนครีเอเตอร์กลับลดลง 86 ราย จึงตรวจสอบก่อนเขียนทับ: เทียบไฟล์ 1–25 กับ 1–26 พบว่ามีครีเอเตอร์ 95 รายที่อยู่ในไฟล์เก่าแต่หายไปจากไฟล์ใหม่ทั้งหมด ทุกรายเป็นยอดขายเล็กมาก (~฿86–89 ต่อราย รวมกันเพียง ฿8,322 จาก ฿1.4 ล้าน) และมีครีเอเตอร์ใหม่เพิ่มเข้ามา 9 ราย จำนวนแถวรวมในไฟล์ยังเพิ่มขึ้น (7,328 → 7,342) แปลว่าไม่ใช่ไฟล์ถูกตัดทอน — สรุปว่าเป็นการเปลี่ยนแปลงของข้อมูลต้นทางฝั่ง TikTok เอง (น่าจะเป็นการกรอง/ยกเลิกออร์เดอร์ย้อนหลัง) จึงเขียนค่า 618 ตามไฟล์จริง

## PRODUCTS (cr / vid) — เปลี่ยน 3 จาก 7 รายการ

Refresh Leather Wipes cr 40→41, vid 37→38 · Interior wipes vid 22→23 · Sugar vid 15→14 · ที่เหลือ (CLEANER / Interior / Refresh / Visible) ค่าเท่าเดิม

หมายเหตุ: ตารางคีย์เวิร์ดในไฟล์ task แมป "Leather" → Refresh Leather Wipes ซึ่งจะแมปผิดกับแถว "WIBWUB Refresh **Leather** Cleaner" จึงเปลี่ยนมาแมปด้วยชื่อภาษาอังกฤษในวงเล็บแบบเต็มแทน และตรวจย้อนกลับด้วยไฟล์เมื่อวานว่าได้ค่าเดิมตรงกับที่แดชบอร์ดแสดงอยู่

## VIDEOS — 225 อัปเดต / 125 เพิ่มใหม่ (รวม 7,038 รายการ)

ไฟล์วีดีโอเป็น inlineStr XML ตามที่บันทึกไว้ (`openpyxl` อ่านไม่ได้) จึง parse `xl/worksheets/sheet1.xml` ด้วย zipfile + regex ได้ข้อมูล 6,689 แถว

วิธีรวมข้อมูล: `monthly.aug` = GMV ของช่วง 1–26 ส.ค. จากไฟล์โดยตรง, `gmv` = ผลรวมของ `monthly` ทุกเดือน, `date` คำนวณใหม่จากเดือนที่มีค่า, `units` ปรับด้วยส่วนต่างระหว่างไฟล์ 1–25 กับ 1–26 (เพื่อไม่ให้ทับยอดสะสมของเดือนก่อน) ชื่อสินค้าของวิดีโอใหม่แมปจาก "รหัสสินค้า" ตัวแรกโดยโหวตจากรายการเดิมที่มีรหัสเดียวกัน

ตรวจสอบหลังเขียน: entry regex จับได้ 7,038 รายการเท่ากับจำนวนบรรทัด, `node eval` ผ่านไม่มี error, ผลรวม `monthly.aug` = 1,197,873 ตรงกับผลรวม GMV ในไฟล์ export แบบปัดต่อรายการพอดี, ไม่มีรายการใดที่ `gmv ≠ ผลรวม monthly`

มี 349 รายการเดิมที่ไม่ปรากฏในไฟล์ export รอบนี้ ทุกรายการมี `aug = 0` อยู่แล้วจึงปล่อยไว้ตามเดิม และมีวิดีโอใหม่ 45 รายการที่แมปชื่อสินค้าไม่ได้ ถูกตั้งเป็น `Unknown` (ตรงกับรายการ Unknown เดิมที่มีอยู่แล้ว)

## สถานะ

คอมมิต `1f74e45` แตะ 2 ไฟล์ (`WIBWUB_Affiliate_Dashboard.html` 365+/240−, `sw.js`) — ประมาณ 0.6% ของไฟล์ ต่ำกว่าเพดาน 50% มาก · bump cache `wibwub-v872` → `wibwub-v873` · สำรองไว้ที่ `.bak_20260828_run` และ `.bak_videos_20260828_run` · `WIBWUB_Mobile.html` ถูกคอมมิตไปแล้วโดยงานอัตโนมัติตัวอื่นก่อนหน้า (ค่าในไฟล์ถูกต้อง ตรวจแล้ว)

`push_now.command` ถูกงานอัตโนมัติตัวอื่นเขียนทับเป็นเวอร์ชันย่อวันนี้ จึงเขียนกลับเป็นเวอร์ชันเต็ม (มีการลบ `.git/objects/*/tmp_obj_*` ที่ sandbox ลบเองไม่ได้ และรอกด Enter)

**ดับเบิลคลิก `push_now.command` เพื่อ deploy ขึ้นเว็บ**
