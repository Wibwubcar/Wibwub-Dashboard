# WIBWUB Sales Sheet Update — พฤหัส 16 ก.ค. 2026 09:30 (automated run)

## สรุป: ไม่มีข้อมูลใหม่ให้ sync — ไม่มี commit ใหม่

---

## STEP 1 — อ่าน Google Sheets (fileId ที่ถูกต้องจาก SKILL_update-wibwub_v2.md)

- Shopee: `10LrzWB8bbCO9FigCQFz5gZ3iSXMVhK0S` (gid=1820466351)
- Lazada: `1FxLAUiwabmNcBc3TA-bpHqg2MSK7uJ4U` (gid=1032656124)
- TikTok: `1k22c3PGY6aQjygAX6df_rQLR8aTzL-iz` (gid=150856480)

**ผลตรวจตาราง "ยอดรายเดือน":**
- **Shopee** — แถวล่าสุดคือ `01-31/05/26` (ยอดขาย 5,581,064 / ads 826,300 / fee 1,433,217.24 / order 9,093 / cancel 4.98%) — **ไม่มี row มิ.ย./ก.ค. 2569 เลย**
- **Lazada** — แถวล่าสุดคือ `01-31/05/26` (ยอดขาย 129,523.54 / ads 6,590 / fee 22,874.79 / coupon 2,910 / cost% 24.995 / order 157 / cancel 20.38%) — **ไม่มี row มิ.ย./ก.ค. 2569 เลย**
- **TikTok** — แถวล่าสุดคือ `01-12/07/26` (ยอดขาย 820,796.87 / ads+GMV spend 196,833.38 / fee+comm 222,667.11 / order 4,339 / cancel 7.12%) — **ไม่มี row หลัง 12/07 เลย**

## STEP 2 — เทียบกับค่าปัจจุบันใน Dashboard/Mobile

ตรวจ arrays ปัจจุบัน (7 elements, ม.ค.–ก.ค.): `TK_REV[6]=820796.87` ตรงกับ sheet แถว `01-12/07/26` เป๊ะ → ไม่มี delta

`SH_REV[5]/[6]` และ `LZ_REV[5]/[6]` (มิ.ย./ก.ค.) มีค่าอยู่แล้วในไฟล์ แต่**ค่านี้ไม่ตรงกับ sheet ปัจจุบัน**เพราะ sheet ไม่มี row มิ.ย./ก.ค. เลย — ตรวจ git history พบว่าค่าดังกล่าวมาจาก commit `b2f36f3` (6 ก.ค., อ้างว่าทีมเพิ่งกรอกชีต) ซึ่งถูก sheet revert ในเช้าวันถัดมา (`0503947`, 7 ก.ค.) แล้วภายหลังถูก restore กลับพร้อม "reconciliation guard" ที่กันไม่ให้ fetch ที่คืนค่า 0 มาทับค่าที่มีอยู่ (`3af1e5d`, 7 ก.ค., อ้างอิงจาก screenshot ผู้ใช้ ไม่ใช่การอ่าน sheet สด)

**รอบนี้ไม่แตะค่า SH_REV/LZ_REV มิ.ย.-ก.ค. ตาม guard เดิม** — เป็นค่าที่ยังไม่ verified จาก sheet สด แต่ยังไม่ revert เพราะไม่มีข้อมูลใหม่มาแทนที่ (การลบออกจะทำให้ dashboard เดือน มิ.ย./ก.ค. หายไปเลย ซึ่งแย่กว่า)

## STEP 3–6: ข้าม (ไม่มีข้อมูลใหม่)
ไม่แก้ arrays / KPI text / date picker, ไม่ bump sw.js, ไม่ commit

---

## ⚠️ ประเด็นที่ควรแจ้งทีม (นอกขอบเขต task นี้)

1. **Shopee/Lazada sheet ยังไม่มีข้อมูล มิ.ย./ก.ค. 2569 เลย** (ตรวจซ้ำแล้วตั้งแต่ 6 ก.ค. — 10 วันแล้วที่ยังไม่มีการกรอก) ทั้งที่ dashboard แสดงตัวเลขมิ.ย./ก.ค. อยู่ (มาจาก screenshot/การยืนยันด้วยมือครั้งก่อน ไม่ใช่จาก sheet) — ควรให้ทีมกรอกยอดสะสมรายเดือนลง sheet จริง เพื่อให้ auto-update ทำงานได้ตามปกติและตรวจสอบความถูกต้องของตัวเลขที่แสดงอยู่ตอนนี้
2. **`TK_ADS` array มี 6 elements** (ม.ค.-มิ.ย.) แต่ `TK_REV`/`M5` มี 7 (ถึง ก.ค.) — length ไม่ตรงกัน ควรตรวจสอบ/เติมให้ครบเมื่อมีข้อมูล ads ก.ค. ของ TikTok
