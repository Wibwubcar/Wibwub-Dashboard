# รายงานอัปเดต WIBWUB Affiliate Dashboard — วันที่ 19 ก.ค. 2569 (รอบ wibwub-thursday-affiliate, 13:03)

## สรุปสถานะ
อัปเดตข้อมูล Affiliate ประจำเดือน กค. 69 (ช่วง 1-18 กค.) เรียบร้อย โดยเจอปัญหา TikTok Affiliate Center export ล่ม (HTTP 503) ตลอดทั้งรอบ จึงต้องใช้วิธีสำรองในการดึงข้อมูลแทน รายละเอียดด้านล่าง

หมายเหตุ: มีรอบก่อนหน้าในวันเดียวกัน (`WIBWUB_Affiliate_Update_Report_20260719_thu.md`) ที่รายงานว่าไม่ได้แก้ไขไฟล์ dashboard เพิ่มเติม เนื่องจากทับซ้อนกับรอบ wibwub-monday-update ก่อนหน้า อย่างไรก็ตาม รอบนี้ (wibwub-thursday-affiliate) ได้ดำเนินการอัปเดตข้อมูลจริงตามรายละเอียดด้านล่าง — ควรตรวจสอบว่าตัวเลขไม่ถูกอัปเดตซ้ำซ้อนผิดพลาด

## ⚠️ ปัญหาที่พบ: TikTok Export ล่ม (HTTP 503)
- ลองดาวน์โหลดไฟล์ export ทั้ง 4 ประเภท (ครีเอเตอร์/สินค้า/วีดีโอ/ไลฟ์สตรีม) — export job ขึ้นสถานะ "ready" ทุกครั้ง แต่ตัว endpoint สำหรับดาวน์โหลดไฟล์จริงตอบกลับ 503 ทุกครั้ง (ลองซ้ำหลายรอบ ทั้งครีเอเตอร์และสินค้า)
- ยืนยันว่าเป็นปัญหาฝั่ง TikTok เอง ไม่ใช่ปัญหาเฉพาะหมวดใดหมวดหนึ่ง
- ไฟล์ที่ดาวน์โหลดสำเร็จมีเพียง `Core_Stats_20260701-20260718_...xlsx` เท่านั้น
- ไฟล์ `Creator_List_...xlsx` ที่ LaunchAgent ย้ายเข้าโฟลเดอร์ Data Affiliate/ครีเอเตอร์/ ไปแล้ว ตรวจสอบพบว่าเป็นไฟล์เปล่า (มีแค่ header ไม่มีข้อมูล) — เป็นผลจากการดาวน์โหลดที่ล้มเหลว
- นอกจากนี้ยังพบว่าหน้า TikTok ถูกปรับดีไซน์ใหม่ — หมวดวีดีโอและไลฟ์สตรีมไม่มีตารางรายการย่อยให้ export/scrape แล้ว เหลือแค่ตัวเลขสรุป (KPI) ในหน้า "ผลการดำเนินงาน" เท่านั้น

## วิธีแก้ปัญหาที่ใช้
- **ครีเอเตอร์**: ใช้ตัวเลขจาก Core_Stats (GMV, ค่าคอม) ผสานกับ KPI "ยอดขายจากครีเอเตอร์" จากหน้า Performance (=514 ราย ตรงกับค่าฐานเดิม AF_CR[6] พอดี) แทนไฟล์ Creator_List ที่ดาวน์โหลดไม่ได้
- **สินค้า**: ดึงข้อมูล cr (จำนวนครีเอเตอร์) และ vid (จำนวนวิดีโอ) โดยตรงจากตาราง live บนหน้าเว็บ (DOM scraping) แทนไฟล์ export ที่ดาวน์โหลดไม่ได้ — ครบทั้ง 7 สินค้าเป้าหมาย
- **วีดีโอ/ไลฟ์สตรีม**: ไม่มีตารางรายการย่อยให้ดึงแล้วในดีไซน์ใหม่ของ TikTok มีแค่ตัวเลขสรุป (วิดีโอ=645, ไลฟ์สตรีม=367) ไม่ได้นำไปอัปเดตค่าใดในแดชบอร์ด (ไม่มีฟิลด์ที่เก็บข้อมูลนี้)
- **ยอดคืนเงิน (returns)**: มี 2 ตัวเลขให้เลือกจากหน้าต่างกัน (51,006.19 vs 12,261.67) ใช้ 12,261.67 เพราะสัดส่วนต่อ GMV (~1.37%) สอดคล้องกับรูปแบบเดือนก่อนหน้า (มี.ค.-มิ.ย. อยู่ที่ 1.47-2.12%) ส่วนอีกตัวเลขสูงผิดปกติ (5.68%)

## ตัวเลขที่อัปเดต (กค. 69, 1-18 กค.)
| รายการ | ค่าเดิม | ค่าใหม่ |
|---|---|---|
| GMV รวม | 897,816 | 897,947 |
| Net GMV (หัก returns) | 877,916 | 885,685 |
| ค่าคอมมิชชั่นรวม | 6 (ผิดพลาด/ไม่สมบูรณ์) | 103,159 |
| จำนวนครีเอเตอร์ | 514 | 514 (ไม่เปลี่ยน) |

อัปเดตทั้งใน `WIBWUB_Affiliate_Dashboard.html` (AF_GMV, AF_NET, AF_COM, AF_CR) และ `WIBWUB_Mobile.html` (AFI_GMV, AFI_NET, AFI_COMM) แบบ overwrite index ล่าสุด (เดือนเดิมตรงกับ label อยู่แล้ว ไม่ได้เพิ่มเดือนใหม่)

## สินค้า (PRODUCTS array) — อัปเดตเฉพาะ cr/vid
| สินค้า | cr เดิม→ใหม่ | vid เดิม→ใหม่ |
|---|---|---|
| Leather Wipes | 192→239 | 104→141 |
| Interior Wipes | 131→153 | 58→75 |
| Sugar | 77→89 | 95→115 |
| Cleaner | 39→39 | 15→18 |
| Interior | 37→43 | 53→70 |
| Refresh ⚠️ | 32→33 | 25→29 |
| Visible | 9→11 | 9→12 |

⚠️ **หมายเหตุ "Refresh"**: ไม่พบสินค้าชื่อ "WIBWUB Refresh" ตรงตัวในระบบ TikTok (70 สินค้า, 7 หน้า) จึงจับคู่แบบใกล้เคียงที่สุดกับ "WIBWUB Refresh Leather Cleaner" — เป็นการประมาณการ ควรตรวจสอบซ้ำในรอบถัดไป

ไม่แตะฟิลด์ gmv/units/monthly/ret ของสินค้าใดๆ ตามข้อกำหนด

## ไฟล์ที่แก้ไข
1. `WIBWUB_Affiliate_Dashboard.html`
2. `WIBWUB_Mobile.html`
3. `sw.js` — bump cache version wibwub-v410 → wibwub-v411

## ขั้นตอนถัดไป
**ดับเบิลคลิกไฟล์ `push_now.command`** ในโฟลเดอร์ claude/All เพื่อ commit + push ขึ้น GitHub (สคริปต์จะ add ไฟล์ทั้ง 3 ที่แก้ไข, commit พร้อมข้อความวันที่ และ push ให้อัตโนมัติ)

## รายการที่ไม่ได้ทำ / ควรตรวจสอบเพิ่ม
- ไฟล์ export ทั้ง 4 ประเภท (ครีเอเตอร์/สินค้า/วีดีโอ/ไลฟ์สตรีม) ไม่สามารถดาวน์โหลดได้จริงในรอบนี้เนื่องจากบั๊ก 503 ฝั่ง TikTok — แนะนำให้ลองอีกครั้งในรอบถัดไป หากยังล่มอยู่อาจต้องพึ่งวิธี scrape ต่อไป
- ตรวจสอบการจับคู่สินค้า "Refresh" อีกครั้งเมื่อ export ใช้งานได้ปกติ
- **สำคัญ**: ควรตรวจสอบว่าตัวเลข GMV/Net/ค่าคอมที่อัปเดตในรอบนี้ไม่ซ้ำซ้อนกับรอบ wibwub-monday-update ก่อนหน้า (ดูรายงานรอบก่อนหน้าประกอบ) เนื่องจากทั้งสองรอบทำงานในวันเดียวกันและช่วงเวลาใกล้กัน
