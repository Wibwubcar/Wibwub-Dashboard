# WIBWUB Weekly Update — วันพุธ 22 ก.ค. 2569 (รันอัตโนมัติ, wibwub-monday-update)

**หมายเหตุสำคัญ**: task นี้รันไป**สองรอบในวันเดียวกัน** — รอบแรกเวลา 09:17 น. (commit `bf20909`) รีเฟรช Top Products เสร็จแต่ TikTok Affiliate export ยังค้าง (HTTP 503) จึงข้าม Affiliate ไปก่อน รอบที่สอง (รายงานนี้ ตอนบ่าย) แก้ปัญหา export ที่ค้างไว้สำเร็จ + รีเฟรช Top Products อีกครั้งด้วยข้อมูลใหม่กว่า — commit ล่าสุด `d8531a6` ยังไม่ push

---

## ✅ สิ่งที่ทำสำเร็จ (รอบบ่าย)

### 0. ตรวจสอบ M5 array (Protection check)
ตรวจแล้วถูกต้องอยู่แล้ว (7 รายการ ตรงเดือนกรกฎาคม) ทั้ง WIBWUB_Dashboard.html และ WIBWUB_Mobile.html — ไม่ต้องแก้

### 1. Export Shipnity + รีเฟรช Top Products (ครั้งที่ 2 ของวัน)
- Parse ไฟล์ Shipnity ทั้งหมด 48 ไฟล์ใน `Data Shipnity/` (dedupe ด้วย เลขออเดอร์+รหัสสินค้า+จำนวน) พร้อมเพิ่ม logic แยกยอด "สินค้าสำหรับทำการตลาด" (ของแจก/ส่ง Influencer) ออกจากยอดขายจริง → ใส่เข้า `mk`/`mkq` แทน `v`/`q` ตาม schema เดิม
- อัปเดต `ALL_PRODUCTS` + `PROD_MO` (WIBWUB_Mobile.html) และ KPI/ตาราง Top สินค้าขายดี + กราฟแท่ง + กราฟโดนัท (WIBWUB_Dashboard.html)
- **อันดับ 1 Wool Duster ฿5.44M (8,515 ชิ้น)** — สูงกว่ารอบเช้า (฿5.43M) เล็กน้อยจากยอดขายที่เพิ่มขึ้นระหว่างวัน
- ยอดขายรวมทั้งหมด (141 สินค้า, ไม่รวมของแจก) = ฿53.24M / 206,724 ชิ้น
- ช่องทางหลัก TikTok ขยับเป็น 17% ของยอดขาย 15 สินค้าหลัก

### 2. TikTok Affiliate Creator List Export — **สำเร็จ** (แก้ปัญหาที่ค้างจากรอบเช้า/HTTP 503 ได้แล้ว)
- ตั้งช่วงวันที่ กำหนดเอง 2026-07-01 ถึง 2026-07-21 ในหน้า "ครีเอเตอร์" (ไม่ใช่หน้า KPI สรุปด้านบนที่ export "Core_Stats" ผิดประเภท)
- กดปุ่ม "ส่งออก" ที่ตารางครีเอเตอร์โดยตรง (แยกจากปุ่ม export ของ KPI การ์ดด้านบน) → export สำเร็จเร็วกว่าที่คาด (ไม่ต้องรอ 60-90 วิ) → ดาวน์โหลดไฟล์ `Creator_List_20260701-20260721_20260722134220.xlsx` (14,993 ครีเอเตอร์, 24 คอลัมน์) เข้า `Data Affiliate/ครีเอเตอร์/` อัตโนมัติ

### 3. อัปเดต Affiliate arrays จากข้อมูลจริง (1-21 ก.ค.)
คำนวณจากไฟล์ Creator List จริง:
- GMV จากแอฟฟิลิเอต = ฿1,094,618 (คอลัมน์ B ทุกแถว)
- Net GMV (หลังหักคืนเงินเฉพาะครีเอเตอร์ที่มียอด) = ฿1,028,111
- ค่าคอมมิชชั่นโดยประมาณ = ฿125,901 (คอลัมน์ H)
- ครีเอเตอร์ที่มียอดขาย (GMV > 0) = 607 ราย จาก 14,993 รายทั้งหมด — สอดคล้องกับ scale ของ AF_CR เดิม (398→418→558→398→576) ต่อเนื่องเป็น 607

อัปเดตเฉพาะ**ตำแหน่งเดือนปัจจุบัน** (ก.ค.) เปลี่ยน label จาก `"ก.ค. (1-20)"` → `"ก.ค. (1-21)"` — ไม่แตะค่าประวัติเดือน มี.ค.-มิ.ย. เลย:
- `AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR` ใน WIBWUB_Affiliate_Dashboard.html
- `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM` ใน WIBWUB_Mobile.html (label เป็น `"กค.69 (1-21)"`)

### 4. Bump เวอร์ชัน + Commit
- `sw.js`: `wibwub-v443` → `wibwub-v444`
- Commit `d8531a6`: **4 ไฟล์เท่านั้น** (WIBWUB_Affiliate_Dashboard.html, WIBWUB_Dashboard.html, WIBWUB_Mobile.html, sw.js) — ไม่แตะไฟล์ untracked/modified อื่นจาก task อื่นในโฟลเดอร์เดียวกัน
- เจอ `.git/index.lock` ค้าง (ปกติของ mount นี้) — rename ทิ้งแบบเดิมที่เคยทำสำเร็จมาก่อน ไม่ได้ force ลบขณะมี process อื่นทำงานจริง แล้ว commit ผ่านปกติ
- ตรวจ diff แล้วยืนยันว่าค่าประวัติของทั้ง AF_* และ AFI_* ไม่ถูกเขียนทับ (เปลี่ยนเฉพาะ element สุดท้าย)
- `push_now.command` มีอยู่แล้วและถูกต้อง ไม่ต้องแก้

---

## ⛔ สิ่งที่ทำไม่ได้ / ข้อจำกัด

1. ยังไม่ push commit `d8531a6` ขึ้น GitHub (sandbox push ตรงไม่ได้ ต้องรันจากเครื่อง)
2. จำนวนชิ้น (q) ของ Top Products อัปเดตเฉพาะยอดขาย ไม่ได้อัปเดต q รายเดือน เหมือนทุกรอบก่อนหน้า (ไม่มี array ประวัติจำนวนชิ้นให้อ้างอิง)
3. การ์ด KPI แบบ static text (เช่น "ครีเอเตอร์ที่มียอด 13,945", "อัปเดต 20 ก.ค.") ในส่วน Overview ของ WIBWUB_Affiliate_Dashboard.html ไม่ได้แก้ในรอบนี้ เนื่องจากไม่ใช่ตัวแปรที่ task ระบุชื่อไว้ชัดเจน (AF_*/AFI_* เท่านั้น) และเสี่ยงตีความสูตรคำนวณผิดหากไม่มีต้นฉบับยืนยัน — ปล่อยไว้ตามเดิมเพื่อความปลอดภัย

---

## 📊 สถานะ dashboard ปัจจุบัน (หลังแก้ไข รอบบ่าย)
- Top Products ก.ค. MTD: Wool Duster ฿5.44M (8,515 ชิ้น) เป็นอันดับ 1, ยอดขายรวมทั้งหมด ฿53.24M / 206,724 ชิ้น
- Affiliate ก.ค. (1-21): GMV ฿1,094,618 · Net ฿1,028,111 · Commission ฿125,901 · ครีเอเตอร์ที่มียอด 607 ราย
- sw.js = v444, commit `d8531a6` พร้อม push

## ▶️ ขั้นถัดไป (แนะนำ)
1. รัน `push_now.command` เพื่อ push commit `d8531a6` ขึ้น GitHub
2. มีบอทอัตโนมัติหลายตัวทำงานบน repo เดียวกันในวันเดียวกัน (auto: update, auto-update stock, wibwub-monday-update) — เห็น `.git/index.lock` ชนกันเป็นระยะ ควรพิจารณาจัด schedule ให้ไม่ทับเวลากันเพื่อลดความเสี่ยง race condition
3. หากต้องการความแม่นยำของการ์ด KPI static text ใน Affiliate Overview ควรระบุสูตร/ตัวแปรที่ต้องการอัปเดตให้ชัดเจนในรอบถัดไป
