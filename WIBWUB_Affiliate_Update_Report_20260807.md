# WIBWUB Affiliate Update — วันศุกร์ 7 ส.ค. 2569 (automated run, wibwub-thursday-affiliate)

**สรุป: ไม่ได้แก้ไข dashboard — เจอ TikTok export endpoint ตอบ 503 ต่อเนื่อง และตรวจพบว่าข้อมูลช่วง 1-5 ส.ค. ถูกอัปเดตไว้แล้วโดย automation อีกตัวก่อนหน้าในวันเดียวกัน**

---

## สิ่งที่ทำเมื่อเริ่มรัน

1. เปิด TikTok Affiliate Center → หน้า "ผลการดำเนินงาน" (Performance) → ตั้งช่วงวันที่ **01/08/2026 - 05/08/2026** (05 ส.ค. คือวันล่าสุดที่มีข้อมูล ตาม "อัปเดตเมื่อ: 5 ส.ค. 2026")
2. กดปุ่ม "ส่งออก" (export) ครบทั้ง 4 แท็บ: ครีเอเตอร์ / สินค้า / วิดีโอ / ไลฟ์สตรีม — export job ขึ้นสถานะสำเร็จในแผงรายงาน (เห็นชื่อไฟล์ + เวลา ส่งออกเมื่อ ส.ค. 07, 2026 ~08:06PM)
3. กด "ดาวน์โหลด" ที่ไฟล์แต่ละประเภท — **ไม่มีไฟล์ตกลงในโฟลเดอร์ Downloads เลยแม้แต่ไฟล์เดียว**
4. ตรวจ network requests เบื้องหลังปุ่มดาวน์โหลด พบว่า endpoint `export_task/export?...task_id=...` ตอบ **HTTP 503** ทุกครั้ง (ลองซ้ำ 4 ครั้ง คนละ task_id 2 ชุด ห่างกัน >30 วินาที) — เป็นปัญหาฝั่งเซิร์ฟเวอร์ TikTok เอง ไม่ใช่การตั้งค่าหรือการคลิกผิดของเรา (ปัญหาเดียวกับที่เคยเจอและบันทึกไว้ในรายงาน 26 ก.ค. 2569)

## ทำไมถึงไม่แก้ dashboard ต่อ

ก่อนจะยอมแพ้กับ TikTok 503 ตรวจ git log ของโฟลเดอร์นี้ก่อน แล้วพบว่า**งานของ workflow นี้ (อัปเดต Affiliate GMV/Net/Commission/ครีเอเตอร์ ช่วง 1-5 ส.ค.) เสร็จไปแล้วในวันนี้โดย automation อีกตัว**:

- Commit `f38e036` "auto-update: Monday 2026-08-07 — Shipnity Top Products + Affiliate GMV/Commission + cache bump" (07 ส.ค. 02:36 UTC)
- ตรวจ `WIBWUB_Affiliate_Dashboard.html` และ `WIBWUB_Mobile.html` ปัจจุบัน พบ array ตรงกับช่วงที่ต้องการอัปเดตพอดี:
  - `AF_MO` มี label ล่าสุด **"ส.ค. (1-5)"** → GMV ฿259,325 · Net ฿239,328 · Commission ฿30,047 · ครีเอเตอร์ 208 ราย
  - `AFI_MONTHS` (Mobile) มี label ล่าสุด **"สค.69 (1-5)"** ตรงกัน ตัวเลขเดียวกัน
- Local repo ตรงกับ origin/main พอดี (0 ahead / 0 behind) — ไม่มีอะไรค้าง push

เนื่องจากช่วงวันที่ที่ต้องการ (1-5 ส.ค.) ถูกอัปเดตไปแล้วครบถ้วนพอดี การพยายามดาวน์โหลดไฟล์ TikTok มาประมวลผลซ้ำ (ต่อให้ 503 หายไปตอนนี้) จะเสี่ยงเขียนทับข้อมูลเดือนเดียวกันโดยไม่จำเป็น จึง**ไม่แตะ** `WIBWUB_Affiliate_Dashboard.html`, `WIBWUB_Mobile.html`, `sw.js` (ยังคง v598), และ `push_now.command` ในรอบนี้ — ทำตามกฎความปลอดภัยของ task นี้ที่ห้าม overwrite เดือนที่มีข้อมูลแล้วโดยไม่จำเป็น

## สิ่งที่ไม่ได้ทำ (และเหตุผล)

| ขั้นตอน | สถานะ | เหตุผล |
|---|---|---|
| ย้ายไฟล์เข้า `Data Affiliate/*` | ไม่ได้ทำ | ไม่มีไฟล์ให้ย้าย (ดาวน์โหลดไม่สำเร็จเลยสักไฟล์จาก 503) |
| อัปเดต Affiliate GMV/Net/Comm/ครีเอเตอร์ | ไม่จำเป็น | มีข้อมูลช่วง 1-5 ส.ค. ครบแล้วจาก commit `f38e036` |
| อัปเดต PRODUCTS cr/vid | ไม่ได้ทำ | ไม่มีไฟล์สินค้าให้ประมวลผล |
| อัปเดต VIDEOS array | ไม่ได้ทำ | ไม่มีไฟล์วิดีโอให้ประมวลผล |
| Bump sw.js / push_now.command | ไม่จำเป็น | ไม่มีการแก้ไขไฟล์ dashboard ในรอบนี้ |

## แนะนำให้ทำต่อ

1. **ไม่ต้องทำอะไรเพิ่ม** สำหรับข้อมูลช่วง 1-5 ส.ค. — อัปเดตครบแล้วจากรอบก่อนหน้าของวันนี้
2. รอบถัดไป (เมื่อ TikTok หายจากปัญหา 503) ควรลองอัปเดตต่อให้ถึงข้อมูลล่าสุด (เช่น 1-6 หรือ 1-7 ส.ค.) เมื่อ TikTok มีข้อมูลใหม่กว่านี้ให้ export
3. หากต้องการยืนยันว่า TikTok export endpoint กลับมาใช้งานได้แล้ว ลองกดดาวน์โหลดไฟล์ในแผงรายงานของหน้า "ผลการดำเนินงาน" ด้วยตนเองอีกครั้ง
