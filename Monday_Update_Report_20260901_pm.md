# WIBWUB Monday Update — 1 ก.ย. 2569 (รอบบ่าย)

**สถานะ: สำเร็จ** — รอบนี้เป็น *การตรวจสอบ* มากกว่าการอัปเดต เพราะรอบเช้าทำงานหลักไปแล้ว
พบและแก้ค่าผิดจริง 1 จุด · ปลดล็อก blocker เรื่อง Transaction Analysis ได้แล้ว

Commit: `ec1768b` · sw.js `wibwub-v933` → `wibwub-v934` · เท่ากับ `origin/main`

---

## สิ่งที่แก้จริง (1 จุด)

**ค่าคอมมิชชั่น Affiliate เดือน ส.ค. ผิดไป 235 บาท**

| | ในไฟล์ (ก่อนแก้) | หน้า TikTok Performance | แก้เป็น |
|---|---|---|---|
| Commission ส.ค. (1-30) | 193,567 | 193,331.74 | **193,332** |

แก้ที่ **index สุดท้ายเท่านั้น** ทั้ง 2 ไฟล์:

- `WIBWUB_Affiliate_Dashboard.html` → `AF_COM[7]`
- `WIBWUB_Mobile.html` → `AFI_COMM[9]`

สคริปต์ที่ใช้มี assertion กันพลาด (`assert len(vals)==8`) แล้วเขียนทับ `vals[-1]` ตัวเดียว ตามกฎ rolling-window — ยืนยันด้วย `git diff` แล้วว่าเดือนก่อนหน้าไม่ถูกแตะแม้แต่ byte เดียว

ค่าอีก 3 ตัว (GMV 1,680,781 · NET 1,653,348 · Creators 828) **ตรงกับหน้า TikTok เป๊ะอยู่แล้ว** ไม่ต้องแก้

---

## Blocker รอบที่แล้วปลดได้แล้ว

รายงานเช้าค้างไว้ว่า "หน้า Transaction Analysis ถูกถอด → ทำต่อไม่ได้"

**ทางออก: ใช้หน้า "ผลการดำเนินงาน" (Performance) แทน** — หน้านี้แสดง GMV / ยอดสุทธิ / คอมมิชชั่น / จำนวนครีเอเตอร์ ครบทั้ง 4 ตัวที่ STEP 4 ต้องการ และเป็นตัวเลข settled เหมือนกัน (ยืนยันโดย GMV/NET/CR ตรงกับที่ในไฟล์ทุกหลัก)

`/insights/transaction-analysis` redirect มาที่หน้านี้เอง → ควรแก้ STEP 2 ใน SKILL.md ให้ชี้มาที่หน้า Performance โดยตรง

---

## จุดที่เบี่ยงจาก runbook (ตั้งใจ ทั้ง 4 ข้อ)

### 1. M5 guard — เติมแล้ว **ย้อนกลับ**

สคริปต์ตาม STEP 0 ขยาย `M5` จาก 8 → 9 (เติม `"ก.ย."`) เพราะวันนี้เดือน 9
แต่ array ข้อมูลที่คู่กับมันทุกตัว (~30 ตัว) ยังยาว 8 · `MTD_COVERAGE` = `2026-08-30` · ข้อมูล TikTok เองก็หยุดที่ 30 ส.ค.

→ label ที่ 9 ที่ไม่มีข้อมูลคู่ = แท่งกราฟ `undefined` ทุกหน้า จึง**ย้อน M5 กลับเป็น 8**

**สาเหตุราก:** กฎ STEP 0 สมมติว่า "ถึงเดือน N แล้วต้องมีข้อมูลเดือน N" ซึ่งไม่จริงเมื่อรันในวันที่ 1 ของเดือน
**ข้อเสนอ:** เปลี่ยนเงื่อนไขเป็น "ขยาย M5 ก็ต่อเมื่อมีข้อมูลจริงของเดือนนั้นแล้ว" ไม่ใช่ดูจากเลขเดือนอย่างเดียว

### 2. สคริปต์ STEP 4 อ่านไฟล์ผิดคอลัมน์ — **บั๊กจริง ต้องแก้ SKILL.md**

สคริปต์สมมติไฟล์ Transaction Analysis มี **12 คอลัมน์** (`col[1]`=GMV, `col[2]`=returns, `col[10]`=commission)
ไฟล์จริง `Transaction_Analysis_Creator_List_20260801-20260829.xlsx` มี **22 คอลัมน์** · 7,802 แถว

ผลถ้ารันตามสคริปต์:

| | สคริปต์ให้ | ค่าจริง (TikTok) | ผล |
|---|---|---|---|
| GMV | 1,603,562 | 1,680,781 | ต่ำไป 77K |
| Returns | 92,913 | 27,433 | สูงไป 3 เท่า |
| Commission | **0** | 193,332 | พังสนิท |
| Creators | 797 | 828 | ขาด 31 |

→ **ไม่ใช้สคริปต์** อ่าน 4 ตัวเลขจากหน้า Performance ตรง ๆ แทน

### 3. Shipnity — ดึง "เดือนที่แล้ว" ไม่ใช่ 1 ก.ย.–1 ก.ย.

Shipnity บังคับช่วงข้อมูล **ไม่เกิน 1 เดือน** (tooltip: *"ต้องเลือกช่วงเวลาของข้อมูลไม่เกิน 1 เดือน"*) และปุ่ม export จะ disable เงียบ ๆ ถ้าเกิน — คลิกไปแล้วไม่มีอะไรเกิดขึ้น

นอกจากนี้ export ยังเปิด dialog ที่ต้องเลือก **"ไฟล์เดียว"** ก่อน ถึงจะเริ่มโหลด — runbook ไม่ได้ระบุขั้นนี้

เลือกดึง ส.ค. เพราะกันยายนยังไม่มีช่องใน array และ ส.ค. คือ index ที่ live อยู่

ไฟล์ที่ใช้: `Data Shipnity/Data_01-09-2026.xlsx` — 34,931 แถว × 20 คอลัมน์ · **100% เป็นข้อมูลเดือน ส.ค.** (แถวล่าสุด `31/08/2026 23:58`)

### 4. Top Products — **ตรวจสอบ ไม่ได้เขียนทับ**

รอบเช้าอัปเดต Top Products เป็น `ส.ค. (1-31)` ไปแล้ว ผมคำนวณ top-15 ของเดือน ส.ค. ใหม่เองจากไฟล์ดิบเพื่อ cross-check → **ตรงกันในระดับ ~1-3%**

ส่วนต่างอธิบายได้ด้วยการจับกลุ่ม SKU: dashboard รวมขนาดเข้าด้วยกัน (เช่น Reflex 250ml + 500ml) แต่ข้อมูลดิบแยกรายการ

**ตัดสินใจไม่ regenerate** เพราะ (ก) ต้องอ่านไฟล์ 97 ไฟล์ รวม ~1.4GB ซึ่งเกิน bash timeout 45 วิ และ (ข) การเดาสูตรจับกลุ่มเองมีความเสี่ยงทำข้อมูลที่ถูกอยู่แล้วพัง

---

## การตรวจสอบขั้นสุดท้าย

```
Dashboard  M5=8  · SH_REV/TK_REV/LZ_REV/SH_ORD/TK_ORD/LZ_ORD = 8  ✅
Mobile     M5=8  · SH_REV/TK_REV/LZ_REV/SH_ORD/TK_ORD/LZ_ORD = 8  ✅
Affiliate  AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR = 8                   ✅
Mobile AFI AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM = 10               ✅
RESULT: ALL CONSISTENT
```

- ✅ `git diff` ยืนยัน: เปลี่ยนเฉพาะ index สุดท้าย (ส.ค.) — เดือนก่อนหน้าไม่ถูกแตะ
- ✅ array length ทุกคู่สอดคล้องกัน
- ✅ commit `ec1768b` = `origin/main`
- ✅ `push_now.command` สร้างใหม่ + `chmod +x` แล้ว
- ⚠️ warning `Operation not permitted` ตอน git cleanup temp objects — เป็นข้อจำกัด Google Drive filesystem ตามปกติ commit ผ่านแล้ว

**หมายเหตุ:** commit นี้มีการแก้ที่ค้างจากรอบเช้าติดมาด้วย 359 บรรทัดใน `WIBWUB_Affiliate_Dashboard.html` (ยอด GMV รายวิดีโอครีเอเตอร์, `797`→`828` ครีเอเตอร์, `ส.ค. 1-29`→`1-30`) ซึ่งอยู่ใน working tree ก่อนผมเริ่ม

---

## ควรแก้ SKILL.md 4 จุด

1. **STEP 4 คอลัมน์ผิด** — ไฟล์จริง 22 คอลัมน์ ไม่ใช่ 12 · แนะนำอ่านจากหน้า Performance แทนการ parse xlsx
2. **STEP 2 URL ตาย** — `transaction-analysis` redirect ไปหน้า Performance แล้ว ให้ชี้ตรงไปเลย
3. **STEP 0 M5 guard** — เปลี่ยนเงื่อนไขจาก "ดูเลขเดือน" เป็น "ดูว่ามีข้อมูลจริงหรือยัง" กันเคสรันวันที่ 1
4. **กฎ Chrome ผิด** — runbook บอก *"ห้ามใช้ navigate แบบ standalone เด็ดขาด"* แต่รอบนี้ `browser_batch` + `navigate` **fail** ตอนแท็บอยู่ที่ `chrome://newtab/` (error: *"Can't interact with browser internal pages"*) และ `tabs_create_mcp` ก็เมิน parameter `url` เงียบ ๆ · **`navigate` แบบ standalone คือตัวที่ใช้ได้จริง** ควรแก้กฎเป็น "ถ้าแท็บปัจจุบันเป็น internal page ให้ใช้ navigate เดี่ยวก่อน"

เพิ่มเติมที่ควรบันทึก: Shipnity มีลิมิตช่วงข้อมูล 1 เดือน และมี dialog "ไฟล์เดียว" ที่ต้องกดก่อน export

---

## ค้างอยู่

- ไฟล์ Shipnity ที่สั่งโหลดใหม่ (ส.ค. 1–31) ยังโหลดไม่เสร็จตอนจบ session — **ซ้ำซ้อน ไม่จำเป็น** เพราะ `Data_01-09-2026.xlsx` ที่มีอยู่ครอบคลุมถึง 31/08 23:58 อยู่แล้ว ถ้าโหลดเสร็จให้ `cp` เข้า `Data Shipnity/` (ห้าม `mv`/`rm` จาก Downloads — LaunchAgent `com.wibwub.download-mover` จัดการเอง)
- รอบ 7 ก.ย. → ขยาย M5 เป็น 9 พร้อมข้อมูลกันยายนจริงของ Shopee/TikTok/Lazada
