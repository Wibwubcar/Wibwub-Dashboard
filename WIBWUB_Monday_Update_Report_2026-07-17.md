# WIBWUB Weekly Update — 17 กรกฎาคม 2026 (รันอัตโนมัติ)

## สรุปผล

| Step | สถานะ | รายละเอียด |
|---|---|---|
| 0. ตรวจสอบ M5 array | ✅ ไม่ต้องแก้ | `M5` มี 7 รายการ (ม.ค.–ก.ค.) ใน WIBWUB_Dashboard.html และ WIBWUB_Mobile.html อยู่แล้ว ถูกต้อง |
| 1. Export Shipnity | ✅ สำเร็จ | Export จาก shipnity.com สำเร็จ → `Data Shipnity/Data_17-07-2026.xlsx` (17,297,399 bytes) และคัดลอกทับ `Data Shipnity/Data_กรกฎาคม.xlsx` (ไฟล์หลักของเดือน) — หมายเหตุ: ตัวเลือกช่วงวันที่บนปฏิทิน Shipnity เลือกเป็น "เดือนนี้" (1-31 ก.ค.) แทนที่จะเป็น 1-17 เนื่องจาก UI คลิกพลาด แต่เทียบเท่ากันเชิงข้อมูล (ยังไม่มีออเดอร์ในวันที่ยังไม่ถึง) |
| 2. Export TikTok Affiliate | ⚠️ **ล้มเหลว — ค้างสถานะ "กำลังส่งออก"** | สั่ง export "Creator_List_20260701-20260716" (12,243 แถว) แล้วรอ >10 นาที (เกินกว่า worst-case 8 นาทีที่เคยเจอ) สถานะไม่ขยับเลย ไม่มี % แสดง — จึงยกเลิกและไม่ใช้ข้อมูลใหม่รอบนี้ |
| 3. อัปเดต Top Products | ⏭️ ข้าม (เหมือนรอบก่อน) | ยังไม่มี mapping table ชื่อสินค้าดิบจาก Shipnity → ยังไม่ปลอดภัยที่จะ auto-update |
| 4. อัปเดต Affiliate arrays | ⏭️ ไม่มีข้อมูลใหม่ (แต่พบ+แก้บั๊กสำคัญ) | ดูรายละเอียดด้านล่าง — TikTok export ล้มเหลว จึงไม่มีข้อมูลใหม่กว่า 1-15 ก.ค. ให้ใช้ |
| 5. Bump sw.js + iframe cache-buster + commit | ✅ สำเร็จ | `wibwub-v392` → `wibwub-v393`, iframe `?v=255` → `?v=256`, commit `a6f2cb4` |
| 6. Push ขึ้น GitHub | ⏳ รอผู้ใช้ | Sandbox push ติด `403 from proxy` เหมือนเดิม — รัน `push_now.command` (ดับเบิลคลิกในเครื่อง) เพื่อ push |

## ⚠️ พบบั๊กสำคัญ: ข้อมูล Affiliate เดือน ก.ค. ถูกเขียนทับผิดโดย task อื่น

ก่อนเริ่มงานรอบนี้ ตรวจสอบพบว่า index เดือน ก.ค. ใน `AF_GMV`/`AF_NET`/`AF_COM` (และ `AFI_GMV`/`AFI_NET`/`AFI_COMM` ใน Mobile) มีค่าผิดปกติ:

| | ค่าที่ถูกต้อง (ยืนยันแล้วจากรอบ 16 ก.ค. 03:05) | ค่าที่พบ (ผิด) |
|---|---|---|
| GMV | 728,951 | 728,841 |
| NET | 685,454 | 709,272 |
| COMMISSION | 83,768 | **8** ⚠️ (ตกหล่นอย่างชัดเจน) |

**สาเหตุ**: ตรวจสอบ `git log`/`git show` พบว่า commit `0efd225` ("auto: update 2026-07-16 18:23" — เป็น task อื่น ไม่ใช่ affiliate task) ได้เขียนทับค่าที่ถูกต้อง (จาก commit `a348f62` ของรอบ affiliate เช้าวันเดียวกัน ซึ่งยืนยันตรงกับ KPI card สดแล้ว) ด้วยค่าที่ไม่ได้ตรวจสอบและมีบั๊กชัดเจน (COMMISSION=8 ต่ำผิดปกติมาก) — เป็นรูปแบบเดียวกับบั๊ก cross-task ที่รายงานเมื่อ 16 ก.ค. (PRODUCTS cr/vid ถูกเขียนทับผิดจาก `dc8e0d9`) **เกิดซ้ำเป็นครั้งที่ 2**

**การแก้ไข**: คืนค่าที่ถูกต้อง (728,951 / 685,454 / 83,768) กลับเข้าไปทั้งใน `WIBWUB_Affiliate_Dashboard.html` และ `WIBWUB_Mobile.html` ไม่มีข้อมูลใหม่กว่านี้ให้ใช้ (TikTok export ของรอบนี้ล้มเหลว) จึง label เดือน ก.ค. ยังคงเป็น "(1-15)" เหมือนเดิม — ไม่ได้ขยับช่วงวันที่

ตรวจสอบ `PRODUCTS` array (cr/vid) ด้วย — รอบนี้ **ไม่พบการเขียนทับผิดซ้ำ** (ค่ายังถูกต้องตรงกับที่แก้ไว้เมื่อ 16 ก.ค.)

## หมายเหตุ / ประเด็นที่ควรแจ้งทีม

1. **บั๊ก cross-task เขียนทับข้อมูล affiliate เกิดซ้ำเป็นครั้งที่ 2** (ครั้งแรก: PRODUCTS cr/vid เมื่อ 16 ก.ค., ครั้งนี้: AF_GMV/NET/COM) — ยืนยันชัดเจนว่ามี scheduled task อื่น (ไม่ใช่ affiliate task) ที่เขียนทับไฟล์เดียวกันโดยใช้สูตร/ข้อมูลที่ไม่ผ่านการตรวจสอบ ควรตรวจสอบขอบเขตของ task นั้นอย่างเร่งด่วน มิฉะนั้นข้อมูลจะถูกเขียนทับผิดซ้ำเรื่อยๆ ทุกครั้งที่ task นั้นรัน
2. **TikTok Affiliate Creator List export ค้างสถานะ "กำลังส่งออก" นานผิดปกติ** (>10 นาที ไม่มี % ขยับ) — เกินกว่า worst-case ที่เคยเจอ (8 นาที) ควรลองใหม่รอบถัดไป
3. **`.git/index.lock` และไฟล์ temp อื่นๆ ใน `.git/objects/` ไม่สามารถลบได้จาก sandbox** (`Operation not permitted`) — ใช้วิธี `mv` แทน `rm` เพื่อเลี่ยง lock ค้าง (workaround ที่ใช้ได้ผลรอบนี้ แต่ควรหาสาเหตุราก เนื่องจากทำให้ commit ล้มเหลวได้ถ้าไม่ทำ workaround)
4. Top Products (Shipnity mapping) ยังคงข้ามเหมือนรอบก่อนหน้าหลายรอบ — ควรทำ mapping table หากต้องการเปิดใช้ step นี้
