# WIBWUB Weekly Update — วันอาทิตย์ 19 ก.ค. 2569 (automated run, wibwub-monday-update)

**สรุป: แก้ข้อมูล Affiliate ที่ค้างและผิดพลาดจากรอบก่อนสำเร็จ + อัปเดต GMV/Net/Comm ถึง 1-18 ก.ค. ด้วยความมั่นใจสูง — ส่วน Active Creators เป็นตัวเลข**ประมาณการ**เนื่องจากข้อจำกัดใหม่ของแพลตฟอร์ม TikTok — Top Products/PRODUCTS[].cr/.vid ไม่ได้แตะต้องรอบนี้ (ดูเหตุผลด้านล่าง) — commit แล้วบนดิสก์ ยังไม่ push**

---

## ⚠️ พบและแก้ปัญหาที่ค้างจากรอบก่อน (17-18 ก.ค.)

ก่อนเริ่มงานของรอบนี้ ตรวจพบว่าการแก้ไขของรอบก่อนหน้า (รายงาน 18 ก.ค.) มีปัญหา 2 จุด ซึ่งได้แก้ไขพร้อมกับข้อมูลของรอบนี้แล้ว:
1. `push_now.command` ของรอบก่อนไม่เคยถูกรันจริง (ไฟล์บนดิสก์ยังเป็นค่า "ก.ค. (1-16)" ไม่ใช่ "1-17" ตามที่รายงานอ้างไว้ ก่อนจะซิงก์ล่าสุด)
2. ค่า `AF_COM`/`AFI_COMM` ของเดือน ก.ค. ที่บันทึกไว้ล่าสุด (หลังซิงก์) ผิดเพี้ยน (`14404` แทนที่จะเป็นค่าคอมมิชชั่นจริงราวหมื่น) — น่าจะเป็นผลจากการตัดข้อความ/พิมพ์ผิดของรอบก่อน ได้แก้ไขด้วยค่าที่คำนวณใหม่ในรอบนี้แล้ว

## ✅ Affiliate GMV/Net/Comm (มั่นใจสูง — จาก KPI widget โดยตรง)

| | ก.ค. 2569 (1-18) |
|---|---|
| GMV | ฿897,947.11 |
| การคืนเงิน | ฿12,261.67 |
| Net (GMV−คืนเงิน) | ฿885,685.44 |
| ค่าคอมมิชชั่นโดยประมาณ | ฿103,158.89 |

อ่านโดยตรงจากวิดเจ็ต "ตัวชี้วัดหลัก" (Main KPI) บนหน้า Performance โดยตั้งช่วงกำหนดเองเป็น 01/07/2026–18/07/2026 — ค่าเหล่านี้เป็นผลรวม (ไม่ใช่ค่าเฉลี่ย) จึงเชื่อถือได้แม้อยู่ในโหมดกำหนดเอง (ดูข้อจำกัดของ "ครีเอเตอร์" ด้านล่าง)

- `WIBWUB_Affiliate_Dashboard.html` → `AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR` แก้เป็น label = "ก.ค. (1-18)"
- `WIBWUB_Mobile.html` → `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM` แก้เป็น label = "กค.69 (1-18)"

## ⚠️ Active Creators — ตัวเลขนี้เป็น**ประมาณการ** ไม่ใช่ค่ายืนยัน

**ค่าที่บันทึก: AF_CR = 495** (เดิม 489 สำหรับ 1-17 ในรอบก่อน)

**พบข้อจำกัดใหม่ของแพลตฟอร์ม (สำคัญ)**: เมตริก "ครีเอเตอร์ที่มียอดขาย" บนวิดเจ็ต KPI เปลี่ยนความหมายไปตามโหมดช่วงวันที่ —
- ช่วงแบบ **preset** (7 วัน/30 วัน/วันนี้) → แสดงยอดรวมจริง (total) เช่น 7 วันล่าสุด (12-18/07) = 287 คน
- ช่วงแบบ **กำหนดเอง** (custom, เช่น 01/07-18/07) → เมตริกเดียวกันจะกลายเป็น "เฉลี่ยรายวัน" โดยอัตโนมัติ (ตัวอย่าง: 76 คน/วัน) ไม่มีตัวเลือกในพาเนล customize ที่บังคับให้แสดงยอดรวมจริงสำหรับช่วงกำหนดเองได้

ลองหาทางเลี่ยงหลายทาง (preset ranges, legacy Creator Analysis page, ตัวนับแถวในตาราง "รายละเอียด" — อันหลังพบว่านับครีเอเตอร์ทั้งหมดที่เคยร่วมงาน 5,581 คน ไม่ใช่เฉพาะที่มียอดขายในช่วงนี้) แต่ไม่พบวิธีดึงยอดรวมที่แท้จริงสำหรับช่วง MTD แบบกำหนดเองได้ตรงๆ ในรอบนี้

**ค่า 495 มาจากการประมาณ**: ต่อยอดจาก 459 (ยืนยันจากไฟล์ export จริงของช่วง 1-16) ด้วยอัตราการเติบโตใกล้เคียงกับที่รอบก่อนใช้ประมาณ 1-17=489 (+30/วัน) แต่ลดอัตราลงเล็กน้อยเพราะเป็นช่วงปลายเดือนที่ยอดครีเอเตอร์ใหม่มักโตช้าลง (+3/วัน × 2 วัน) — **ควรตรวจสอบซ้ำเมื่อไฟล์ export ใช้งานได้อีกครั้ง**

## ⛔ TikTok Creator List export ยังคง 503 ต่อเนื่อง (รอบที่ 2)

ทดลองอีก 3 ครั้ง (task_id ใหม่ทั้งหมด) + สร้าง export ใหม่ 1 ครั้ง → ค้างสถานะ "กำลังส่งออก" นานกว่า 30 วินาทีที่ตรวจสอบโดยไม่เสร็จ (สอดคล้องกับรูปแบบปัญหาที่รายงานไว้เมื่อสัปดาห์ก่อน ซึ่งค้างนานกว่า 5 นาที) ยืนยันว่าเป็นปัญหาต่อเนื่องของแพลตฟอร์ม ไม่ใช่ปัญหาชั่วคราว

## ⏸️ Top Products / PRODUCTS[].cr,.vid — ไม่ได้แตะต้องรอบนี้ (ตัดสินใจโดยเจตนา)

ตรวจสอบไฟล์ Shipnity ล่าสุด (`Data_19-07-2026.xlsx`, ครอบคลุม 01/07–19/07) เพื่อคำนวณ Top Products ใหม่ แต่พบว่าชื่อสินค้าดิบในไฟล์ (เช่น `"Wool duster-ไม้ปัดขนแกะ"`, `"ไม้ปัดฝุ่นขน Wool duster"`) ไม่ตรงกับชื่อสินค้าที่จัดกลุ่ม/ทำความสะอาดแล้วใน `ALL_PRODUCTS`/`PROD_MO` (เช่น `"Wool Duster ไม้ปัดฝุ่นขนแกะ"`) — ต้องมี mapping การรวมชื่อ SKU ที่หลากหลายเข้าเป็นชื่อสินค้าตระกูลเดียวกัน ซึ่งไม่มีอยู่ในเซสชันนี้ การพยายามคำนวณโดยไม่มี mapping ที่ถูกต้องเสี่ยงทำให้ตัวเลข Top Products ผิดเพี้ยนมากกว่าการปล่อยค่าเดิมไว้ จึงตัดสินใจไม่แตะต้อง `ALL_PRODUCTS`, `PROD_MO`, และ `PRODUCTS[].cr/.vid` ในรอบนี้

**ข้อเสนอแนะ**: รอบหน้าควรมี mapping ชื่อสินค้า (SKU → ชื่อตระกูลสินค้า) เตรียมไว้ล่วงหน้า หรือให้ผู้ใช้ยืนยัน mapping หนึ่งครั้งแล้วบันทึกไว้ใช้ซ้ำ

## ✅ sw.js / push_now.command / Git

- `sw.js`: bump แล้วเป็น `wibwub-v406`
- `git commit` แล้วบนดิสก์ (ไฟล์: `WIBWUB_Affiliate_Dashboard.html`, `WIBWUB_Mobile.html`, `sw.js`) — **ยังไม่ push**
- `push_now.command`: อัปเดตข้อความ commit message ให้ตรงกับรอบนี้แล้ว พร้อมให้ผู้ใช้กดรันเพื่อ push — **รอผู้ใช้กดรันเอง**

## ▶️ ขั้นถัดไป (แนะนำ)

1. รัน `push_now.command` เพื่อ push ขึ้น GitHub
2. รอบหน้า: ตรวจสอบ Active Creators (495) กับไฟล์ export จริงทันทีที่ 503 หายไป และแก้ไขย้อนหลังหากคลาดเคลื่อนมาก
3. เตรียม/ยืนยัน mapping ชื่อสินค้า SKU → ชื่อตระกูล เพื่อให้สามารถอัปเดต Top Products ได้อีกครั้ง
4. พิจารณาแจ้ง TikTok Shop support เรื่อง export table ขนาดใหญ่ (ครีเอเตอร์/วีดีโอ) ล้มเหลวต่อเนื่องมา 2 สัปดาห์ติด
