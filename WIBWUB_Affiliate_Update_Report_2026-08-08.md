# รายงานการอัปเดตข้อมูล Affiliate (WIBWUB) — 8 ส.ค. 2026

## สถานะ: ❌ ไม่สำเร็จ — ไม่สามารถดาวน์โหลดไฟล์ได้เนื่องจากปัญหาฝั่ง TikTok

## ช่วงวันที่ที่พยายามดึงข้อมูล
1–6 สิงหาคม 2026 (GMT+7), ตั้งค่า custom date range ใน Performance page ของ TikTok Affiliate Center สำเร็จ (ยืนยันจากตัวเลข GMV บนหน้าเว็บที่เปลี่ยนตามช่วงวันที่ที่เลือก)

## สิ่งที่ทำสำเร็จ
- เข้าสู่ TikTok Affiliate Center → หน้า "ผลการดำเนินงาน" (Performance, ซึ่งเป็นหน้าใหม่แทนที่ "การวิเคราะห์ธุรกรรม" เดิม) สำเร็จ
- ตั้งค่าช่วงวันที่ custom 01/08/2026–06/08/2026 สำเร็จ
- กดปุ่ม "ส่งออก" (Export) สำหรับทั้ง 4 แท็บ (ครีเอเตอร์ / สินค้า / วิดีโอ / ไลฟ์สตรีม) — ระบบแสดงข้อความ "ระบบเตรียมข้อมูลสำเร็จแล้ว" ยืนยันว่า export งานสร้างไฟล์สำเร็จในฝั่ง TikTok

## จุดที่ติดปัญหา (Root cause)
เมื่อกดปุ่ม "ดาวน์โหลด" ในแผงรายงาน (บันทึกการดาวน์โหลดไฟล์) เพื่อดึงไฟล์ที่ export ไว้ — **ระบบ TikTok ตอบกลับ error `503 Service Unavailable` ทุกครั้ง** ที่ endpoint:
```
GET /api/v1/oec/affiliate/compass/export_task/export?...task_id=...
```

ยืนยันแล้วว่าไม่ใช่ปัญหาฝั่งเรา เพราะ:
- ทดสอบกับ task_id ที่ต่างกันอย่างน้อย 3 รายการ (ทั้งรายงานเก่าและรายงานที่เพิ่ง export ใหม่ภายในไม่กี่วินาที) → ได้ 503 ทุกครั้ง
- ทดสอบทั้งแท็บ ครีเอเตอร์ และ สินค้า → ได้ 503 เหมือนกัน
- รอและลองใหม่หลายรอบ (ห่างกัน ~10 วินาที ต่อเนื่องกว่า 10 นาที) → ยัง 503 เหมือนเดิม ไม่ฟื้นตัว
- ไม่มีไฟล์ .xlsx ใหม่ที่ตรงกับ 4 รายงานเป้าหมายปรากฏในโฟลเดอร์ Downloads เลย (ตรวจสอบซ้ำหลายครั้ง)

มีไฟล์เดียวที่ดาวน์โหลดลงมาได้ (`f3ad954426b7e540c092bf750d3981dd.xlsx`) แต่เมื่อเปิดตรวจสอบพบว่าเป็นรายงานคนละแบบ (มีคอลัมน์เดียวคือ `orderItemId` — เป็น order-item level export ไม่ตรงกับ 4 รายงานที่ต้องการ) จึง**ไม่ได้นำมาใช้และไม่ได้ย้ายไฟล์นี้ไปไหน**

## การดำเนินการที่ "งด" ทำ (ตามกฎ safety ของ task)
เนื่องจากไม่มีไฟล์ต้นทางที่ถูกต้องแม้แต่ไฟล์เดียว จึง **ไม่ได้แตะต้อง** ไฟล์ต่อไปนี้เลย (ป้องกันข้อมูลเสียหาย):
- `WIBWUB_Affiliate_Dashboard.html` (AF_GMV / AF_NET / AF_COM / AF_CR / PRODUCTS.cr,vid / VIDEOS)
- `WIBWUB_Mobile.html` (AFI_GMV / AFI_NET / AFI_COMM)
- `sw.js` (ไม่ได้ bump เวอร์ชัน cache)
- ไม่ได้สร้าง `push_now.command` เพราะไม่มีการเปลี่ยนแปลงใดๆ ให้ push

โฟลเดอร์ `Data Affiliate/{ครีเอเตอร์,สินค้า,วีดีโอ,ไลฟ์สตรีม}/` ยังคงว่างเปล่าเหมือนเดิม ไม่มีไฟล์ใหม่ถูกย้ายเข้าไป

## สรุปตัวเลข
ไม่มี — เนื่องจากไม่มีข้อมูลใหม่ที่ประมวลผลได้ (GMV/Net/Commission/จำนวนครีเอเตอร์ที่ active, cr/vid ของสินค้า, และ VIDEOS entries **ไม่มีการเปลี่ยนแปลงจากค่าเดิมในแดชบอร์ด**)

## ข้อเสนอแนะ
1. ลองรันงานนี้ใหม่อีกครั้งในภายหลัง (เช่น อีก 30–60 นาที หรือวันถัดไป) เผื่อเป็นปัญหาชั่วคราวฝั่ง TikTok
2. หากยังเจอ 503 ซ้ำ แนะนำให้ตรวจสอบด้วยตนเองผ่านเบราว์เซอร์ปกติว่าปุ่ม "ดาวน์โหลด" ในหน้า TikTok Affiliate Center ใช้งานได้หรือไม่ (อาจเป็นปัญหาที่ TikTok เอง หรือ rate limit ของบัญชี)
3. ไม่ต้องดับเบิลคลิก `push_now.command` เพราะไม่มีการเปลี่ยนแปลงไฟล์ใดๆ ในรอบนี้ให้ commit/push

---
*รายงานสร้างโดยระบบอัตโนมัติ (scheduled task: wibwub-thursday-affiliate) — ไม่มีการแก้ไขไฟล์ dashboard ใดๆ ในรอบนี้*
