# WIBWUB Weekly Update — รายงาน (จันทร์ 25 มิ.ย. 2569)

รันอัตโนมัติ (scheduled task `wibwub-monday-update`) — ไม่มีผู้ใช้อยู่

## สรุปผล
ดาวน์โหลดข้อมูลใหม่ทั้ง 2 แหล่งเรียบร้อย ตรวจสอบแล้ว **ตัวเลขที่แสดงบน dashboard ไม่เปลี่ยน** จึง **ไม่แก้ไฟล์ HTML และไม่ bump sw.js/commit** ในรอบนี้ (ทำตาม precedent ของ 2 รอบก่อน)

## STEP 1 — Shipnity (เสร็จ)
- ดาวน์โหลด purchase-history เดือน มิ.ย. (1–25 มิ.ย.) ส่งออก xlsx สำเร็จ
- บันทึกที่ `Data Shipnity/Data_มิถุนายน.xlsx` (19,279 แถว, ช่วงวันที่ 01/06–25/06/2026)
- ยอดขายรวม มิ.ย. (dedup): **฿6,210,127**

## STEP 2 — TikTok Affiliate (เสร็จ)
- ดาวน์โหลด Transaction Analysis creator list (1–23 มิ.ย. = วันล่าสุดที่ระบบ finalize) สำเร็จ
- บันทึกที่ `Data Affiliate/Transaction_Analysis_Creator_List_20260601-20260623_062508.xlsx`
- ตัวเลขรวม (creator GMV>1000 ตาม spec):
  - **GMV ฿436,626 · Net ฿429,316 · Commission ฿54,274**
  - creators ที่มี GMV>0 = 308 ราย (มี GMV>1,000 = 66 ราย)

## STEP 3 — Top Products (ตรวจแล้ว — ไม่แก้)
- เทียบ ALL_PRODUCTS (ยอดสะสม) ปัจจุบันกับข้อมูลใหม่: ต่างกัน **< 1%** (เช่น Wool Duster ฿5,067,689 → ฿5,099,839 = +0.6%) — มองไม่เห็นความต่างบนหน้าจอ
- ค่า PROD_MO เดือน มิ.ย. เก่ากว่าจริง ~10% สำหรับบางตัว **แต่** การ map รหัส SKU ดิบ (เช่น `Sugar (Sugar-500ml+Spray)`) เข้าชื่อที่ curate ไว้ (`Sugar สเปรย์แวกซ์เคลือบสีรถ`) ไม่มีตาราง mapping — เขียนทับเสี่ยงทำชื่อสินค้าเสีย
- **ตัดสินใจ: คงค่าเดิม** ตาม precedent รอบ 23–24 มิ.ย. (delta เล็ก + เสี่ยงต่อชื่อที่ curate)

## STEP 4 — Affiliate Arrays (ตรวจแล้ว — ไม่แก้)
- ค่าใน `WIBWUB_Mobile.html` ที่ index 7 อยู่แล้ว: `AFI_GMV=436626 / AFI_NET=429316 / AFI_COMM=54274`
- ตรงกับไฟล์ใหม่เป๊ะ (ช่วง 1–23 มิ.ย. ยังเหมือนเดิมเพราะ 24+ มิ.ย. ยังไม่ finalize) → **ไม่ต้องแก้**

## STEP 5 — sw.js / commit (ไม่จำเป็น)
- ไม่มีการแก้เนื้อหา HTML → ไม่ต้อง bump cache (ปัจจุบัน `wibwub-v238`) และไม่ต้อง commit/push

## หมายเหตุ / การตัดสินใจอัตโนมัติ
1. ตัวเลข affiliate รอบนี้เท่ารอบก่อนทุกประการ (ระบบ TikTok ยัง finalize ถึง 23 มิ.ย.)
2. ข้อมูล Shipnity refresh ถึง 25 มิ.ย. แล้ว แต่ผลต่อยอดสะสมที่แสดง < 1%
3. หลีกเลี่ยงการเขียนทับ Top Products เพื่อกันชื่อสินค้าที่ curate เสีย (ไม่มี SKU→name map)
4. หากต้องการให้ refresh PROD_MO เดือน มิ.ย. ให้ตรงเป๊ะ ต้องสร้างตาราง mapping SKU→ชื่อสินค้าก่อน (งานครั้งเดียว) — แนะนำทำตอนมีผู้ใช้ยืนยันชื่อ

**ไฟล์ข้อมูลใหม่พร้อมใช้ในรอบถัดไป — ไม่มีการแก้ dashboard ในรอบนี้**
