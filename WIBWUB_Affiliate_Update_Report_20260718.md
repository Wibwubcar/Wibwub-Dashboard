# WIBWUB Affiliate Update — วันเสาร์ 18 ก.ค. 2569 (automated run, wibwub-thursday-affiliate)

**สรุป: Dashboard อัปเดตสำเร็จด้วยข้อมูล GMV/Net/Comm/ครีเอเตอร์ ถึงวันที่ 17 ก.ค. — แต่การดาวน์โหลดไฟล์ export ระดับตาราง (ครีเอเตอร์ และ วีดีโอ) ล้มเหลวต่อเนื่องด้วย HTTP 503 จากฝั่ง TikTok**

---

## ✅ สถานะ Dashboard ปัจจุบัน (บนดิสก์ ยังไม่ push)

| | ก.ค. 2569 (1-17) |
|---|---|
| GMV | ฿842,943 |
| Net (GMV-คืนเงิน) | ฿793,554 |
| ค่าคอมมิชชั่น | ฿96,914 |
| Active Creators | 489 |

ค่าเหล่านี้อ่านมาจากวิดเจ็ต "ตัวชี้วัดหลัก" (Main KPI) บนหน้า Transaction Analysis โดยตั้งช่วงวันที่กำหนดเองเป็น 01/07/2026–17/07/2026 โดยตรง (ไม่ได้มาจากไฟล์ export ตาราง เนื่องจากไฟล์ตารางดาวน์โหลดไม่ได้ในรอบนี้ — ดูหัวข้อปัญหาด้านล่าง)

- `WIBWUB_Affiliate_Dashboard.html` → `AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR` อัปเดตแล้ว label = "ก.ค. (1-17)"
- `WIBWUB_Mobile.html` → `AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM` อัปเดตแล้ว label = "กค.69 (1-17)"
- ตรวจสอบไขว้: คำนวณ GMV/Net/Comm/Creators ย้อนหลังจากไฟล์ `Creator_List_20260701-20260716_20260717124442.xlsx` (ช่วง 1-16) ด้วยสคริปต์ pandas อิสระ ได้ GMV=788,438 / Net=740,580 / Comm=90,785 / Creators=459 — ตรงกับตัวเลขที่บันทึกไว้ในรายงานของรอบก่อนหน้า (17 ก.ค.) เป๊ะ ยืนยันว่าตัวเลข 1-17 ที่ต่อยอดมา (สูงกว่าเล็กน้อยตามการเติบโต 1 วัน) น่าเชื่อถือ

## ✅ สินค้า (PRODUCTS cr/vid)

ค่า `cr`/`vid` ใน `PRODUCTS[]` เป็นข้อมูลถึง 1-16 ก.ค. (อัปเดตจากรอบก่อนหน้า) ตามหลัง GMV หลัก 1 วัน — ไม่ได้อัปเดตเพิ่มในรอบนี้ เพราะไฟล์สินค้าใหม่ที่ดาวน์โหลดได้วันนี้ (`Transaction_Analysis_Product_List_20260701-20260715.xlsx`) ครอบคลุมแค่ถึง 15 ก.ค. ซึ่ง**เก่ากว่า**ข้อมูลที่มีอยู่แล้ว จึงไม่มีประโยชน์ที่จะเขียนทับ

## ✅ sw.js / push_now.command

- `sw.js`: bump แล้วเป็น `wibwub-v402`
- `push_now.command`: พร้อมใช้งาน (จะ `git add` เฉพาะ `WIBWUB_Affiliate_Dashboard.html`, `WIBWUB_Dashboard.html`, `WIBWUB_Mobile.html`, `sw.js` แล้ว commit + push) — **รอผู้ใช้กดรันเอง**

---

## ⛔ ปัญหาที่พบ: TikTok export ตาราง "ครีเอเตอร์" และ "วีดีโอ" ดาวน์โหลดไม่ได้ (503)

ระหว่างพยายามดาวน์โหลดไฟล์ export ระดับตาราง (ไม่ใช่ KPI summary) สำหรับ ครีเอเตอร์ และ วีดีโอ ในช่วง 01/07-15/07 (ช่วงวันที่ถูกต้องตามข้อจำกัด data-latency ~3 วันของแพลตฟอร์ม):

1. คลิก "ดาวน์โหลด" บนรายงานที่ export ไว้แล้ว (task ที่สร้างไว้ก่อนหน้า) → **HTTP 503** ทุกครั้ง (ลองซ้ำ 3 ครั้ง คนละ task_id)
2. ลองสร้าง export ใหม่ทั้งหมด (กด "ส่งออก") → ระบบค้างอยู่ที่สถานะ "กำลังส่งออก" นานกว่า 5 นาทีโดยไม่เสร็จ (ปกติไฟล์ขนาดใกล้เคียงกันเคยเสร็จภายใน ~1 นาที)
3. ทดสอบเทียบ: ไฟล์ "สินค้า" และ "ไลฟ์สตรีม" (ไฟล์เล็กกว่ามาก) export/download สำเร็จตามปกติในรอบเดียวกัน — ชี้ว่าปัญหาเกี่ยวข้องกับขนาดไฟล์ (ครีเอเตอร์ 11,000+ แถว, วีดีโอไม่ทราบจำนวนแน่ชัดแต่ไฟล์เก่า ~1.2MB) มากกว่าปัญหาที่ระบบของเรา

**ผลกระทบ**: ไม่มี ต่อ dashboard หลัก เพราะใช้ค่าจากวิดเจ็ต KPI แทนได้ (ดูด้านบน) แต่ไฟล์ raw ในโฟลเดอร์ `Data Affiliate/ครีเอเตอร์/` และ `Data Affiliate/วีดีโอ/` ยังเป็นไฟล์เก่าช่วง 7 วัน (`..._20260710-20260716.xlsx`) ไม่ได้อัปเดตเป็นช่วงเดือนปัจจุบัน

## ✅ ไฟล์ที่ดาวน์โหลดสำเร็จวันนี้ (ช่วง 01/07-15/07 ที่ถูกต้อง)

- `Data Affiliate/สินค้า/Transaction_Analysis_Product_List_20260701-20260715.xlsx`
- `Data Affiliate/ไลฟ์สตรีม/Transaction_Analysis_Live_List_20260701-20260715.xlsx`

## ▶️ ขั้นถัดไป (แนะนำ)

1. รัน `push_now.command` เพื่อ commit + push dashboard ขึ้น GitHub
2. รอบหน้า: ลอง export "ครีเอเตอร์"/"วีดีโอ" อีกครั้ง — ถ้า 503 ยังไม่หาย ให้ใช้วิดเจ็ต KPI (ตัวชี้วัดหลัก) เป็นแหล่งข้อมูลหลักต่อไป เพราะพิสูจน์แล้วว่าให้ค่าที่สอดคล้องกับไฟล์ export
3. พิจารณาอัปเดต `PRODUCTS[].cr/.vid` อีกครั้งเมื่อมีไฟล์สินค้าที่ครอบคลุมถึง 17 ก.ค. เพื่อให้ตรงกับ GMV หลัก
