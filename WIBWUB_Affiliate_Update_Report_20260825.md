# รายงานซิงก์ "คำขอสินค้า" — 25 ส.ค. 2569

ดึงข้อมูลสดจากชีต "Top 30 Affiliate" แท็บ "ขอสินค้า" ผ่าน Chrome (CSV 269,223 ตัวอักษร, 1,443 แถวข้อมูล) หัวคอลัมน์ตรงกับที่คาดไว้ทุกช่อง นับคำขอต่อสินค้าจากคอลัมน์ 4 "สินค้าที่ Creator ขอ" ตามเดิม (ไม่ใช้คอลัมน์ 5 "สินค้าที่ให้")

## เดือนปัจจุบัน (ส.ค. 2569, 1–24 ส.ค.)

คำขอทั้งหมด 261 รายการ อนุมัติ 123 ปฏิเสธ 138 — ตัวเลขเท่ากับที่มีอยู่ในแดชบอร์ดอยู่แล้ว เพราะวันที่ล่าสุดในชีตยังเป็น 24/08/2026 (ทีมยังไม่ได้บันทึกแถวของวันที่ 25 ส.ค.) จึงไม่มีการแก้ `SAMPLE_MONTHS`

สินค้าที่ถูกขอมากสุดเดือนนี้: Refresh Wipes 129 ครั้ง (อนุมัติ 69 / ปฏิเสธ 60), Interior wipes 90 (42/48), Interior 29 (11/18), Reflex 23 (9/14), Sugar 10 (5/5)

## 7 วันล่าสุด — อัปเดตแล้ว

หน้าต่างเลื่อนเป็น 19–25 ส.ค. 2569 (เดิม 18–24 ส.ค.) รวม 79 คำขอ ลดจาก 90 เพราะวันที่ 18 ส.ค. (11 คำขอ) หลุดออกจากหน้าต่างและยังไม่มีข้อมูลวันที่ 25 เข้ามาแทน

รายละเอียด: Refresh Wipes 45 (22/23), Interior wipes 25 (10/15), Interior 7 (2/5), Reflex 6 (2/4), Sugar 1 (0/1)

## สิ่งที่ต้องดู

มี 30 แถวในชีตที่ไม่มีวันที่ในคอลัมน์ Date (NO. 55–84, สถานะ "On Process") แถวเหล่านี้จัดเข้าเดือนไม่ได้จึงไม่ถูกนับใน bucket ใดเลย ถ้าอยากให้เข้าสถิติต้องให้ทีมเติมวันที่ย้อนหลัง

ตอนเริ่มงานพบว่าไฟล์ `WIBWUB_Affiliate_Dashboard.html` ในเครื่องมีการแก้ค้างอยู่แล้วจากงานอัตโนมัติตัวอื่น (ส่วน `VIDEOS` — เรียงลำดับใหม่ ~107 บรรทัด และมี caption หนึ่งอันที่ backslash escape ถูกเพิ่มซ้ำจนกลายเป็น `\\\\\\\\ud83d`) ส่วนนี้ไม่เกี่ยวกับงานนี้ จึงคอมมิตเฉพาะบรรทัดของ `SAMPLE_PRODUCTS_LAST7D` เท่านั้น (stage แบบ patch เจาะจง) และปล่อยการแก้ค้างของงานอื่นไว้ใน working tree ตามเดิม แนะนำให้ตรวจ escape ตัวนั้นใน `wibwub_update.py` ด้วย

## สถานะ

ตรวจ syntax ด้วย `node --check` ผ่าน / คอมมิต `78c7650` แตะเฉพาะ 3 บรรทัดใน `SAMPLE_PRODUCTS_LAST7D` + `sw.js` / bump cache `wibwub-v816` → `wibwub-v817` / สำรองไฟล์เดิมไว้ที่ `WIBWUB_Affiliate_Dashboard.html.bak_20260825_run`

**ดับเบิลคลิก `push_now.command` เพื่อ deploy ขึ้นเว็บ**
