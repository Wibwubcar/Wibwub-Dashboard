# WIBWUB Auto-Update — 19 ส.ค. 2569 (รอบบ่าย)

Task: `wibwub-monday-update` · รันอัตโนมัติ ไม่มีผู้ใช้อยู่ในระหว่างรัน

## สรุปผล

| ขั้นตอน | สถานะ |
|---|---|
| Protection check (M5) | ผ่าน — M5 = 8 labels ทั้ง Dashboard และ Mobile ตรงกับเดือนปัจจุบัน (ส.ค. = 8) ไม่ต้องแก้ |
| STEP 1 — Shipnity export | สำเร็จ — `Data Shipnity/Data_19-08-2026.xlsx` (19.9 MB) และคัดลอกเป็น `Data_สิงหาคม.xlsx` |
| STEP 2 — TikTok Affiliate export | สำเร็จ (ผ่านหน้าใหม่ `/data/creator-analysis`) — `Creator_List_20260801-20260818_*.xlsx` |
| STEP 3 — Top Products / ภาพรวมธุรกิจ | สำเร็จ |
| STEP 4 — Affiliate arrays | สำเร็จ |
| STEP 5 — sw.js + git commit | สำเร็จ (commit `e6d6390`) |
| Verification | 22/22 ผ่าน |

## ตัวเลขที่อัปเดต

### Affiliate — ส.ค. (1-18)

| ค่า | เดิม ส.ค. (1-17) | ใหม่ ส.ค. (1-18) |
|---|---|---|
| GMV | 902,978 | **960,112** |
| Net GMV | 884,466 | **904,149** |
| Commission | 106,107 | **112,928** |
| Creators | 491 | **515** |

อัปเดตที่ index สุดท้ายของทุก array (`AF_*[7]`, `AFI_*[9]`) ตามกฎ rolling-window — เดือนก่อนหน้าไม่ถูกแตะต้อง (ตรวจสอบแล้ว)

### Top Products — ส.ค. (1-19) จาก Shipnity

อัปเดตเฉพาะ index 7 (ส.ค.) ของ `PROD_MO` และคำนวณ `ALL_PRODUCTS.v` ใหม่เป็นผลรวมของ `mo` — ม.ค.–ก.ค. คงเดิมทั้งหมด

5 อันดับแรก (ยอดสะสม ม.ค.–19 ส.ค.): Wool Duster ฿5.85M · Sugar 500ml ฿5.21M · Interior 500ml ฿4.05M · Refresh Wipes ฿2.64M · Interior Wipes ฿2.61M

### Dashboard — ตาราง/กราฟ แยกช่องทาง

คำนวณใหม่ทั้งหมดจากไฟล์ Shipnity รายเดือน 8 ไฟล์ (ใช้คอลัมน์ 15 = ช่องทางติดต่อ):
ยอดรวม **฿61,329,199** · **241,162 ชิ้น** (เดิม ฿60.48M / 237,029 — ข้อมูลถึง 14–17 ส.ค.)

สัดส่วนช่องทางทั้งหมด: Shopee 59% · TikTok 18% · Facebook 14% · Line Shopping 5% · Lazada 1.3%

---

## เรื่องที่ต้องรู้ / ข้อควรระวัง

### 1. หน้า Transaction Analysis ของ TikTok ถูกปิดถาวรแล้ว

`/insights/transaction-analysis` และ `/data/transaction-analysis` redirect ไปหน้า Performance เปล่า
ไฟล์ format เดิม 22 คอลัมน์ (`Transaction_Analysis_Creator_List_*.xlsx`) **ดึงใหม่ไม่ได้อีกแล้ว**

ตัวแทนคือ export จากเมนู "ครีเอเตอร์" → `/data/creator-analysis` ได้ไฟล์ `Creator_List_*.xlsx` **24 คอลัมน์**
mapping ที่ยืนยันแล้ว: col 1 = GMV · col 7 = ค่าคอมมิชชั่น · col 21 = GMV ของการคืนเงิน · col 23 = ผู้ติดตาม

> **ควรแก้ SKILL.md:** เปลี่ยนแหล่งข้อมูล affiliate จาก Transaction Analysis เป็น Creator Analysis พร้อม mapping ใหม่

### 2. เอกสาร column mapping ใน skill ผิด

Skill ระบุว่าไฟล์ affiliate มี 12 คอลัมน์ โดย col[2] = การคืนเงิน และ col[10] = ค่าคอมมิชชั่น
ถ้าใช้ตามนั้นจะได้ RET = 63,301 และ COMM = **0** ซึ่งผิดทั้งคู่
ค่าที่ถูกต้องของไฟล์ 22 คอลัมน์คือ col[1] / col[4] / col[21] (ตรวจสอบแล้วว่าให้ผลลัพธ์ตรงกับค่าที่อยู่ใน HTML เป๊ะ)

### 3. นิยาม "การคืนเงิน" เปลี่ยน ทำให้ Net GMV ไม่ต่อเนื่อง

- ไฟล์เดิม 22 คอลัมน์ "การคืนเงิน" ส.ค. 1-17 = **18,512**
- ไฟล์ใหม่ 24 คอลัมน์ "GMV ของการคืนเงินจากแอฟฟิลิเอต" ส.ค. 1-18 = **55,963**

เพิ่มขึ้น 3 เท่าจากข้อมูลเพิ่มแค่วันเดียว = สองไฟล์นิยาม refund ต่างกัน
ตั้งแต่รอบนี้เป็นต้นไป Net GMV จะอิงนิยามใหม่ ดังนั้น **ห้ามเทียบ Net ของ ส.ค. กับเดือนก่อนหน้าตรงๆ**

### 4. ยอด Top Products บางตัวลดลงเทียบกับรอบเช้า

เช่น Sugar 500ml ส.ค. 539,583 → 524,529 · Spot 500ml 293,773 → 284,031
เกิดจาก Shipnity export สะท้อนสถานะออเดอร์ปัจจุบัน — ออเดอร์ที่ถูกยกเลิก/แก้ไขจะหายไปจากไฟล์
ไม่ใช่ bug แต่เป็นธรรมชาติของข้อมูล

### 5. แก้ bug ที่พบระหว่างทาง (ยังไม่ commit)

กราฟ `soc_follow` ใน Dashboard มี `pointRadius` ยาว 7 แต่ M5 มี 8 labels (ตรงกับ "ผิดพลาด #5" ใน skill)
แก้เป็น 8 แล้ว แต่ **commit ไม่ผ่านเพราะ sandbox ลบ `.git/index.lock` ไม่ได้** — เพิ่มคำสั่ง commit ไว้ใน `push_now.command` แล้ว

ยังพบด้วยว่า Facebook/Instagram ใน `soc_follow` มีข้อมูลแค่ 7 เดือน (ขาด ส.ค.) — **ไม่ได้เติมค่าเอง** เพราะจะเป็นการกุตัวเลข ต้องดึงจาก Google Sheet social รอบหน้า

### 6. ไฟล์ Shipnity: ห้ามเชื่อชื่อไฟล์ (ยืนยันซ้ำ)

`Data_30-06-2026.xlsx` = ข้อมูล **กุมภาพันธ์** · `Data_02-06-2026.xlsx` = ข้อมูล **พฤษภาคม**
ทุกไฟล์เป็น export แบบสะสมภายในเดือนเดียว ต้องอ่าน col[19] (วันที่สร้าง) เสมอ
ไฟล์ที่ใช้รอบนี้: มกราคม / กุมภาพันธ์ / มีนา / เมษา / **02-06-2026 (พ.ค.)** / มิถุนายน / กรกฎาคม / สิงหาคม

---

## ต้องทำต่อ

**ดับเบิลคลิก `push_now.command`** เพื่อ commit ที่ค้าง + push ขึ้น GitHub
(sandbox push ไม่ได้ — HTTP 403 ผ่าน proxy ตามที่ skill ระบุไว้)
