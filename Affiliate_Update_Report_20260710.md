# WIBWUB Affiliate Auto-Update Report — 10 ก.ค. 2026

## สรุปสิ่งที่อัปเดต

### 1. ดึงข้อมูลจาก TikTok Affiliate Center
หน้า Transaction Analysis แบบเดิม (4 แท็บในหน้าเดียว) ถูก TikTok เปลี่ยนโครงสร้างใหม่ระหว่างทาง ตอนนี้ต้องเข้าผ่านหน้าแยกหมวด (การวิเคราะห์ → ครีเอเตอร์ / สินค้า) แทน และมีปัญหา **HTTP 503 (Service Unavailable)** เกิดขึ้นซ้ำๆ บน endpoint ดาวน์โหลดไฟล์ของ TikTok เอง (`/api/v1/insights/export/file/...`) ทำให้ปุ่ม "ดาวน์โหลด" กดแล้วไม่มีไฟล์ตกลงมาหลายครั้งติดกัน แม้ระบบจะขึ้นข้อความ "เตรียมข้อมูลสำเร็จแล้ว" ก็ตาม

ผลลัพธ์การดึงข้อมูล 4 หมวด (ช่วงวันที่ 1–9 ก.ค. 2026):
- **ครีเอเตอร์**: ✅ สำเร็จ — `Creator_List_20260701-20260709_20260710025022.xlsx` (8,399 แถว ครีเอเตอร์ทั้งหมด)
- **สินค้า**: ✅ สำเร็จ — `ListProducts_2026-07-01-2026-07-09_ALLPlan_20260710024310.xlsx` (69 SKU)
- **วีดีโอ**: ❌ ไม่พบ — หน้า/ปุ่ม export แยกสำหรับ "วีดีโอ" ไม่มีอยู่แล้วในโครงสร้าง UI ใหม่ (ข้อมูลล่าสุดที่มีอยู่ยังเป็นช่วง 30 มิ.ย.–6 ก.ค.)
- **ไลฟ์สตรีม**: ❌ ไม่พบ — เช่นเดียวกับวีดีโอ ไม่มีหน้า/ปุ่ม export แยกในโครงสร้างใหม่ (ข้อมูลล่าสุดยังเป็นช่วง 30 มิ.ย.–6 ก.ค.)

ไฟล์ที่ดาวน์โหลดสำเร็จถูกย้ายเข้าโฟลเดอร์ `Data Affiliate/{หมวด}/` เรียบร้อยแล้ว (ผ่าน LaunchAgent อัตโนมัติ)

### 2. อัปเดตข้อมูลครีเอเตอร์ (ก.ค. 1-9)
คำนวณจากไฟล์ครีเอเตอร์ (สูตร: GMV = ผลรวม "GMV จากแอฟฟิลิเอต", Net = GMV − ผลรวม "GMV ของการคืนเงินจากแอฟฟิลิเอต", ค่าคอม = ผลรวม "ค่าคอมมิชชั่นโดยประมาณ", จำนวนครีเอเตอร์ = แถวที่มี GMV > 0) — ตัวเลขตรงกับการ์ด KPI บนหน้าเว็บเป๊ะทุกค่า (413,170.40 / 23,102.39 / 47,190.87):

- GMV รวม: ฿413,170
- Net GMV: ฿390,068
- ค่าคอมมิชชั่น: ฿47,191
- จำนวนครีเอเตอร์ที่มียอดขาย: 299 คน

อัปเดต index สุดท้ายของเดือน ก.ค. (rolling window, overwrite เพราะ label "ก.ค." เป็นตัวสุดท้ายอยู่แล้ว, เปลี่ยน label จาก "(1-8)" → "(1-9)") ใน:
- `WIBWUB_Affiliate_Dashboard.html`: AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR
- `WIBWUB_Mobile.html`: AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM

### 3. สินค้า (PRODUCTS cr/vid)
ไฟล์สินค้าใหม่ (ListProducts) เป็นฟอร์แมตต่างจากเดิม — คอลัมน์ "ยอดขายจากครีเอเตอร์" = จำนวนครีเอเตอร์ (cr), "วิดีโอ" = จำนวนวิดีโอ (vid) ตรวจสอบเทียบกับค่าปัจจุบันแล้วพบว่าตัวเลขเพิ่มขึ้นสอดคล้อง trend เดิมทุกสินค้า (ไม่มีความผิดปกติแบบที่เจอในรอบ 8 ก.ค.) จึงอัปเดต cr/vid ให้ครบทั้ง 7 SKU:

| สินค้า | cr เดิม→ใหม่ | vid เดิม→ใหม่ |
|---|---|---|
| Leather Wipes | 82→100 | 31→41 |
| Interior Wipes | 65→83 | 26→32 |
| Sugar | 42→52 | 28→44 |
| Cleaner | 28→34 | 6→9 |
| Interior | 20→26 | 18→30 |
| Refresh | 17→24 | 7→8 |
| Visible | 6→7 | 1→1 |

(field อื่น เช่น gmv/units/monthly/ret ไม่ถูกแตะตามที่ระบุในสโคป)

### 4. Cache version
Bump `sw.js`: wibwub-v357 → **wibwub-v358**

### 5. Push script
`push_now.command` เดิมมีแค่ `git push` ไม่มี add/commit — แก้ให้ทำ `git add -A` + `git commit` + `git push` ครบในตัว (sandbox เขียน `.git/index.lock` บน Google Drive path ไม่ได้ ต้องรันสคริปต์นี้บนเครื่อง Mac จริง)

## Action ที่ต้องทำต่อ (สำหรับผู้ใช้)
1. ดับเบิลคลิก `push_now.command` ในโฟลเดอร์ All เพื่อ commit + push ขึ้น git
2. TikTok Affiliate Center ไม่มีหน้า export แยกสำหรับ "วีดีโอ" และ "ไลฟ์สตรีม" อีกต่อไปในโครงสร้าง UI ใหม่ — ข้อมูล 2 หมวดนี้ค้างอยู่ที่ช่วง 30 มิ.ย.–6 ก.ค. ต้องหาช่องทาง export ใหม่ (อาจอยู่ในหน้า "ผลการดำเนินงาน" ที่ TikTok กำลังย้ายไปใช้) ก่อนจะอัปเดตต่อได้
3. หากยังเจอ HTTP 503 ตอนดาวน์โหลด export ในรอบถัดไป ให้ลองรอนานขึ้น (>3 นาที) หรือรีเฟรชหน้าใหม่ก่อนกดดาวน์โหลดซ้ำ — เป็นปัญหาฝั่งเซิร์ฟเวอร์ TikTok ไม่ใช่ปัญหาการคลิก
