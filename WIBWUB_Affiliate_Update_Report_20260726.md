# WIBWUB Affiliate Update — วันอาทิตย์ 26 ก.ค. 2569 (automated run, wibwub-thursday-affiliate)

**สรุป: อัปเดตข้อมูล Affiliate ครบ (ครีเอเตอร์/สินค้า/วิดีโอ) ช่วง 1–24 ก.ค. 2569 เข้า `WIBWUB_Affiliate_Dashboard.html` และ `WIBWUB_Mobile.html`, bump sw.js เป็น v463, แก้ `push_now.command` ให้ add+commit+pull+push ครบ (ของเดิมมีแค่ push ลอยๆ จะ fail) — ทุกอย่างแก้ไว้บนดิสก์แล้ว ยังไม่ push ขึ้น GitHub (ต้องดับเบิลคลิก push_now.command เอง)**

⚠️ **มีปัญหาการดาวน์โหลดจาก TikTok ระหว่างรัน (ดูหัวข้อ "ปัญหา" ด้านล่าง) — ใช้ไฟล์ที่ดาวน์โหลดไว้ก่อนหน้าในวันเดียวกันแทน**

---

## ✅ สิ่งที่ทำสำเร็จ

### 1. ดาวน์โหลดไฟล์ export จาก TikTok Affiliate Center
พยายาม export/ดาวน์โหลดไฟล์ ครีเอเตอร์ และ ไลฟ์สตรีม ใหม่ช่วง 1–24 ก.ค. แต่ TikTok endpoint (`export_task/export`) ตอบ **HTTP 503 ทุกครั้ง** (ลองซ้ำหลาย task_id ต่อเนื่องหลายชั่วโมงข้ามวัน) — สรุปว่าเป็นปัญหาฝั่ง TikTok ไม่ใช่ปัญหาการตั้งค่าฝั่งเรา จึงหยุดลองใหม่ และ **ใช้ไฟล์ที่ดาวน์โหลดสำเร็จไว้ก่อนหน้าในวันเดียวกัน (25 ก.ค.) แทน**:
- ครีเอเตอร์: `Transaction_Analysis_Creator_List_20260701-20260724.xlsx` (ช่วง 1–24 ก.ค. ครบ ✅)
- สินค้า: `ListProducts_2026-07-01-2026-07-25_ALLPlan_20260725113231.xlsx` (ครบ ✅)
- วิดีโอ: `Video_List_20260701-20260724_20260725113328.xlsx` (ช่วง 1–24 ก.ค. ครบ ✅)
- ไลฟ์สตรีม: `Transaction_Analysis_Live_List_20260701-20260721.xlsx` (**ช่วงถึงแค่ 21 ก.ค. เท่านั้น เก่ากว่าไฟล์อื่น 3 วัน** — ไฟล์ล่าสุดที่มีอยู่ ไม่มีการประมวลผลต่อเข้า array ใดๆ อยู่แล้วตาม scope ของ task นี้)

### 2. อัปเดตข้อมูลครีเอเตอร์ (ก.ค. 1-24)
- `WIBWUB_Affiliate_Dashboard.html`: overwrite ค่าสุดท้ายของ AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR (index เดิมของเดือน ก.ค. เพราะเป็นการรันซ้ำเดือนเดิม) → **"ก.ค. (1-24)" → GMV ฿1,210,752 · Net ฿1,140,157 · Commission ฿139,226 · ครีเอเตอร์ 658 ราย**
- ตรวจทานยอดจากไฟล์ดิบตรงๆ: sum(GMV) = 1,210,751.90 (ตรงกับ KPI panel บนหน้า TikTok เป๊ะ), creators ที่มียอด = 658 ราย
- `WIBWUB_Mobile.html`: overwrite AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM ชุดเดียวกัน + การ์ด KPI หน้าแรก (`฿1,135K/625 creators (1-22)` → `฿1,211K/658 creators (1-24)`)

### 3. อัปเดต PRODUCTS[].cr / .vid (สินค้า)
Fuzzy-match ชื่อสินค้าทั้ง 7 รายการกับไฟล์ export สินค้า อัปเดตเฉพาะฟิลด์ cr/vid (ฟิลด์อื่นไม่แตะ):
Interior wipes cr 182→197/vid 98→115, Sugar cr 104→113/vid 142→154, Cleaner cr 46→49/vid 28→35, Interior cr 57→63/vid 87→93, Refresh cr 37→43/vid 33→40, Visible cr 13→13/vid 12→14

### 4. ประมวลผลไฟล์วิดีโอ (inlineStr XML)
Parse ด้วย zipfile+regex ได้ **5,667 แถวข้อมูล** (ผ่าน safety check ไม่ใช่ 0 แถว) → match ด้วย vid_id กับ `VIDEOS` array เดิม (**2,146 รายการ**):
- **matched และมียอดเปลี่ยนแปลง: 89 รายการ** → อัปเดต `monthly.jul` และคำนวณ `gmv` รวมใหม่ (mar+apr+may+jun+jul ใหม่)
- matched แต่ยอดเท่าเดิม (ไม่มี GMV เพิ่มในช่วงนี้): 2,033 รายการ — ไม่แตะ
- ไม่พบใน export ใหม่: 24 รายการ — ปล่อยค่าเดิมไว้ (ไม่ทราบว่าเป็นศูนย์จริงหรือเป็นข้อจำกัดของช่วงวันที่ที่ export)
- **ไม่ได้เพิ่มวิดีโอใหม่เข้า array** (มี vid_id ใหม่ ๆ ในไฟล์ export ที่ยังไม่มีใน array ~3,450 รายการ) เพราะไฟล์ export ไม่มีคอลัมน์ "ชื่อสินค้า" (product) ซึ่งเป็น field บังคับของ schema เดิม — เขียนข้อมูลเดาไม่ได้ ปล่อยไว้ก่อนเพื่อความปลอดภัย
- **ไม่ได้แตะฟิลด์ `units`** เพราะตรวจสอบแล้วพบว่าคอลัมน์ที่เป็นไปได้ (F/K) ไม่ตรงกับค่าเดิมในระบบแม้ในกรณีที่ GMV ไม่เปลี่ยน (มีส่วนต่างเล็กน้อยอธิบายไม่ได้ชัดเจน) — เดาสูตรแล้วเขียนทับเสี่ยงข้อมูลผิด จึงปล่อยค่าเดิมไว้
- ตรวจ JS syntax ผ่าน `node --check` ทั้ง `WIBWUB_Affiliate_Dashboard.html` และ `WIBWUB_Mobile.html` แล้ว ไม่มี error

### 5. Bump เวอร์ชัน + สคริปต์ push
- `sw.js`: `wibwub-v462` → `wibwub-v463`
- แก้ `push_now.command`: ของเดิมมีแค่ `git push` ลอยๆ (ไม่มี add/commit จะ push ไม่ได้อะไรใหม่เลย) → เพิ่ม `git add -A`, `git commit`, `git pull --no-edit` ก่อน push เพื่อรองรับ branch ที่ diverge ด้วย

---

## ⛔ ปัญหาที่พบ / ต้องตรวจสอบเพิ่ม

1. **TikTok export 503 error**: ดาวน์โหลดไฟล์ครีเอเตอร์และไลฟ์สตรีมรอบใหม่ไม่สำเร็จเลย (ลองหลาย task_id ข้ามวัน) — ใช้ไฟล์เก่าที่ดาวน์โหลดสำเร็จไว้ก่อนหน้าแทน ผลคือไฟล์ไลฟ์สตรีมเก่ากว่าที่อื่น 3 วัน (ถึง 21 ก.ค. ไม่ใช่ 24 ก.ค.)
2. **Git branch diverged**: local นำหน้า origin/main 18 commits, origin มี 1 commit ที่ local ไม่มี — **ยังไม่ได้ commit/push การเปลี่ยนแปลงรอบนี้** ต้องดับเบิลคลิก `push_now.command` (ตอนนี้จะทำ pull ก่อน push ให้อัตโนมัติ แต่ถ้ามี merge conflict จริงจะหยุดรอให้แก้เอง)
3. **VIDEOS array**: มีวิดีโอใหม่ ~3,450 รายการในไฟล์ export ที่ยังไม่ถูกเพิ่มเข้า dashboard เพราะไฟล์ไม่มีชื่อสินค้า — ถ้าต้องการให้ครบ ต้องหาแหล่งข้อมูลชื่อสินค้าต่อวิดีโอเพิ่ม
4. **ฟิลด์ `units` ของ VIDEOS**: ยังไม่มีสูตรแปลงที่มั่นใจ 100% — ปล่อยค่าเดิมไว้ทุกรายการ

---

## 📊 สถานะ dashboard ปัจจุบัน (หลังแก้ไข)
- Affiliate ก.ค. (1-24): GMV ฿1,210,752 · Net ฿1,140,157 · Commission ฿139,226 · ครีเอเตอร์ 658 ราย
- VIDEOS array: 2,146 รายการ (89 อัปเดตยอด ก.ค., 24 ไม่มีข้อมูลใหม่ในไฟล์นี้, 2,033 ยอดเท่าเดิม)
- sw.js = v463, ยังไม่ commit/push

## ▶️ ขั้นถัดไป (แนะนำ)
1. ดับเบิลคลิก `push_now.command` เพื่อ add + commit + pull + push ขึ้น GitHub (สคริปต์แก้ให้รองรับ diverge แล้ว แต่ถ้า conflict จริงต้องแก้มือ)
2. ลองดาวน์โหลดไฟล์ไลฟ์สตรีมและครีเอเตอร์ใหม่อีกครั้งเมื่อ TikTok หายจากปัญหา 503 เพื่อให้ข้อมูลครบถึง 24 ก.ค. เท่ากันทุกไฟล์
3. พิจารณาหาแหล่งข้อมูลชื่อสินค้าต่อวิดีโอ เพื่อให้เพิ่มวิดีโอใหม่ ๆ (~3,450 รายการ) เข้า VIDEOS array ได้ในอนาคต
