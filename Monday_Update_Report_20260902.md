# WIBWUB Weekly Update — 2 ก.ย. 2569 (พุธ)

**สถานะ: สำเร็จ — ดึงข้อมูลครบ แต่ไม่มีอะไรต้องเขียนลง dashboard รอบนี้**

ไม่มี commit · ไม่ bump sw.js (ยังเป็น `wibwub-v944` ที่ job อื่นตั้งไว้) · ไม่ต้อง push

---

## STEP 0 — M5 guard: **รันแล้ว แต่ revert กลับโดยตั้งใจ**

สคริปต์ตามตัวอักษรเติม `"ก.ย."` เข้า `M5` ทั้งสองไฟล์ (8 → 9) เพราะวันนี้เดือน 9 ผมปล่อยให้มันรัน
แล้วตรวจผลทันที และ **ย้อนกลับเป็น 8 เหมือนเดิม** ด้วยเหตุผลเดียวกับรอบ 1 ก.ย.:

```
SH_REV / TK_REV / LZ_REV / SH_ORD / TK_ORD / LZ_ORD  = length 8
PROD_MO ทุกแถว (mo:[...])                            = length 8
MTD_COVERAGE = { Shopee:'2026-08-31', TikTok:'2026-08-31', Lazada:'2026-08-31' }
```

ถ้าปล่อยให้ M5 ยาว 9 ค้างไว้ `mtdCoverageLabel()` จะอ่าน `M5[M5.length-1]` ได้ `"ก.ย."` แล้วพ่นข้อความ
ขัดแย้งตัวเอง — *"ก.ย. = MTD ถึง 31 ส.ค."* — บวกกับคอลัมน์กันยายนว่างเปล่าในทุกกราฟ

จะเติม `ก.ย.` พร้อมกันกับรอบที่มีข้อมูล Shopee / TikTok / Lazada ของกันยายนจริง

> **ข้อเสนอให้แก้ runbook:** เงื่อนไข `required_months = today.month` ผิดสมมติฐานทุกต้นเดือน
> guard นี้มีไว้กู้เดือนที่หายไป ไม่ใช่สร้างเดือนล่วงหน้า ควรเปลี่ยนเป็น
> `required_months = len(SH_REV)` (ผูกกับ array ข้อมูลจริง) จะได้ไม่ต้อง revert มือทุกต้นเดือน

## STEP 1 — Shipnity export: **สำเร็จ**

ดึงช่วง 1–30 ก.ย. 2569 ผ่าน Claude Chrome (ปุ่ม preset "เดือนนี้" → Export File → ไฟล์เดียว)
LaunchAgent `com.wibwub.download-mover` ย้ายไฟล์ออกจาก Downloads ให้เองเรียบร้อย

`Data Shipnity/Data_02-09-2026.xlsx` — 1,489 แถว (1,484 หลัง dedup), 20 คอลัมน์, product-level ✅

| วันที่ | ยอดขาย |
|---|---|
| 1 ก.ย. 2026 | ฿384,815 |
| 2 ก.ย. 2026 | ฿72,596 *(วันไม่เต็ม)* |
| **รวม** | **฿457,411** |

Top 5 SKU ของกันยายน: Sugar 500ml ฿50,753 · Refresh wipes ฿37,663 · Interior 500ml ฿26,512 ·
Anna nano diamond ฿21,480 · Interior wipes ฿18,484

## STEP 2 — Affiliate export: **ข้าม (ไม่มีข้อมูลใหม่)**

หน้า Performance ของ TikTok Affiliate Center ยังขึ้น **"อัปเดตเมื่อ: 30 ส.ค. 2026 0:00 (GMT+7)"**
เท่าเดิมกับรอบ 01:57 น. ของวันนี้ ไม่มีข้อมูลใหม่ให้ export — ถ้า export ซ้ำจะได้ไฟล์เนื้อหาเดิมทุกประการ
และทำให้โฟลเดอร์รก จึงไม่ดึง

หมายเหตุ: `/insights/transaction-analysis` ยัง redirect ไปหน้า Performance เหมือนที่รอบ 1 ก.ย. รายงานไว้
เป็น blocker เดิมที่ยังรอคนตัดสินใจ

## STEP 3 — Top Products: **ไม่แก้ไข โดยตั้งใจ**

`PROD_MO` มี 8 เดือน (ม.ค.–ส.ค.) ผูกกับ `M5` ที่ยาว 8 การเติมกันยายนเข้าไปตอนนี้ต้องเติม M5 ด้วย
ซึ่งย้อนแย้งกับ STEP 0 ข้างบน และจะพัง invariant ที่รอบ 1 ก.ย. ใช้ตรวจงานคือ
`sum(PROD_MO[x].mo) == ALL_PRODUCTS[x].v`

ข้อมูลกันยายนตอนนี้มีแค่ 2 วัน (วันที่ 2 ยังไม่จบวัน) — รอบหน้าจะครอบคลุมให้อยู่แล้ว
ฝั่งสิงหาคมไม่มีไฟล์ export ใหม่วันนี้ จึงไม่มี delta ให้บวก

**สรุป: `ALL_PRODUCTS` และ `PROD_MO` ไม่ถูกแตะ** — ตรงกับแผนที่รอบ 1 ก.ย. เขียนไว้ว่าจะทำรอบ 7 ก.ย.

## STEP 4 — Affiliate arrays: **ไม่แก้ไข**

ไม่มีข้อมูลต้นทางใหม่ `AF_MO` / `AF_GMV` / `AF_NET` / `AF_COM` / `AF_CR` และ
`AFI_MONTHS` / `AFI_GMV` / `AFI_NET` / `AFI_COMM` คงเดิมทั้งหมด (รอบ 01:57 ตรวจแล้วว่าตรงกับตัวเลขสดบนเว็บ)

## STEP 5 — sw.js + commit: **ข้าม**

ไม่มีการเปลี่ยนแปลงข้อมูลใน `WIBWUB_Mobile.html`, `WIBWUB_Dashboard.html`, `WIBWUB_Affiliate_Dashboard.html`
จากรอบนี้ การ bump cache version โดยไม่มีเนื้อหาใหม่มีแต่จะบังคับให้ผู้ใช้โหลดใหม่ฟรี ๆ

**ตรวจแล้ว:** `git status` ของ `WIBWUB_Mobile.html`, `WIBWUB_Dashboard.html`, `sw.js` = สะอาด
(M5 ที่แก้แล้ว revert กลับตรงกับ HEAD ทุกไบต์)

---

## ⚠️ เรื่องที่ต้องให้คนดู

**1. `WIBWUB_Affiliate_Dashboard.html` มีการแก้ค้างอยู่ที่ยังไม่ commit — ไม่ใช่ของรอบนี้**

`git diff` = +1,227 / −1,198 บรรทัด อยู่ใน array `CREATORS` (ตัวเลข gmv/comm/orders ขยับขึ้นเล็กน้อย
และมีการสลับอันดับ เช่น `.namoshop125` แซง `pailong05`) ไฟล์ถูกแก้เวลา **02:05:07** ซึ่งเป็นเวลาเดียว
กับ commit `f57fc72` ("auto: update TikTok product sales data 2026-09-02 02:05:27") — น่าจะเป็น job
อื่นที่แก้ไฟล์แล้ว commit ไม่ติด

ผมไม่ commit ให้ เพราะ (ก) ไม่ใช่การแก้ของรอบนี้ ตรวจที่มาไม่ได้ และ (ข) ตอนตรวจพบว่ามี
`.git/index.lock` ค้างอยู่ แปลว่ามี git process อื่นทำงานพร้อมกัน — commit ทับตอนนี้เสี่ยงชนกัน

**ควรทำ:** ดูว่า job ไหนแก้ แล้ว commit ด้วยมือ หรือ `git checkout` ทิ้งถ้าเป็นผลลัพธ์ที่ล้มเหลวครึ่งทาง

**2. TikTok Affiliate ค้างที่ 30 ส.ค. เป็นวันที่ 3 แล้ว**

รายงาน 01:57 บอกว่า "หากพรุ่งนี้ยังค้างที่ 30 ส.ค. อีก น่าจะเป็นปัญหาฝั่ง TikTok ที่ควรแจ้ง support"
— ตอนนี้ครบเงื่อนไขนั้นแล้ว

**3. PRODUCTS[].cr / .vid ยังไม่ถูกแก้** — blocker เดิมจากรอบ 01:57 (mapping ชื่อไทย↔อังกฤษ)
รอคนยืนยัน mapping 7 คู่
