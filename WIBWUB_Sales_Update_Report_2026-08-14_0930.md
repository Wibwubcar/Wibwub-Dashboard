# WIBWUB Sales Sheet Update — รายงานรอบ 2026-08-14 09:30

## สรุปผล: ไม่มีข้อมูลใหม่ — ไม่แก้ไขไฟล์ ไม่ commit

อ่านครบทั้ง 3 Sheets ในรอบนี้ (รวม TikTok ที่รอบก่อนอ่านไม่ได้) แล้วพบว่าค่าทุกตัวที่ควรอัปเดต
**ตรงกับค่าที่มีอยู่ใน array ปัจจุบันทุกประการ** จึงไม่มีการแก้ไขไฟล์ใดๆ

## STEP 0 — Verification (verify-only, ไม่แก้ไฟล์)

ตรวจความยาว array ทั้ง 2 ไฟล์: `M5`, `SH_REV`, `TK_REV`, `LZ_REV`, `SH_ORD`, `SH_CANCEL(_PCT)`,
`SH_ADS`, `SH_FEE`, `LZ_ADS`, `LZ_FEE`, `LZ_COUPON`, `LZ_COST_PCT`, `MONTH_LABELS_FULL/SHORT`,
`MP_MONTH_BOUNDS` — **ทั้งหมด = 8 elements (ม.ค.–ส.ค.) ตรงกันหมด ไม่มี mismatch**
บั๊กเดือน ส.ค. ที่เคยเกิดเมื่อ 2026-08-03 ไม่กลับมาซ้ำ

## ข้อมูลที่อ่านได้จาก Sheets

| Platform | แถวสุดท้าย | ยอดขาย | ค่าใน array ปัจจุบัน | สถานะ |
|---|---|---|---|---|
| Shopee | `01-09/08/26` | ฿2,306,333 | `SH_REV[7]` = 2306333 | ✅ ตรงกัน |
| TikTok | `01-12/08/26` | ฿974,777.29 | `TK_REV[7]` = 974777.29 | ✅ ตรงกัน |
| Lazada | `01-09/08/26` | ฿42,724.80 | `LZ_REV[7]` = 42724.8 | ✅ ตรงกัน |

รายละเอียดประกอบที่ตรงกันทั้งหมด:
Shopee 3,816 orders · ยกเลิก 6.29% · ค่าธรรมเนียม ฿690,977.37 ·
Lazada Ads ฿4,290 · fee ฿7,546.13 · coupon ฿990 · cost 30.02%

Shopee/Lazada ยังคงเป็น cumulative ถึง 09/08/26 เหมือนรอบ 2026-08-11 และ 2026-08-13
ส่วน TikTok ถูกอัปเดตเป็น 01-12/08/26 ไปแล้วตั้งแต่รอบ 2026-08-13 (commit `898a8f8`)

## ⚠️ ประเด็นที่ตรวจพบและ **จงใจไม่แก้** — `SH_ADS[7]`

- ค่าใน array ปัจจุบัน: **฿414,752.47**
- ค่าใน Sheet แถว `01-09/08/26` คอลัมน์ "ยอดใช้ ads": ฿314,347.19 (Shopee-only)
- ค่าใน Sheet แถวซ้ำ (รวม facebook C-pass + Shopee C-pass): ฿432,482.25

ค่า ฿414,752.47 มาจาก commit `0f48313` "Shopee Ads: refresh Aug to official 01-13 Aug export"
คือ **Shopee Ads export อย่างเป็นทางการถึง 13 ส.ค.** ซึ่งใหม่และแม่นยำกว่าตัวเลขในชีตที่ยังหยุดอยู่ที่ 9 ส.ค.
การเขียนทับด้วยค่าจากชีตจะทำให้ยอด Ads **ถอยหลัง** จึงคงค่าเดิมไว้

**ข้อควรระวังรอบถัดไป:** เมื่อพนักงานอัปเดตชีตถึงวันที่ใหม่กว่า 13 ส.ค. แล้ว ให้เทียบกับ
`data Ads/WIBWUB_Ads_Dashboard.html` ก่อนเขียนทับ `SH_ADS` เสมอ — ใช้ค่าที่ครอบคลุมวันที่ล่าสุดกว่า
และใช้คอลัมน์ Shopee-only (ไม่รวม facebook C-pass) ให้สอดคล้องกับเดือนก่อนหน้า
(เช่น ก.ค. = 942,122.40 ซึ่งเป็น Shopee-only)

## Action ที่ทำ

- ❌ ไม่แก้ไข `WIBWUB_Dashboard.html`, `WIBWUB_Mobile.html`
- ❌ ไม่ bump `sw.js` — ไม่มีเนื้อหาเปลี่ยน จึงไม่ต้อง invalidate cache
- ❌ ไม่ git commit / ไม่สร้าง push_now.command

## หมายเหตุทางเทคนิค — วิธีอ่าน TikTok sheet สำเร็จ

TikTok sheet (`1k22c3PGY6aQjygAX6df_rQLR8aTzL-iz`, tab `gid=150856480`) export เป็น markdown
ได้ ~167k tokens เกิน limit ของทุก tool และไฟล์ temp ที่ MCP เขียนไว้ที่ `/var/folders/.../tool-results/`
**bash sandbox เข้าไม่ถึง** (mount ไม่ครอบคลุม path นี้) — เป็นสาเหตุที่รอบ 2026-08-13 อ่านไม่ได้

รอบนี้แก้ได้ด้วยการ **ดึง gviz CSV ผ่าน Claude in Chrome แทน** ซึ่งได้ข้อมูลครบ
แนะนำให้ใช้วิธีนี้เป็นค่าเริ่มต้นสำหรับ TikTok sheet ในรอบถัดๆ ไป แทนการเรียก
`read_file_content` ที่จะล้มเหลวเสมอ
