# รายงาน sync "คำขอสินค้า" — 31/08/2026

แหล่งข้อมูล: Google Sheet "Top 30 Affiliate" → แท็บ "ขอสินค้า" (ดึงสดผ่าน Chrome, 1,451 แถวที่มีวันที่, ไม่มีวันที่ผิดรูปแบบ)

## เดือนปัจจุบัน — ส.ค. 2569 (01/08 - 31/08)

| รายการ | ค่า |
|---|---|
| คำขอทั้งหมด | 299 (เดิม 298, +1 แถวใหม่วันที่ 31/08) |
| อนุมัติ (Approve) | 136 |
| ปฏิเสธ (Reject) | 163 |
| อื่นๆ (On Process/Done/Waiting) | 0 |
| ไม่ระบุสินค้า | 0 |

### สินค้าที่ Creator ขอ — ส.ค. 2569

| สินค้า | ขอ | อนุมัติ | ปฏิเสธ |
|---|---|---|---|
| Refresh Wipes | 150 | 77 | 73 |
| Interior wipes | 101 | 44 | 57 |
| Interior | 33 | 12 | 21 |
| Reflex | 27 | 11 | 16 |
| Sugar | 10 | 5 | 5 |

ผลรวมรายสินค้า (321) มากกว่ายอดคำขอ (299) เพราะหนึ่งคำขอระบุได้หลายสินค้า — เป็นพฤติกรรมที่ถูกต้องตามที่ระบุไว้ในหมายเหตุบนหน้าเว็บ

## 7 วันล่าสุด (25/08 - 31/08)

รวม 40 คำขอ — Refresh Wipes 23 (อนุมัติ 8 / ปฏิเสธ 15), Interior wipes 11 (1/10), Interior 4 (1/3), Reflex 4 (2/2)

**หมายเหตุ:** ตัวเลขลดจากรอบก่อน (82 → 40) เพราะหน้าต่าง 7 วันเลื่อนผ่านวันที่ 24/08 ซึ่งมีการบันทึกก้อนใหญ่ 43 แถว ตรวจสอบยอดรายวันแล้วยืนยันว่าถูกต้อง ไม่ใช่ข้อมูลหาย

## การตรวจสอบ

- เดือนย้อนหลังทั้งหมด (ต.ค. 68 – ก.ค. 69) ตรงกับค่าที่บันทึกไว้เดิมทุกเดือน — **ไม่มี anomaly**, ไม่ได้แก้ไขข้อมูลเดือนเก่า
- โครงสร้างคอลัมน์ตรงตามที่คาด ใช้คอลัมน์ 4 "สินค้าที่ Creator ขอ" (ไม่ใช่คอลัมน์ 5 "สินค้าที่ให้") ตามที่กำหนด
- `node --check` ผ่าน ไม่มี syntax error
- diff เทียบกับ HEAD แตะเฉพาะ SAMPLE_MONTHS / SAMPLE_PRODUCTS / SAMPLE_PRODUCTS_LAST7D (5 บรรทัด) เท่านั้น
- sw.js: v908 → **v909**
- Commit: `be8020b`

## ข้อสังเกตที่ยังค้าง (ไม่ได้แก้ในรอบนี้)

`SAMPLE_LAST7D.funnel` ยังเป็นค่าเก่า (req 10, ช่วง 12/08-18/08) ซึ่งไม่ตรงกับ `SAMPLE_PRODUCTS_LAST7D` (40) — เป็นปัญหาเดิมที่มีหมายเหตุกำกับไว้ในโค้ดและแสดงบนหน้าเว็บอยู่แล้ว อยู่นอกขอบเขตงานรอบนี้

---
⚠️ **ดับเบิลคลิก `push_now.command` เพื่อ deploy ขึ้นเว็บ**
