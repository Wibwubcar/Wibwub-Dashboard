# ✅ รายงานการอัปเดตข้อมูล Affiliate (WIBWUB) — 12 ส.ค. 2026 (scheduled: wibwub-thursday-affiliate)

## ช่วงวันที่ข้อมูล
1–9 สิงหาคม 2026 (GMT+7) — 9 ส.ค. คือวันล่าสุดที่ TikTok มีข้อมูลให้ export (แม้วันนี้จะเป็น 12 ส.ค.)

## ไฟล์ที่ดาวน์โหลดและย้ายสำเร็จ
ทั้ง 4 ไฟล์ดาวน์โหลดสำเร็จ (ครีเอเตอร์/วีดีโอ/ไลฟ์สตรีมค้างสถานะ "กำลังส่งออก" นานผิดปกติ ต้อง reload หน้าเว็บก่อนจะเห็นปุ่มดาวน์โหลด) และถูก auto-move เข้าโฟลเดอร์ที่ถูกต้องแล้ว:
- `Data Affiliate/ครีเอเตอร์/Transaction_Analysis_Creator_List_20260801-20260809.xlsx` (4,987 ครีเอเตอร์)
- `Data Affiliate/สินค้า/Transaction_Analysis_Product_List_20260801-20260809.xlsx` (72 สินค้า)
- `Data Affiliate/วีดีโอ/Transaction_Analysis_Video_List_20260801-20260809.xlsx` (4,481 แถว, 332 แถวมี GMV > 0)
- `Data Affiliate/ไลฟ์สตรีม/Transaction_Analysis_Live_List_20260801-20260809.xlsx`

หมายเหตุ: พบว่า dashboard มีค่า "ส.ค. (1-9)" อยู่แล้วก่อนรอบนี้ (จากไฟล์ที่ดาวน์โหลดไว้เมื่อ 11 ส.ค. แต่ไม่มีรายงานบันทึกไว้) — ตัวเลขที่คำนวณได้จากไฟล์ใหม่รอบนี้ใกล้เคียงกับค่าเดิมมาก (<0.1%) ยืนยันว่าข้อมูลเสถียรแล้วสำหรับช่วง 1-9 ส.ค.

## สรุปตัวเลข (ครีเอเตอร์ — อัปเดตแล้ว)
- **GMV**: ฿484,852 (เดิม ฿484,748)
- **Net GMV** (GMV − การคืนเงิน): ฿474,423 (เดิม ฿474,319)
- **Commission**: ฿56,606 (เดิม ฿56,459)
- **ครีเอเตอร์ที่ active** (GMV>0): 321 ราย (เดิม 321 ราย — เท่าเดิม)
- Label คงเดิม "ส.ค. (1-9)" — overwrite ค่าเดิมของเดือน ส.ค. ตามกฎ rolling-window (label ตรงกับช่วงข้อมูลปัจจุบันพอดี จึงไม่ append)
- อัปเดตทั้ง `WIBWUB_Affiliate_Dashboard.html` (AF_MO/AF_GMV/AF_NET/AF_COM/AF_CR) และ `WIBWUB_Mobile.html` (AFI_MONTHS/AFI_GMV/AFI_NET/AFI_COMM)

## PRODUCTS (cr/vid) — ⏭️ ข้าม ไม่อัปเดตรอบนี้
ปัญหาเดิมที่บันทึกไว้ต่อเนื่องตั้งแต่ 1, 6, 8, 10 ส.ค.: คอลัมน์ "วิดีโอ"/"ไลฟ์สตรีม" ในไฟล์สินค้าเป็นตัวเลขเฉพาะช่วงวันที่ที่เลือก (1-9 ส.ค.) เท่านั้น ไม่ใช่ตัวเลขสะสม ในขณะที่ `cr`/`vid` ใน PRODUCTS array ปัจจุบันเป็นตัวเลขสะสมข้ามเดือน (เช่น cr:78, vid:35 สำหรับ Leather Wipes) ไม่มีสูตรแปลงที่ชัดเจน จึง**ไม่แตะ** field นี้ต่อเนื่องเพื่อป้องกันข้อมูลเสียหาย

## VIDEOS array — ⏭️ ข้าม ไม่อัปเดตรอบนี้ (ยืนยันปัญหาเดิมซ้ำ)
ไฟล์วิดีโอเปิดด้วย `pandas.read_excel` ได้ปกติ (แต่ต้องระบุ `dtype={'รหัสวิดีโอ': str}` เพราะ vid_id เป็นเลข 19 หลักซึ่งเกินความแม่นยำของ float64 — ไม่ทำแบบนี้จะจับคู่ vid_id ไม่ตรงเลย) หลังแก้ปัญหา precision แล้ว จับคู่ vid_id กับ VIDEOS array เดิมได้ 269 รายการ (จาก 332 แถวที่มี GMV > 0) แต่เมื่อเทียบ GMV ดิบจากไฟล์กับค่า `monthly.aug` ที่บันทึกไว้ พบว่า **ยังคงไม่สอดคล้องกันอย่างมีนัยสำคัญ** — ค่าดิบสูงกว่าค่าที่บันทึกไว้ประมาณ 1.5–10 เท่า ไม่มีอัตราส่วนคงที่ (ตัวอย่าง: vid 7641925575564627208 ราคาดิบ ฿29,443 vs stored ฿6,435 = 4.6x; vid 7645490747550010642 ราคาดิบ ฿6,130 vs stored ฿604 = 10.1x)

**นี่คือรอบที่ 2 ติดต่อกัน** (หลังจาก 10 ส.ค.) ที่พบปัญหาเดียวกัน ยืนยันว่าไม่ใช่ความผิดพลาดชั่วคราว — น่าจะเป็นการเปลี่ยนวิธีคำนวณ/attribution window ของ TikTok อย่างถาวร จึง**ไม่เขียนทับ** VIDEOS array ต่อเนื่อง เพื่อป้องกันข้อมูลผิดพลาด **แนะนำให้ตรวจสอบด้วยตนเองอย่างจริงจัง** ว่าจะ recalibrate สูตรแปลงค่าอย่างไร หรือเปลี่ยนไปใช้ค่าดิบจากไฟล์แทนค่าที่บันทึกไว้เดิมทั้งหมด

## sw.js
Bump cache version: `wibwub-v644` → **`wibwub-v645`**

## push_now.command
พบว่าไฟล์เดิม (แก้ไขล่าสุดโดยกระบวนการอื่นเมื่อ 02:21) เป็น push-only อีกครั้ง (ไม่มี add+commit) — แก้ให้ทำ `git add -A` + `git commit` + `git pull --no-edit` + `git push` ครบในคลิกเดียว เหมือนที่เคยแก้ไว้ในรายงาน 6 ส.ค. และ 10 ส.ค.

**⚠️ ยังไม่ได้ push ขึ้น GitHub — กรุณาดับเบิลคลิก `push_now.command` เพื่อ commit และ push การเปลี่ยนแปลงรอบนี้** (หมายเหตุ: `git status` พบไฟล์อื่นที่ถูกแก้จากกระบวนการอื่นด้วย เช่น stock_snapshot.json, Followers_wibwubcar.zip, Order.all zip, auto_push.log และรายงาน .md จำนวนมากที่ยังไม่ commit — สคริปต์ใช้ `git add -A` จะ commit ไฟล์เหล่านั้นไปด้วย)

## ตรวจสอบความถูกต้อง
- คำนวณตัวเลข GMV/Net/Commission/ครีเอเตอร์ ด้วย pandas จากไฟล์ดิบโดยตรง (ไม่ได้อ่านจากหน้าเว็บ)
- Cross-check กับค่าที่บันทึกไว้เดิมในระบบ (จากรอบก่อนหน้าที่ไม่มีรายงาน): GMV/Net/Commission ต่างกัน <0.1-0.3%, จำนวนครีเอเตอร์ตรงเป๊ะ (321=321) — ยืนยันว่าสูตรถูกต้องและข้อมูลเสถียร
- ตรวจ JS syntax ผ่าน `node --check sw.js` — ไม่มี error; ตรวจ HTML files ด้วย grep ยืนยันค่าที่แก้ไขถูกบันทึกจริงในไฟล์ทั้งสอง
- ตรวจ VIDEOS/PRODUCTS ก่อนตัดสินใจ skip — ทำ safety check เทียบ GMV ดิบ vs stored ก่อนทุกครั้งตามระเบียบ ไม่ merge ข้อมูลที่ไม่ผ่าน check

---
*รายงานสร้างโดยระบบอัตโนมัติ (scheduled task: wibwub-thursday-affiliate)*
