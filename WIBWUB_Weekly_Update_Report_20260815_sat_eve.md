# WIBWUB Weekly Update — รายงาน (เสาร์ 15 ส.ค. 2026, รอบเย็น)

**สรุป:** ดาวน์โหลด Shipnity รอบใหม่สำเร็จ · Affiliate ไม่มีข้อมูลใหม่ · **ไม่ได้แก้ไฟล์ dashboard ใด ๆ ในรอบนี้**

---

## 🛡️ Protection check — ผ่าน
`const M5` ใน `WIBWUB_Dashboard.html` และ `WIBWUB_Mobile.html` มีครบ 8 เดือน (ม.ค.–ส.ค.) ตรงกับเดือนปัจจุบัน ไม่ต้องแก้

---

## STEP 1 — Shipnity export ✅
ดึงผ่าน Claude Chrome (Mac browser `b75a6bb0`) หน้า ประวัติ → สินค้าในออเดอร์

| รายการ | ค่า |
|---|---|
| ช่วงวันที่ | 1 ส.ค. 2569 ~ 15 ส.ค. 2569 |
| รูปแบบ | .xlsx / ไฟล์เดียว |
| ไฟล์ | `Data Shipnity/Data_15-08-2026.xlsx` |
| ขนาด | 16,094,055 B (เดิม 02:22 = 15,379,232 B) |
| แถวข้อมูล | 17,053 (ตรวจแล้ว: 01/08/2026 → 15/08/2026) |

LaunchAgent `com.wibwub.download-mover` ย้ายไฟล์เข้าโฟลเดอร์ปลายทางอัตโนมัติตามปกติ

---

## STEP 2 — Affiliate export ⏭️ ข้าม (ไม่มีข้อมูลใหม่)
หน้า Transaction Analysis แสดง **"อัปเดตเมื่อ: 13 ส.ค. 2026 0:00 (GMT+7)"** — ค่าเดียวกับรอบ 13:06 ของวันนี้
TikTok ยังไม่ปล่อยข้อมูลวันที่ 14 จึงไม่ export ซ้ำ เพื่อไม่ให้ได้ไฟล์ที่เหมือนเดิมทุกประการ

---

## STEP 3 — Top Products ⛔ ไม่ได้อัปเดต (ตั้งใจหยุด)

รันการรวมข้อมูลจากไฟล์รายเดือนครบ 8 ไฟล์ (ม.ค.–ส.ค.) — ตรวจ column วันที่สร้างแล้ว **ทุกไฟล์มีเดือนเดียวตรงตามชื่อ ไม่มีเดือนปน**:
`ม.ค. 27,979 · ก.พ. 24,469 · มี.ค. 22,269 · เม.ย. 23,771 · พ.ค. 28,620 · มิ.ย. 24,250 · ก.ค. 30,889 · ส.ค. 17,028 แถว`

**ปัญหาที่พบ:** ตัวเลขที่คำนวณได้ **ไม่ตรงกับค่าที่เก็บอยู่ใน `PROD_MO` ทุกเดือน** ไม่ใช่แค่เดือนปัจจุบัน

| สินค้า | เดือน | คำนวณได้ | ที่เก็บอยู่ | ต่าง |
|---|---|---|---|---|
| Wool Duster | ม.ค. | 1,495,083 | 1,507,099 | −12,016 |
| Wool Duster | ก.ค. | 377,588 | 399,467 | −21,879 |
| Sugar 500ml | ส.ค. | 436,874 | 446,706 | −9,832 |

ทดสอบวิธีรวมข้อมูล 4 แบบกับเดือน ม.ค. (เป้า 1,507,099) — **ไม่มีแบบไหนตรง**:

| วิธี | ผลลัพธ์ |
|---|---|
| dedup (order, sku, qty) × ราคา×จำนวน | 1,495,083 |
| ไม่ dedup × ราคา×จำนวน | 1,497,132 |
| dedup (order, sku) × ราคา×จำนวน | 1,494,393 |
| ไม่ dedup × col5 ยอดขายออเดอร์ | 2,151,499 |

**สาเหตุ:** ค่าที่เก็บอยู่มี logic ที่ยังไม่มีสคริปต์รองรับ — สังเกตจาก field `mk` / `mkq` ใน `ALL_PRODUCTS` ซึ่งเป็นการแยกยอดแจกการตลาดออกมาต่างหาก และการ map ชื่อ SKU ดิบ → ชื่อแสดงผลภาษาไทย

**สิ่งที่ทำ:** ไม่แตะ `ALL_PRODUCTS` / `PROD_MO` เลย ตามกฎเหล็กของ skill ("ห้ามสร้าง array ใหม่ทั้งหมด" / "ถ้าไม่แน่ใจ ให้หยุดและ log แทนที่จะเดา") การเขียนทับด้วยตัวเลขที่คำนวณจากคนละวิธี จะทำให้ series ขาดตอนกลางทาง

> ⚠️ รอบวันที่ 29 ก.ค. สรุปเหมือนกันนี้มาแล้วโดยอิสระ — **นี่คือปัญหาซ้ำ ไม่ใช่เหตุบังเอิญ** ควรเขียนและ vet สคริปต์ ALL_PRODUCTS-from-Shipnity ให้เรียบร้อยก่อน จึงจะ automate step นี้ได้จริง

---

## STEP 4 — Affiliate arrays ✅ ปัจจุบันแล้ว ไม่ต้องแก้
ตรวจค่าจริงในไฟล์ ตรงกันทั้งสองไฟล์ และ label ตรงกับช่วงข้อมูล (1-13):

| | GMV | NET | COM | CR |
|---|---|---|---|---|
| ส.ค. (1-13) | 699,495 | 683,618 | 82,404 | 409 |

- `AF_MO` / `AF_GMV` / `AF_NET` / `AF_COM` / `AF_CR` → ยาว 8 เท่ากันหมด ✓
- `AFI_MONTHS` / `AFI_GMV` / `AFI_NET` / `AFI_COMM` → ยาว 10 เท่ากันหมด ✓
- ไม่มีเดือนก่อนหน้าถูกเขียนทับ

---

## STEP 5 — sw.js / commit ⏸️ ไม่มีอะไรใหม่จากรอบนี้
รอบนี้ไม่ได้แก้ไฟล์ dashboard ใด ๆ จึง **ไม่ bump sw.js และไม่สร้าง commit ซ้อน** (คงที่ `wibwub-v680`)

**ยังค้างอยู่จากรอบ 13:06 (ยังไม่ push):**
```
M WIBWUB_Affiliate_Dashboard.html
M WIBWUB_Mobile.html
M sw.js
```
`push_now.command` ที่รอบก่อนเขียนไว้ครอบคลุมไฟล์เหล่านี้แล้ว (add + commit + push)

### 👉 ต้องทำต่อ
ดับเบิลคลิก **`push_now.command`** เพื่อ commit + push งาน Affiliate 1-13 ส.ค. ขึ้น GitHub

---

## หมายเหตุการรัน
- `browser_batch` ใช้กับ tab ที่อยู่ที่ `chrome://newtab` ไม่ได้ → ต้อง `navigate` เดี่ยว 1 ครั้งก่อน (ไม่ hang) แล้วค่อย batch ต่อ — ตรงกับที่รอบก่อนบันทึกไว้
- Date picker ของ Shipnity: ปุ่มลัด "เดือนนี้" กดแล้ว label ไม่เปลี่ยน ต้องคลิกวันที่ 1 แล้ววันที่ 15 ในปฏิทินเอง
- ต้องคลิกนอก popup ให้ปฏิทินปิดก่อน ปุ่ม "ส่งออกข้อมูล" จึงจะ enable
- Export ไฟล์เดียว 16MB ใช้เวลาราว 2 นาที
