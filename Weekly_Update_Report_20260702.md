# WIBWUB Weekly Update — รายงานการรัน 2 ก.ค. 2569 (2026-07-02)

สรุป: รันเป็น automated task วันนี้ **ยังไม่สามารถอัปเดตข้อมูลเดือน ก.ค. เข้า dashboard ได้อย่างปลอดภัย** เพราะข้อมูลเดือนกรกฎาคมยังไม่พร้อม + โครงสร้าง array ของ dashboard ไม่ตรงกับสมมติฐาน month index ของสคริปต์ จึงเลือกทำแบบระมัดระวัง (ไม่ commit การเปลี่ยนแปลงที่จะทำให้หน้า dashboard พัง) และรายงานผลแทน

---

## ✅ สิ่งที่ทำสำเร็จ

1. **Protection M5** — ตรวจแล้ว รันแล้ว (ดูหมายเหตุด้านล่างเรื่องการ revert)
2. **STEP 1 — ดาวน์โหลด Shipnity สำเร็จ**
   - ตั้งช่วงวันที่ = 1–31 ก.ค. 2569, filter "สินค้าในออเดอร์" (product-level)
   - Export ได้ไฟล์ `Data_02-07-2026.xlsx` (1.1 MB) มีข้อมูลจริง (ออเดอร์ 1–2 ก.ค.)
   - บันทึกเป็น `Data Shipnity/Data_กรกฎาคม.xlsx`

## ⛔ สิ่งที่ติดขัด / ยังทำไม่ได้

### STEP 2/4 — Affiliate เดือน ก.ค. **ยังไม่มีข้อมูล**
- หน้า Transaction Analysis ระบุ **"อัปเดตเมื่อ: 30 มิ.ย. 2026"** — TikTok ยังไม่ปล่อยข้อมูลเดือน ก.ค.
- ในปฏิทินเลือกวันที่ **วันที่ 1–2 ก.ค. ถูก disable ทั้งหมด** เลือกไม่ได้ → export เดือน ก.ค. ไม่ได้
- จึง **ไม่ได้ดาวน์โหลดไฟล์ affiliate ใหม่** (ไฟล์ล่าสุดที่มีคือ `Transaction_Analysis_Creator_List_20260601-20260627.xlsx` = เดือน มิ.ย.)

### ปัญหาโครงสร้างข้อมูล (สำคัญ — ทำให้รัน STEP 3/4 แบบอัตโนมัติไม่ปลอดภัย)
สคริปต์ในไฟล์ task สมมติว่า `array[month_idx]` โดย `month_idx = เดือน - 1` (0 = ม.ค.) แต่ array จริงใน dashboard ใช้ index คนละแบบ:

- **Mobile `AFI_GMV/NET/COMM`** ใช้ `AFI_MONTHS = [พย.68, ธค.68, มค.69, กพ.69, มีนา.69, เมษา.69, พค.69, มิย.69]` → index เริ่มที่ **พ.ย. 2568** ไม่ใช่ ม.ค. ดังนั้น `month_idx=6` จะชี้ไปที่ **พ.ค.** ไม่ใช่ ก.ค. → เขียนทับข้อมูลเดือน พ.ค. เสียหาย
- **Affiliate Dashboard** ติดตามแค่ มี.ค.–มิ.ย. (เห็น label `['มี.ค.','เม.ย.','พ.ค.','มิ.ย.(1-27)']`)
- **Sales arrays** (`SH_REV, TK_REV, TOTAL_REV, ...`) มี **6 ช่อง (ม.ค.–มิ.ย.)** ทั้งหมด ยังไม่มีช่องเดือน ก.ค.

### M5 Protection ทำให้เกิด mismatch → revert แล้ว
- Protection บังคับเพิ่ม "ก.ค." เข้า `M5` (6→7 ช่อง) ในทั้ง 2 ไฟล์
- แต่ array ข้อมูลทุกตัวยังมี 6 ช่อง → ถ้าปล่อยไว้ กราฟ/ตารางทุกอันบนหน้า dashboard จะขึ้นคอลัมน์ "ก.ค." ว่าง/NaN (หน้าพัง)
- การอัปเดต sales arrays ให้มีเดือน ก.ค. **อยู่นอกขอบเขต** ของ task นี้ (task ครอบแค่ Shipnity Top Products + Affiliate) และเดือน ก.ค. เพิ่งมีข้อมูล 1–2 วัน
- **จึง revert `M5` กลับเป็น 6 ช่องทั้ง 2 ไฟล์** เพื่อรักษาความสอดคล้อง — ตอนนี้ `WIBWUB_Mobile.html` และ `WIBWUB_Dashboard.html` กลับสู่สถานะเดิม (ไม่มี diff)

### STEP 3 — Top Products: เลือกไม่แก้ในรอบนี้
- `ALL_PRODUCTS` ปัจจุบันเป็นข้อมูล curated ม.ค.–มิ.ย. (updated 30.06.69) และ consistent (ผลรวม `PROD_MO` = `ALL_PRODUCTS.v`)
- โฟลเดอร์ `Data Shipnity/` มีไฟล์ปนกันเยอะมาก (ไฟล์รายเดือน + ไฟล์รายวัน + ไฟล์ซ้ำ `(1)(2)`) การ re-aggregate อัตโนมัติเสี่ยงนับซ้ำ/ได้ตัวเลขเพี้ยน ขณะที่ ก.ค. เพิ่มข้อมูลแค่ 2 วัน (ผลต่อ Top-15 สะสมแทบไม่มี)
- จึงเลือก **ไม่เขียนทับ Top Products** ในรอบนี้เพื่อเลี่ยงความเสี่ยง

### STEP 5 — ไม่ commit / ไม่ bump sw.js
- ไม่มีการแก้ไขที่ปลอดภัยพอจะ ship ในรอบนี้ จึงไม่ commit และไม่สร้าง push ใหม่
- หมายเหตุ: working tree มีไฟล์ค้าง uncommitted จากรอบก่อน (`WIBWUB_Affiliate_Dashboard.html`, `push_now.command`, โฟลเดอร์ `Data Affiliate/`, รายงานเก่าหลายไฟล์) — **ไม่ได้แตะ** ไฟล์เหล่านี้

---

## ข้อเสนอ / สิ่งที่ควรทำต่อ

1. **รอข้อมูล affiliate ก.ค.** — ลองใหม่หลัง TikTok ปล่อยข้อมูล (ปกติหลังต้นเดือน 2–3 วัน)
2. **การ roll over เดือนใหม่ต้องทำพร้อมกันทั้งชุด** — เพิ่มช่องเดือน ก.ค. ให้ครบทั้ง `M5` + sales arrays (`SH_REV, TK_REV, ...`) + `AFI_*` (ต่อท้าย ไม่ใช่ใช้ month_idx) มิฉะนั้นหน้าจะพัง ควรทำผ่าน skill `update-wibwub` ที่เข้าใจ index จริงของแต่ละ array
3. **ทำความสะอาดโฟลเดอร์ `Data Shipnity/`** — ลบไฟล์รายวัน/ไฟล์ซ้ำ ให้เหลือไฟล์รายเดือนไฟล์เดียวต่อเดือน เพื่อให้ re-aggregate Top Products ปลอดภัย
4. หมายเหตุ sandbox: ลบไฟล์ใน `Downloads/` และแก้ `.git/*.lock` ไม่ได้ (Operation not permitted) — เป็นข้อจำกัด mount ไม่กระทบผลลัพธ์

---
สถานะ dashboard ปัจจุบัน: **ไม่เปลี่ยนแปลง (ยังใช้งานได้ปกติ ข้อมูลถึงเดือน มิ.ย.)**
